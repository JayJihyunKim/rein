# Weekly Summary: 2026-W31

---
## 2026-07-27
# 2026-07-27 일일 기록

<!-- from inbox/2026-07-27-persona-select-create-entry.md -->

# 2026-07-27 — 페르소나: 만들기 입구 + 선택지 상한 + 전환 즉시 반영 + 호출 경로 규약

- DoD: `trail/dod/dod-2026-07-27-persona-select-create-entry.md`
- spec ref: `docs/specs/2026-07-22-persona-user-selection.md` §11 (목록 제시 형식 — 미해소 open question 을 이번에 해소)
- 변경: `plugins/rein-core/skills/persona/SKILL.md` + `tests/skills/test-persona-skill.sh` (실행 코드 무변경)
- 등급 판정: user-facing 동작 변화(선택 목록 + 전환 반영) → **minor 후보**. 최종 등급은 릴리스 시 사용자 결정.

## 발단

사용자가 `/rein:persona` 를 인자 없이 호출 → 선택 목록에 "새로 만들기" 가 없어 커스텀 생성에 도달할 수 없음을 발견. 이어서 다른 프로젝트의 실사용 화면을 제보 — 보스 에이스 → 제니 전환 직후 이름을 "지니"로 오독, 호칭이 직전 프리셋의 "보스"로 유지, 인사말을 요약만 보고 창작.

## 근본 원인 2건 (둘 다 이번에 봉합)

1. **세션 중 전환 시 본문 미갱신** — 캐릭터 본문은 세션 시작 훅이 1회 주입할 뿐이고, 매 턴 들어가는 것은 `활성 프리셋: <이름> — <요약>` 1줄뿐(`get_turn_brief()`). 전환해도 직전 본문이 남아 요약과 섞인다. 증상 3종이 이 하나로 설명됨.
2. **로더 호출 형태가 환경변수 존재를 전제** — 스킬 문서가 `${CLAUDE_PLUGIN_ROOT:-plugins/rein-core}/…` 형태로 호출하는데, 스킬의 명령 실행 환경에 그 변수가 **없다**(2026-07-27 실측). 스크립트 파일은 fallback 으로 찾아지지만 로더 내부 경로 해석이 실패해 **오류 없이 빈 출력**을 반환 → 호출자가 "인사말 없음"으로 오해해 즉석 창작. 인사말 창작의 직접 원인.
   - 실측: 변수 없이 `--persona-greeting jennie` → 빈 출력 / 변수 주면 정상 문구. `--persona-file` 도 동일.

## 변경 요약 (5건)

1. 선택 목록에 **새로 만들기** 고정 옵션 추가 (끄기와 함께 실존 프리셋 수와 무관한 고정 2종)
2. **선택지 상한 4개** 명문화 — 총 4 이하 단일 질문 / 5 이상 2단 제시(갈래 → 갈래 내 목록), 갈래 내 4 초과 시 `다음 목록 보기` 로 이어보기, 갈래 항목 1개면 `뒤로 가기` 동반(최소 2개 제약), **도달 가능성 불변식**(상한 때문에 후보 누락 금지)
3. **전환 즉시 본문 반영** 단계 신설 — 기록 직후 로더가 해석한 경로 + 불변층을 읽어 그 턴부터 적용. 요약으로 이름·호칭 추측 금지, 빈 출력이면 호출 의심 후 1회 재시도, 실패 시 추측 말고 중립 보고. 끄기는 직전 말투 즉시 폐기
4. **로더·lint 호출 규약** 신설 — plugin 루트를 스킬 자기 위치(base directory 의 `../..`)에서 유도해 `CLAUDE_PLUGIN_ROOT` 로 명시 전달. 셸 상태가 호출 간 유지되지 않으므로 예시 4곳 전부 `PR=` 정의 동반. 빈 출력을 "비활성"으로 오해 금지 — 내장 프리셋이 비면 호출 결함이므로 창작 금지
5. **인사말 후보 선택** — 생성 흐름 초안 확인 단계에서 후보 2~3개 제시 + `직접 쓸게요` 경로. 7문항 세트·8번째 질문 부재 불변식은 그대로 유지(제시는 확인 단계 소속)

## 검증

- persona 스킬 검사 94/94 통과 (신설 (h)(i)(j)(k) 4그룹 39건 포함). skills/hooks/scripts 전 스위트 통과, 미러 정합 통과
- **실동작 확인**: 규약대로 유도한 경로로 본문 경로·인사말·검사 스크립트 3개 호출 모두 정상 반환

## 리뷰

- 코드: codex 워치독이 정지로 판정해 종료 → 규칙대로 대체 리뷰. round 1 에서 High 1(셸 변수 미유지로 수정 무효화) + Medium 2 + Low 1 지적 → 전부 수정, round 2 에서 Medium 1(검사 스크립트 호출 예시 누락) 추가 수정 후 통과
  - ⚠️ **이 정지 판정은 오판으로 확정** (2026-07-27 사후 분석). 로그 마지막 동작이 `bash tests/skills/run-all.sh` 실행이고, 그 명령의 실측 소요는 **219초**. codex 는 자식 명령 실행 중에는 출력을 내지 않고 완료 시점에 일괄 방출하므로 219초간 출력 성장이 0 이 된다. 워치독은 무성장 60초에서 종료(elapsed=360s = 300s 상한 + 60s). **codex 는 hang 이 아니라 우리 테스트 완료를 기다리는 중이었다.** 별도 사이클로 다룬다 — 아래 후속 참조
- 보안: 보안 수준 standard 기준 경로 traversal 중점 검토 → PASS (경로 검증은 로더에 남고 스킬은 이름→경로 조합 금지, 환경변수 명시 전달이 검증을 우회하지 않음)

## 후속 (미착수)

- **High (다음 사이클)** — **워치독 오판 클래스 확정**: 생존 신호가 "결합 출력 성장" 단일 축이라 "hang" 과 "긴 자식 명령 대기" 를 구분하지 못한다. codex 가 테스트를 돌리는 성실한 리뷰일수록 죽는 역-인센티브. 과거 사이클(v1.6.2/v1.6.3 등)에 "codex timeout" 으로 기록된 건들도 동일 원인일 가능성이 높아 재해석 필요. 방향 후보: 자식 프로세스 생존/CPU 관찰, codex 실행 중 exec 시작 이벤트를 성장 신호로 계수, 리뷰용 테스트 명령을 빠른 부분집합으로 유도
- **Medium** — `tests/skills/run-all.sh` 자체가 219초. 리뷰어가 습관적으로 호출하는 진입점이므로 소요 자체를 줄이거나 빠른 경로를 분리하는 것도 완화책
- **Low** — 다른 스킬에도 `${CLAUDE_PLUGIN_ROOT:-…}` 기대형 호출이 남아 있는지 전수 점검 (이번엔 persona 스킬만 수정)


---

<!-- from inbox/2026-07-27-review-watchdog-false-stall.md -->

# 2026-07-27 — 리뷰 워치독 오판 수리 (자식 명령 대기를 정지로 오인)

- DoD: `trail/dod/dod-2026-07-27-review-watchdog-false-stall.md`
- 설계/계획: `docs/specs/2026-07-22-review-time-cap.md` + `docs/plans/2026-07-22-review-time-cap.md` (기존 문서 개정 — Phase 5 증분)
- 변경: 래퍼 2사본 + 워치독 스위트 + fixture + 운영 문서 + 설계 2종
- 등급 판정: 사용자 노출 게이트 동작 변경 → **patch 후보**(버그 수정). 최종 등급은 릴리스 시 사용자 결정.

## 발단 — 직전 사이클에서 실증된 오판

페르소나 작업의 코드 리뷰가 6분에 강제 종료됐다. 사후 분석 결과 codex 는 멈춘 게 아니라 **우리 테스트가 끝나기를 기다리는 중**이었다.

## 근본 원인

생존 판정이 **결합 출력 성장 단일 축**이었다. codex 는 자식 명령 실행 **동안 출력을 내지 않고** 완료 시점에 일괄 방출하므로, 리뷰어가 테스트를 돌리면 그 소요만큼 성장이 정확히 0 이 된다.

실측: 실패한 리뷰의 마지막 동작이 `bash tests/skills/run-all.sh`(당시 219초, 현재 258초) 실행이었고 종료 시각은 elapsed=360초(=300 상한 + 60 무성장). 별도 SPIKE(`sleep 60` 지시)에서 출력은 604바이트에 45초 이상 정지, 자손 프로세스는 1→3 증가, 로그에는 시작 표식만 있고 완료 표식은 명령 종료 후 등장.

→ **리뷰어가 성실히 검증할수록 죽는 역-인센티브.** 과거 사이클의 "codex timeout" 기록도 상당수 동일 원인일 가능성 (재해석은 후속).

## 변경 요약

1. **생존 신호를 3축으로 확장** — 출력 성장(기존) + 미완료 자식 명령 표식 + 자손 프로세스 활동.
2. **edge / level 구분 + 유한 lease** — 창 사이 실제 변화(출력 성장·자손 PID 집합 변동)는 **edge** 로 무기한 유예(기존 계약 유지). 상태만 유지되는 **level**(미완료 표식·자손 존재)은 기본 20창 lease 안에서만 유예하고 소진 후 정지 계수 재개. *이 구분이 없으면 표식만 남긴 hang 이 영구 생존한다 — 리뷰 1회차 High 지적.*
3. **정지 임계 2창(60초) → 6창(180초)** — 자식도 출력도 없는 순수 추론 구간 보호(사용자 결정).
4. **기준선은 불변 상수 0** — 관측값으로 잡으면 상한 전에 시작된 장기 자식이 기준선에 흡수돼 축이 영구 침묵한다(설계 리뷰 High).
5. **시작 표식 판정을 명령행 구조 인식으로** — 절대경로 프로그램으로 시작 + 작업 디렉토리 절 포함. 산문 오인은 막고 공백 있는 경로는 허용.
6. **진단 문구**를 모든 종료 경로에서 참인 형태로 교체. 호출자 판별용 라인 앵커는 불변.
7. **override 3종 → 5종**(정지 임계·lease 추가), 경고 문구에 단위 라벨.

## 검증

- 워치독 스위트 **95/95 통과**(신규 W13~W20 포함), skills·scripts·hooks 전 스위트 통과, 미러 정합·플러그인 정합 통과.
- **역변이 검증 2종**: ①기준선을 관측값으로 되돌리면 W19·W20 만 정확히 실패 ②잔존 검사의 PID 파일 주입을 빼면 W4·W9b 만 정확히 실패 → 두 검사 모두 공검사가 아님을 실증.
- **실전 검증**: 수리 후 이 사이클의 리뷰 12회 전부 정상 완주, **정지 오판 0건**. 그중 다수가 15~19분 소요 — 예전 코드였다면 6분에 죽었을 리뷰들이다.
- 환경 의존: 자손 관측이 불가한 격리 환경에서는 해당 축 검증 4건이 명시적 skip(전제 불충족). 결정론 seam(W17)이 그 공백을 환경 비의존으로 메운다.

## 리뷰

- 설계 4라운드 → **PASS**. 계획 4라운드 → Medium-only 자체검토 종결. 코드 **6라운드** → 최종 최고 severity Medium 2건(잔존 검사 공검사, 설계서 resolver 계약 서술) 반영 후 자체검토 종결.
- 리뷰가 실제로 잡아낸 것: 오판 방지가 hang 검출을 없앤 High, 아직 쓰지 않은 코드가 기존 테스트를 깨뜨릴 것이라는 예측, 사실 확인 없이 단정한 오류("plan 문서가 없다" — 실재했다), vacuous oracle 2건(테스트가 production helper 대신 로직을 재작성 / PID 파일 부재 시 잔존 검사가 무조건 통과), 그리고 **내가 수리 중 새로 만든 회귀**(잔존 검사 RC 분기가 격리 환경 4건을 깨뜨림).
- 보안: standard 기준 신규 표면 3종(프로세스 열거·스풀 파싱·환경변수) 검토 → PASS.

## 후속

- **다음 사이클 (사용자 확정)**: 설계→계획→테스트 연쇄 드리프트 구조 재설계. 원인 3가지 — 계약 값이 12곳에 복제, 검사기가 이름 집합만 보고 의미는 못 봄, 계획서가 "이력"과 "지시" 두 역할을 겸함. 범위는 **구조 재설계**(대장/실행 분리 + 값 단일화 + 폐기 계약 자동 탐지).
- **그 다음 (사용자 확정)**: rein 산출물 경로 재배치. 실측상 설치 시 만드는 건 `.rein/` 와 `trail/` 뿐이고, `docs/specs|plans|brainstorms` 는 스킬이 필요 시 생성 — **사용자 자기 문서 폴더와 이름 충돌**이 진짜 문제. 전부 숨김 폴더로 넣기보다 "도구 상태 vs 프로젝트 설계 문서" 2계층 분리가 방향. 주요 버전급 + 기존 사용자 이사 필요.
- **Low**: 정지 fixture 의 SIGKILL 경로 고아 프로세스(최대 30초, 테스트 전용). lease 경계(마지막 창 유예/다음 창부터 계수)를 역변이로 고정하지 못함.
- **Medium**: `tests/skills/run-all.sh` 자체가 258초 — 리뷰어가 습관적으로 호출하는 진입점이라 빠른 경로 분리가 완화책.


---

<!-- from inbox/2026-07-27-release-v164.md -->

# 2026-07-27 — v1.6.4 릴리스 완료

- dev `a003dee` / main `18ba9e6` / tag `v1.6.4` — push + publish + mirror 전부 success
- public 검증: plugin.json 1.6.4 도착, 내부 기록(trail·설계문서·AGENTS·.rein) 전부 404, public 태그 clean `fc6ded0` (트리 최상위에 내부 기록 없음, public docs 는 사용자용 5종만)
- GitHub Release 게시 (Latest)
- 등급: **patch (사용자 결정)** — 기계 판정으로는 선택 목록에 새 항목이 생기고 인사말 고르기가 추가돼 minor 로도 읽히나, 실질은 "이미 구현된 흐름이 닿지 않던 것" + "명백한 버그" 라 patch 로 결정

## 번들 내용 (커밋 2건)

1. `beffab9` 페르소나 — 선택창 만들기 입구 / 선택지 상한 초과 시 2단 제시 / 세션 중 전환 즉시 본문 반영 / 로더 호출 경로 규약 / 인사말 후보 선택
2. `c58106b` 리뷰 워치독 — 생존 신호 3축 + edge·level 유한 lease + 정지 임계 6창 + 기준선 상수 0

## 이 릴리스에서 배운 것

- **리뷰 라운드가 13회까지 간 절반은 문서 연쇄 드리프트 탓**이었다. 설계 고치면 계획이, 계획 고치면 테스트가 어긋났다. 다음 사이클 주제로 확정 (`trail/inbox/2026-07-27-backlog-next-two-cycles.md`).
- **워치독 수리 자체가 리뷰 품질을 되살렸다**: 수리 후 이 사이클 리뷰 12회 전부 오판 없이 완주(다수 15~19분). 예전 코드였다면 6분에 죽었을 리뷰들이다.
- 하니스 10분 제한에 리뷰가 잘리는 건 별개 문제 — 백그라운드 실행으로 우회했다.

## 후속 (다음 사이클 순서 고정)

1. 설계→계획→테스트 연쇄 드리프트 **구조 재설계**
2. rein 산출물 경로 재배치 (`docs/` 이름 충돌, 주요 버전급)
3. Low: 정지 fixture SIGKILL 경로 고아 프로세스, lease 경계 역변이 미고정, `tests/skills/run-all.sh` 258초
---
<!-- source: 2026-07-27-backlog-next-two-cycles.md -->
## backlog-next-two-cycles

### 완료 기록

이번 세션에서 사용자가 확정한 후속 작업 2건. **순서 고정** — ①을 먼저 하고 그 위에서 ②를 한다. 이유는 ② 자체가 ①이 다루는 연쇄의 전형이라, 방어 장치 없이 ②를 하면 훨씬 큰 연쇄가 난다.

---

## ① 설계 → 계획 → 테스트 연쇄 드리프트 (범위: **구조 재설계**)

**문제**: 설계를 고치면 계획이 안 맞고, 계획을 고치면 테스트가 안 맞는 연쇄. 사용자 보고로 **다른 프로젝트에서도 재발**.

**이번 세션 실측 근거** — 워치독 수리 1건에서:

| 원인 | 관측 |
|---|---|
| 계약 값의 대량 복제 | "2창/60초" 하나가 **12곳**에 리터럴로 존재 (설계서 정책·범위표·의사코드·미결질문·테스트전략, 계획서 매트릭스·작업지시·진단문, 코드, 테스트 이름·단언, 운영문서) |
| 검사기가 의미를 못 봄 | 커버리지 검사기는 **항목 이름 집합 일치**만 검증. 계획서 본문이 옛 동작을 그대로 서술해도 통과 — 매 단계 통과했는데 문서는 서로 모순이었고, 잡은 건 사람(codex) |
| 계획서의 역할 혼재 | 계획서가 **일회성 실행 지시**와 **지속되는 계약 대장** 두 역할을 겸함. 완료 후 앞은 이력이 되고 뒤는 최신이어야 하는데 한 문서라 매 수정 때 "이력인가 지시인가" 가 문제 |

**후보 방향** (설계 사이클에서 검토):
1. **폐기 계약 자동 탐지** — 항목 교체 시 옛 이름 + 옛 계약의 특징 문구를 저장소 전수 검색해 이력 구간 밖 잔존을 차단. 가장 값싸고 효과 큼 (사람이 15분 걸려 잡던 것의 상당수를 초 단위로).
2. **계약 대장 / 실행 지시 분리** — 대장을 계획서 본문에서 떼어내 "옛 지시와 새 계약의 공존" 상황 자체를 제거.
3. **값 단일화** — 설계서 한 표에만 값을 두고 나머지는 이름으로 참조. 테스트는 기본값을 하드코딩하지 말고 코드에서 읽기.

**한계 인정**: 산문 간 의미 모순은 기계가 전부 못 잡는다. 목표는 전부 자동화가 아니라 **기계적인 부분을 걷어내 사람이 볼 것을 의미 판단만 남기는 것**.

**주의**: rein 은 이미 관문이 많다. "검사를 하나 더 늘리는 게 정말 이득인가"부터 따져야 하며, 잘못하면 마찰만 늘고 체감 가치가 없다. 구상 단계에서 이 질문을 먼저 통과시킬 것.

---

## ② rein 산출물 경로 재배치 (주요 버전급)

**사용자 문제 제기**: rein 이 만드는 문서를 `.rein/` 안으로 모으는 게 낫지 않나.

**실측 (2026-07-27)**: 설치 시 사용자 저장소에 만드는 것은 **`.rein/`(+policy) 와 `trail/` 뿐**이다. `docs/` 는 만들지 않으며, `docs/specs|plans|brainstorms` 는 스킬·에이전트가 필요 시 생성한다 (brainstorming / spec-writer / codex-review / persona 스킬이 참조).

**진단 — 전부 `.rein/` 로 옮기는 건 아니다. 두 종류를 갈라야 한다.**

- **`docs/specs|plans|brainstorms` — 여기가 진짜 문제.** 사용자 프로젝트엔 이미 자기 `docs/` 가 있을 가능성이 높아 **이름 충돌**이 난다. 다만 이 문서들은 rein 의 상태가 아니라 **사용자 프로젝트의 설계 문서**다. 도구 내부 폴더에 묻으면 소유 관계가 뒤집힌다 → `docs/rein/` 처럼 **보이되 네임스페이스만 분리**하거나 경로를 설정 가능하게.
- **`trail/`** — 옮길 수는 있으나 손해가 있다. rein 이 파는 가치가 "증거를 남긴다" 인데 숨김 폴더는 발견성이 떨어진다. 최상위 유지가 낫고, 거슬리면 옮기기보다 **설정으로 빼는** 편.

**비용**: 경로가 훅·스크립트·스킬·테스트·문서에 하드코딩. 기존 설치 사용자 저장소도 **이사** 필요(업데이트가 옮겨주거나 양쪽 읽기). 배포·미러·패키징 규칙에도 경로가 박혀 있음. **주요 버전급.**


