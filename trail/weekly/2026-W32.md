# Weekly Summary: 2026-W32

---
## 2026-08-04
# Daily Summary: 2026-08-04

---
<!-- source: 2026-08-04-review-cycle-progress.md -->
## review-cycle-progress

### 완료 기록

> **중단 지점**: 보안 재검토 실행 중 사용자 요청으로 중단. 코드는 전부 저장돼 있고
> 커밋은 하지 않았다. 이어가는 방법은 맨 아래 "다음 세션 재개 절차" 참조.

작업 기준서: `trail/dod/dod-2026-08-04-review-scope-and-round-budget.md`

## 입력

사용자 제출 회고 "rein 프로세스 과잉 회고 — 복원 사이클 실측 (2026-08-04)" O1~O13.
실측: 설계 리뷰 41회 + 계획 리뷰 26회를 돌고도 구현 코드 0줄.

사용자 결정: ① 처방은 A 2건만 좁게 ② 상한 초과 시 거부하되 호출자 선언으로 연장
가능 ③ 설계·계획 문서 없이 작업 기준서만 ④ 동시 실행 한계는 엄격화 대신 사실대로
서술 ⑤ 리뷰 4회차에서 2건 수정 후 마지막 회차로 종결.

## 회고보다 한 단계 더 정확했던 원인

회고는 리뷰어의 심도 표류와 산문 지시의 무력화를 원인으로 봤다. 코드 실측 결과
**둘 다 구현에 원인이 있었다**.

1. 리뷰 프롬프트 조립부에 모드 분기가 **없었다**. 문서 리뷰에도 코드 리뷰 슬롯이
   그대로 나갔다 — 계획서를 놓고 "자원 누수를 찾아라", "테스트 단언을 대조하라" 고
   지시한 셈이다. 리뷰어가 파일 핸들 수명주기를 소송한 건 지시대로 한 결과였고,
   대응하려면 구현 세부를 문서에 쌓아야 해서 리뷰 표면이 자가증식했다.
2. 래퍼에 회차 개념 자체가 **없었다**. 스킬 문서가 서술하던 회차 기록 필드는 존재한
   적이 없다. 상한이 산문으로만 존재해 두 번 무력화됐다.

## 완료된 변경

- **문서 리뷰 전용 슬롯 3종**(결정 건전성 / 범위·추적성 / 주장 감사) + **판정 규율
  3종**(심도-계층 / 결함 족 일괄 열거 / 근거 출처 분리)을 래퍼가 직접 방출. 코드
  결함·테스트 정합 슬롯은 문서 모드 미방출. 문서 리뷰 프롬프트 8.8KB → 4.4KB.
  코드 리뷰 프롬프트는 전문 골든 대조로 무회귀 고정(공백 1개 차이도 잡는 것을
  역변이로 확인).
- **회차 예산**: 사이클 키별 카운터(`trail/dod/.review-rounds/`), 기본 상한 5(상수).
  상한 판정은 실행 이전, 계수는 판정 확정 이후. 통과 시 카운터 제거, 미통과만 누적.
  준비도 거부·모델 오류·워치독 정지는 미계수. 연장은 첫 줄 단독 정수 선언만 인정하며
  경고 + 이력에 남는다. 카운터 불가(쓰기 실패·손상·잠금 잔존·경로가 링크)는 예산
  소진과 **다른 종료 신호**로 분리.
- 스킬 문서에 예산 계약 절 + 종료 신호 판별 계약 추가, 존재한 적 없는 기록 필드
  서술 정정. 설계/계획 작성 에이전트에 예산 소진·카운터 불가 분기 추가.
  계획 작성 에이전트의 정본 호출문을 두 줄 규약으로 통일(경로 뒤 지시문이 대상
  경로를 오염시켜 예산 키가 갈라지던 문제).

## 리뷰 이력

**코드 리뷰 5회차 PASS 로 종결** (통과 표식 생성됨). 회차별 지적과 대응:

| 회차 | 판정 | 지적 |
|---|---|---|
| 1 | 수정 필요 | 상한 환경변수 우회 / 동시 실행 회차 유실 / 손상·쓰기 실패가 예산을 풀어줌 / 공백 포함 마커 미제거 / 문서 정정 불완전 / 전문 회귀 테스트 부재 |
| 2 | 수정 필요 | "최대 1회 초과" 주장 부정확 / 인프라 오류가 예산 소진과 같은 신호 / 주석과 실제 동작 상충 |
| 3 | 수정 필요 | 경로 표기별 예산 분리 우회 / 마커가 본문 어디서나 인식(인용만으로 연장) / 새 종료 신호가 자동 호출자에 미반영 |
| 4 | 수정 필요 | 파일 링크·한 줄 호출 형식으로 키 우회 잔존 / 기록 실패가 조용히 통과(잠금 잔존 하나로 상한 무력화) |
| 5 | **통과** | — |

**보안 리뷰**: 1차에서 즉시 수정 권장 1건 + 가벼운 2건.

- (수정 완료) 카운터 임시파일명이 고정이라 그 자리에 링크를 심으면 저장소 밖 파일이
  덮어써졌다 — 재현됨. 무작위 임시명 + 경로 링크 검사로 차단, 외부 파일 무손상을
  테스트로 고정.
- (수정 완료) 대상 문자열의 와일드카드가 파일명으로 확장돼 리뷰 대상과 예산 키가
  조용히 바뀌었다 — glob 확장 차단.
- (문서 정정) 연장 이력은 통과 시 카운터와 함께 정리되므로 영구 감사 기록이 아니다.
  이 예산의 위협 모델(정직한 에이전트 규율, 적대적 우회 하드닝 범위 밖)을 명시.

**⚠️ 보안 재검토는 실행 도중 중단됐다** — 위 3건 반영분에 대한 재검토가 미완이다.

## 검증 상태 (중단 시점)

- 변경 셸 스크립트 5개 구문 검사 통과, 래퍼 두 사본 내용 동일.
- 회차 예산 테스트 61건 전량 통과 / 문서 모드 테스트 22건 전량 통과.
- 전체 스킬 테스트 스위트 전량 통과.
- **커밋하지 않았다.**

## 세션 중 실측된 rein 자체 결함 (백로그)

회고 O7 의 실증이며 그보다 범위가 넓었다.

1. **게이트가 현재 저장소 밖 경로까지 판정한다.** 이 저장소의 리뷰 표식 폴더에 다른
   저장소(`set-n-rest`, 구 경로 `claude-code-ai-native`) 문서를 가리키는 표식이 25건
   있었고, 그 중 하나가 저쪽 세션에서 편집되자 이 저장소 편집이 전부 잠겼다.
   사용자 승인 하에 백업 후 제거(백업은 이 세션 임시 폴더 — 영구 보관 아님).
2. 무관한 설계 문서의 대기 표식이 임시파일 작성까지 전역 차단(누적 28회).
3. 커밋 게이트가 샌드박스 재현 스크립트 안의 git 명령 **문자열**을 실제 커밋으로 오인.

2·3 은 "훅 결함(규칙 준수로 회피 불가)" 으로 분류해 종결. 셋 다 훅 소스 수정 대상.

## 기존 결함 관찰 (이번 범위 밖)

`tests/skills/test-codex-effort-deterministic.sh` 4건 실패는 이번 변경과 무관한 기존
상태다(변경 전 상태를 격리 트리로 재현해 확인). 자가검증 관문 도입 때 하네스가
따라가지 않은 것으로 보인다. 전체 스위트 목록에는 없어 통과에는 영향이 없다.

## 다음 세션 재개 절차

1. 보안 재검토를 다시 요청한다 — 대상은 위 보안 지적 3건의 반영분(무작위 임시명 +
   경로 링크 검사, 와일드카드 차단, 문서 정정). 특히 링크 공격이 실제로 막혔는지와
   무작위 임시파일 도입으로 새 표면(잔존·권한)이 생기지 않았는지.
2. 통과하면 이 진행 기록을 작업 기준서와 같은 이름의 완료 기록으로 정리한다
   (`trail/inbox/2026-08-04-review-scope-and-round-budget.md`). 그래야 게이트가 이
   작업을 종결로 인식한다 — 이번 세션에서 완료 기록을 먼저 써서 작업 기준서가
   비활성화돼 편집이 막힌 적이 있다.
3. 커밋 여부는 사용자 승인 사항. 배포(main 머지·태그·공개 반영)는 아직 결정되지
   않았고, 사용자 눈에 보이는 게이트 동작이 바뀌므로 버전 등급 결정이 필요하다.

---
<!-- source: 2026-08-04-review-scope-and-round-budget.md -->
## review-scope-and-round-budget

### DoD

- 날짜: 2026-08-04
- slug: review-scope-and-round-budget
- 유형: 버그 수정 + 계약 신설 (사용자 노출 리뷰 게이트 동작)
- 입력: 사용자 제출 회고 "rein 프로세스 과잉 회고 — 복원 사이클 실측 (2026-08-04)" O1~O13
- 사용자 결정: ① 처방 범위는 A 2건만 좁게 ② 회차 상한 초과 시 거부하되 호출자 선언으로 연장 가능 ③ 설계·계획 문서 없이 작업 기준서만

## 배경 — 회고 진단보다 한 단계 더 정확한 원인

회고는 리뷰어의 "심도 표류"(O6·O12)와 "회차 상한 산문 지시의 무력화"(O11·O13)를 원인으로 봤다. 코드 실측 결과 둘 다 **구현에 원인이 있었다**.

### 원인 1 — 문서 리뷰가 코드 리뷰 지시문을 그대로 받는다

`rein-codex-review.sh` 의 `build_envelope()` 에는 리뷰 모드 분기가 **없다**. 문서 리뷰(`spec-review`)에서도 코드 리뷰와 동일한 4개 슬롯이 방출된다:

| 슬롯 | 문서 리뷰에서의 문제 |
|---|---|
| `1. Code defects and regressions — (look-ahead, 타입, 경계, resource leak 등)` | 문서에 코드가 없는데 자원 누수를 찾으라고 지시 |
| `2. Design Alignment` | 문서 리뷰에선 covers/scope_items 가 N/A 로 가므로 대부분 공회전 |
| `3. Test Alignment` | 문서에 테스트 함수가 없음 |
| `4. Claim Audit` | 유효 (문서의 수치 주장 검증) |

→ O12 에서 리뷰어가 계획서를 놓고 파일 핸들 수명주기·닫기 실패·이중 닫기를 지적한 것은 **지시받은 그대로 수행한 결과**다. 리뷰어 판정 표류가 아니라 rein 의 지시 결함이다.

여기에 `SKILL.md` §6.7 이 문서 리뷰에서 요청서 사전검사·증거 계약을 통째로 건너뛰므로, **판정 축은 부재한데 코드 리뷰 지시만 남은** 상태가 된다. O1(구현 세부의 문서 침투)·O2(재진술)·O6(일반론 수입)·O12 가 여기서 파생된다.

### 원인 2 — 회차 개념이 코드에 존재하지 않는다

`SKILL.md` §3 은 "stamp 의 `review_round` 필드에 현재 회차를 기록한다"고 서술하지만, 실제 stamp 작성부(`write_code_review_stamp`)가 쓰는 12개 필드에 `review_round` 는 **없다**. 래퍼 2,470줄 전체에 회차 카운터·상한이 부재하다.

→ 회차 상한이 오직 산문 지시로만 존재했고, 실측에서 두 번 무력화됐다(13회차 무보고 연속 실행 / 종결 명령 후 5회차 폭주). **산문 지침을 한 줄 더 쓰는 처방은 이미 두 번 반증됐으므로 채택하지 않는다.**

## 범위

### 1. 문서 리뷰 판정 계약 (A-1)

`build_envelope()` 에 리뷰 모드 분기를 신설한다.

- **코드 리뷰 모드**: 현행 슬롯을 **바이트 동일**로 유지 (기존 하위호환 패턴 준수 — 회귀 0).
- **문서 리뷰 모드**: 슬롯 세트를 문서 계층에 맞게 교체한다.
  - 코드 결함 슬롯·테스트 정합 슬롯은 **방출하지 않는다**.
  - 결정 건전성 슬롯 — 채택 결정·기각 대안의 근거 유무, 문서 내 상충 결정.
  - 범위·추적성 슬롯 — Scope ID 가 측정 가능한 계약인가, 상위 문서(브레인스톰/설계)와 정합하는가, 결정해야 할 변수가 누락됐는가.
  - 주장 감사 슬롯 — 유지 (문서의 수치·완료 주장 검증은 문서 리뷰에서도 유효).
- **판정 규율 3종을 envelope 에 실제 삽입**한다 (규칙 문서에만 쓰지 않는다):
  - (i) **심도-계층**: 구현 수준 결함(오류 경로·자원 수명주기·버퍼링 방식·자료구조 선택)은 판정(verdict)에 반영하지 않고 "구현 단계 이관" 참고사항으로 분류한다. 문서 판정 축은 결정·범위·추적성이다.
  - (ii) **결함 족 일괄 열거**: 같은 성격의 지적은 적용 대상 전체를 이번 회차에 한 번에 열거한다. 입력·대상별로 쪼개 회차를 늘리는 것을 금지한다.
  - (iii) **근거 출처 분리**: 저장소에 존재하지 않는 일반 규칙(임의의 줄 수 상한 등)을 근거로 반려 판정하지 않는다. 저장소 계약 위반과 일반론 참고사항을 분리 표기한다.

기존 선례를 따른다 — 래퍼는 이미 `TIER2_DESIGN_ADVISORY_NOTE` 로 "이 항목 단독으로 판정을 승격하지 마라"는 지시문을 envelope 에 삽입하고 있다. 같은 방식·같은 자리다.

### 2. 리뷰 회차 예산 코드 강제 (A-2)

- **사이클 키**: 문서 리뷰는 리뷰 대상 문서 경로(`SPEC_REVIEW_SUBJECT`), 코드 리뷰는 활성 작업 기준서 slug(stamp 의 `cycle` 과 동일 산출). 기준서 부재 시 고정 sentinel.
- **카운터 저장**: `trail/dod/.review-rounds/<키 해시>` 파일. 키 원문·현재 회차·적용 상한·갱신 시각·연장 이력을 기록한다.
- **기본 상한**: **5회차** (양 모드 공통). 근거 — 기존 "3회차에도 High 잔존 시 사람 에스컬레이션" 규칙에 2회 여유를 둔 값이며, 실측(문서 41회·26회)과의 격차가 이번 처방의 절감분이다.
- **초과 시 동작**: codex 를 **실행하지 않고** 전용 종료코드로 거부한다. 진단은 라인 앵커 형식(`ERROR: [codex-review][round-budget-exceeded]`)으로 방출하고 사람 에스컬레이션을 안내한다. 종료코드는 기존 미사용 값을 쓴다(현행 사용: 2/3/4/5).
- **연장 계약** (사용자 결정): 호출자가 프롬프트 마커로 상한을 선언하면 그 값을 적용한다. 기존 `[EFFORT:]` 마커 파싱 패턴을 따른다.
  - 연장의 **가시성 강제**: 연장 선언은 카운터 파일에 누적 기록되고 리뷰 출력 상단에 표기된다. 에이전트가 조용히 상한을 늘리는 경로를 없앤다(사용자가 이 위험을 인지하고 연장 허용을 선택했으므로, 차단 대신 가시성으로 완화한다).
- **계수 시점**: codex 실행 직전 1회 증가. 사전검사에서 거부된 호출은 계수하지 않는다(요청서 형식 반려가 예산을 잠식하지 않게).
- **리셋**: 키가 바뀌면 자연히 0에서 시작. 코드 리뷰 통과 시 해당 키의 카운터를 제거한다.

### 3. 문서 정정

- `SKILL.md`: 문서 리뷰 판정 축 + 회차 예산 계약 + 종료코드 표를 반영하고, 존재하지 않는 `review_round` stamp 필드 서술을 정정한다.
- `spec-writer` / `plan-writer` 에이전트 정의: 예산 소진 종료코드를 받으면 재호출하지 말고 사람에게 넘기도록 분기를 명시한다.

### 범위 외

- 회고 O5(요청서 사전검사 일괄 보고)·O7(대기 표식 전역 차단 범위 축소) — B 묶음. 사용자 결정으로 이번 사이클 제외.
- O8·O9·O10 및 문서 템플릿·에이전트 작성 규율(O1~O3 의 산문 처방) — C 묶음. 이번 제외.
- 코드 리뷰 모드의 슬롯·사전검사·증거 계약 변경 — 불변. 이번 변경은 문서 리뷰 경로에만 작용한다.
- 연장 선언의 절대 천장 도입 — 사용자가 연장 가능을 선택했으므로 하드 천장은 두지 않고 가시성으로 대체한다.
- 문서 리뷰의 같은 사이클 이어하기(O9) — 규칙은 이미 허용이고 관행만 굳은 사안이라 C 묶음으로 미룬다.

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/scripts/rein-codex-review.sh` | `build_envelope()` 모드 분기 + 문서용 슬롯·판정 규율 3종 삽입, 회차 카운터·상한·연장 마커·전용 종료코드 |
| `scripts/rein-codex-review.sh` | 루트 미러 — 동일 반영 + 정합 확인 |
| `tests/skills/test-review-doc-mode-slots.sh` | 신규 — 모드별 슬롯 방출·코드 리뷰 바이트 동일 회귀 |
| `tests/skills/test-review-round-budget.sh` | 신규 — 계수·거부·연장·미계수 경로 |
| `plugins/rein-core/skills/codex-review/SKILL.md` | 판정 축·예산 계약·종료코드 반영, `review_round` 오기재 정정 |
| `plugins/rein-core/agents/spec-writer.md` / `plan-writer.md` | 예산 소진 시 재호출 금지 + 사람 핸드오프 분기 |

## 검증 기준

- [ ] **코드 리뷰 모드 envelope 이 변경 전과 바이트 동일**하다 (문서 리뷰 분기가 코드 경로를 오염시키지 않음).
- [ ] 문서 리뷰 모드에서 코드 결함 슬롯·테스트 정합 슬롯이 방출되지 않고, 판정 규율 3종이 방출된다.
- [ ] 문서 리뷰 모드에서 결정 건전성·범위 추적성 슬롯이 방출된다.
- [ ] 상한 도달 후 호출이 **codex 실행 없이** 거부되고, 전용 종료코드 + 앵커 진단이 나온다.
- [ ] 연장 마커 선언 시 통과하며, 연장 사실이 카운터 파일과 출력 상단에 남는다.
- [ ] 사전검사에서 거부된 호출은 회차를 소모하지 않는다.
- [ ] 서로 다른 사이클 키는 독립적으로 계수된다.
- [ ] 테스트는 **행위 기반**으로 작성한다 — 함수 source 후 내부 변수만 보는 방식은 금지(기존 false-green 사례). 실제 실행 + 출력/종료코드 단언.
- [ ] `bash -n` 통과 + 리뷰 래퍼 관련 기존 테스트 전량 무회귀 + 루트 미러 정합.
- [ ] 코드 리뷰 + 보안 리뷰 통과.

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 구현
- skills: `rein:codex-review`, `rein:security-reviewer`
- mcps: 없음
- rationale: 두 변경이 같은 스크립트의 인접 경로(프롬프트 조립부 / 실행 직전 관문)를 건드려 병렬 분할 이득이 없다. 게이트 동작을 바꾸는 위험 경로이므로 변경 지점을 좁게 유지하고, 코드 리뷰 모드 무회귀를 테스트로 먼저 고정한 뒤 진행한다

approved_by_user: true

### 완료 기록

작업 기준서: `trail/dod/dod-2026-08-04-review-scope-and-round-budget.md`
(2026-08-04 구현+코드리뷰 5R, 2026-08-05 보안 재검토 → 커밋 게이트가 리뷰 후
수정분 재리뷰를 요구 → 추가 결함 발견·수리 5R → 델타 보안 재검토로 종결)

## 입력

사용자 제출 회고 "rein 프로세스 과잉 회고 — 복원 사이클 실측 (2026-08-04)" O1~O13.
실측: 설계 리뷰 41회 + 계획 리뷰 26회를 돌고도 구현 코드 0줄.

사용자 결정: ① 처방은 A 2건만 좁게 ② 상한 초과 시 거부하되 호출자 선언으로 연장
가능 ③ 설계·계획 문서 없이 작업 기준서만 ④ 동시 실행 한계는 엄격화 대신 사실대로
서술 ⑤ (08-05) 커밋 승인 + 다음 작업은 게이트 판정 범위 결함 3건 선행.

## 회고보다 한 단계 더 정확했던 원인

회고는 리뷰어의 심도 표류와 산문 지시의 무력화를 원인으로 봤다. 코드 실측 결과
**둘 다 구현에 원인이 있었다**.

1. 리뷰 프롬프트 조립부에 모드 분기가 **없었다**. 문서 리뷰에도 코드 리뷰 슬롯이
   그대로 나가, 리뷰어가 계획서를 놓고 자원 수명주기를 소송했고 리뷰 표면이
   자가증식했다.
2. 래퍼에 회차 개념 자체가 **없었다**. 스킬 문서가 서술하던 회차 기록 필드는 존재한
   적이 없다. 상한이 산문으로만 존재해 두 번 무력화됐다.

## 완료된 변경

- **문서 리뷰 전용 슬롯 3종**(결정 건전성 / 범위·추적성 / 주장 감사) + **판정 규율
  3종**(심도-계층 / 결함 족 일괄 열거 / 근거 출처 분리)을 래퍼가 직접 방출. 코드
  결함·테스트 정합 슬롯은 문서 모드 미방출. 문서 리뷰 프롬프트 8.8KB → 4.4KB.
  코드 리뷰 프롬프트는 전문 골든 대조로 무회귀 고정.
- **회차 예산**: 사이클 키별 카운터(`trail/dod/.review-rounds/`), 기본 상한 5(상수).
  상한 판정은 실행 이전, 계수는 판정 확정 이후. 통과 시 카운터 제거, 미통과만 누적.
  준비도 거부·모델 오류·워치독 정지는 미계수. 연장은 첫 줄 단독 정수 선언만 인정
  (경고 + 이력 기록). 카운터 불가(쓰기 실패·손상·잠금 잔존·경로 링크)는 예산 소진과
  다른 종료 신호로 분리.
- **카운터 경로 하드닝** (08-04 보안 1차 + 08-05 코드 리뷰 2 High 의 합):
  - 판정 초입(디렉토리 생성·카운터 읽기 이전)에 카운터 디렉토리/파일 링크 검사.
  - 미통과 기록은 락 안에서 링크 재검사 + mktemp 무작위 임시명 + 원자 교체.
  - **통과 정리도 같은 키별 락을 획득**한 뒤 링크 재검사 → 삭제. 락 미획득·링크
    전환·삭제 실패·자기 락 해제 실패 전부 진단 후 중단 — 통과 표식 작성 **이전에**
    인프라 오류로 종료 (파괴·실패가 성공으로 위장되는 경로 제거).
  - 대상 문자열 glob 확장 차단(`set -f` 유한 구간).
- 스킬 문서에 예산 계약 절 + 종료 신호 판별 계약 + 위협 모델(3지점 검사) 반영,
  존재한 적 없는 기록 필드 서술 정정. 설계/계획 에이전트에 예산 소진·카운터 불가
  분기. 계획 에이전트 정본 호출문 두 줄 규약 통일.
- **테스트**: 회차 예산 79건(+문서 모드 22건). 링크 방어는 판정 초입(RB18·RB20)·
  리뷰 도중 전환(RB21)·활성 락 보존(RB22) 각각을 실행 오라클로 고정. fake-codex 에
  "verdict 방출 직전 명령 1회" 테스트 전용 훅을 추가해 도중-전환을 sleep 경쟁 없이
  결정론 재현. 전체 스킬 스위트 무회귀.

## 리뷰 이력

**1차 코드 리뷰 (08-04): 5회차 PASS.** 회차별 지적은 이전 기록 참조
(`trail/daily/2026-08-04.md`) — 상한 우회·동시성·키 정규화·마커 오인·기록 실패
fail-open 계열.

**보안 리뷰 1차 (08-04)**: 고정 임시파일명 링크 공격(재현됨)·glob 확장·문서 정정
3건 → 반영.

**2차 코드 리뷰 (08-05, 커밋 게이트가 "리뷰 후 수정분" 재리뷰 요구 → 5회차 PASS)**:

| 회차 | 판정 | 핵심 지적 |
|---|---|---|
| 1 | 수정 필요 | **High: 통과 경로가 링크 무검사** — 카운터 디렉토리가 외부 링크면 통과 정리(rm)가 저장소 밖 해시 파일 삭제 후 정상 통과 (격리 재현: 외부 파일 삭제됨). RB18 은 손상 검사가 먼저 발화하는 허탕 오라클 |
| 2 | 수정 필요 | **High: 통과 정리가 자신이 획득하지 않은 활성 락을 삭제** (상호배제 파괴 가능) / 일반 정리 실패가 조용히 통과 / 새 테스트가 도중-전환 경로 미실행 / 요청서 개수 표기 불일치 |
| 3 | 수정 필요 | 락 소유권 수리의 직접 회귀 오라클 부재 (복귀해도 기존 테스트 전량 통과) |
| 4 | 수정 필요 | 코드·테스트 결함 0 (역변이 검증 포함 오라클 유효 확인). 요청서의 과거 회차 수치 인용 무근거 1건만 |
| 5 | **통과** | 정량 주장 전량 정합, 종결 |

**보안 재검토 (08-05, 2회)**: ①1차 지적 3건 반영분 PASS (링크 공격 격리 실측 —
단, 이 판정은 미통과 기록 경로에만 유효했음이 2차 코드 리뷰에서 판명) ②추가 수리
델타 PASS — 3지점 검사 체계·락 대기 유한성·픽스처 훅 이중 관문 확인, 잔존 TOCTOU
는 위협 모델(정직한 에이전트 규율) 범위 밖으로 수용. 비차단 참고 2건: 조상 디렉토리
링크는 검사 밖(기존 공유 표면), 강제 종료 시 임시파일 잔존 가능(위생).

## 검증 상태

- 회차 예산 테스트 79건 + 문서 모드 22건 + 전체 스킬 스위트 전량 통과.
- 래퍼 두 사본 내용 동일, 변경 셸 스크립트 구문 검사 통과.
- 작업 기준서 검증 기준 10항 전부 충족.
- dev 커밋: 사용자 승인 (08-05). 배포(main 머지·태그·공개 반영)는 미결정 —
  사용자 눈에 보이는 게이트 동작이 바뀌므로 버전 등급 결정 필요.

## 프로세스 관찰 (이번 사이클의 교훈)

- **"완료 기록을 리뷰 종결 전에 쓰지 마라"가 또 실증됐다**: 08-05 세션이 보안
  재검토 통과 직후 완료 기록을 썼다가, 커밋 게이트의 재리뷰 요구 → 결함 발견으로
  기록을 되돌렸다. 완료 기록은 모든 게이트 + 커밋 직전까지 끝난 뒤가 맞다.
- 보안 리뷰 PASS 가 코드 리뷰를 대체하지 못한다는 실측: 보안 재검토가 "디렉토리
  링크 거부"를 실측 PASS 로 판정했지만 그 실측은 미통과 기록 경로였고, 통과 경로의
  같은 클래스 결함은 코드 리뷰가 잡았다. 경로별 실측 범위를 판정문에 명시해야 한다.
- 리뷰 5회차 중 2개 회차의 유일 지적이 **요청서 수치 표기**였다 (개수 불일치·무근거
  인용). 주장 감사는 요청서 본문에도 적용된다 — 수치는 세거나, 빼거나, 증거를 달라.

## 세션 중 실측된 rein 자체 결함 (백로그 — 다음 사이클 확정)

1. **게이트가 현재 저장소 밖 경로까지 판정** — 다른 저장소(`set-n-rest`) 문서 표식
   25건이 이쪽 편집을 전부 잠갔다. 사용자 승인 하에 백업 후 제거(세션 임시 폴더).
2. 무관한 설계 문서의 대기 표식이 임시파일 작성까지 전역 차단(누적 28회).
3. 커밋 게이트가 샌드박스 재현 스크립트 안의 git 명령 **문자열**을 실제 커밋으로 오인.

셋 다 훅 소스 수정 대상. 2026-08-05 사용자 결정: **이 3건을 다음 작업으로 선행**
(연쇄 드리프트 구조 재설계·산출물 경로 재배치보다 먼저).

## 기존 결함 관찰 (이번 범위 밖)

`tests/skills/test-codex-effort-deterministic.sh` 4건 실패는 이번 변경과 무관한 기존
상태 (변경 전 상태를 격리 트리로 재현해 확인). 전체 스위트 목록에 없어 통과에는
영향 없음.


---
## 2026-08-05
# Daily Summary: 2026-08-05

---
<!-- source: 2026-08-05-gate-scope-defects.md -->
## gate-scope-defects

### DoD

- 날짜: 2026-08-05
- slug: gate-scope-defects
- 유형: 버그 수정 (훅 게이트 오차단 — 사용자 노출 동작)
- 입력: 2026-08-04 사이클 실측 3건 (`trail/inbox/2026-08-04-review-scope-and-round-budget.md` §백로그) + 2026-08-05 읽기 전용 정찰
- 사용자 결정 (2026-08-05): ① 결함 3건을 연쇄 드리프트 재설계보다 선행 ② 미리뷰 문서 차단은 "현재 작업 관련만 차단, 무관 문서는 비차단 경고" ③ 샌드박스 git 은 "이동 대상이 리터럴로 저장소 밖일 때만 면제, 변수·불명은 차단 유지"

## 배경 — 공통 뿌리

세 결함 모두 **어휘적 판정(문자열/표식이 존재하는가)을 의미적 판정(현재 저장소·현재 작업에 실제 영향을 주는가)으로 취급**한다. 필요한 올바른 패턴은 이미 저장소에 존재한다 — 경로 소속 검사 lib(`path-containment.sh`)는 다른 3개 훅이 쓰지만 이 게이트만 안 쓰고, 절 형태 검증은 커밋 게이트 내부에 이미 있다. 발명이 아니라 **연결 누락**의 수리다.

## 범위

### 1. 결함 1 — 게이트가 저장소 밖 경로 표식까지 판정

`pre-edit-dod-gate.sh` 의 리뷰 표식 순회 2곳(pending 루프 537~583, orphan 루프 627~768)이 표식의 `path=` 를 "파일 존재" 만으로 신뢰한다. 타 저장소 문서를 가리키는 표식이 유입되면(실측: 25건) 이 저장소 편집 전체가 잠긴다.

- `lib/path-containment.sh` 를 source 하고, 두 루프에서 `path=` 가 `PROJECT_DIR` 밖이면 **skip + 1회성 비차단 경고**(표식 정리 안내).
- orphan 루프의 접두사 제거 실패 경로(725행 — 절대경로가 그대로 `git ls-files` 로 가서 무조건 fail-closed)도 같은 검사로 도달 불가하게 만든다.
- 표식 자동 삭제는 하지 않는다 (판정 제외만 — 파일 파괴는 이 수리 범위 밖).

### 2. 결함 2 — 무관 문서의 대기 표식이 전역 차단

같은 파일의 소스 판정(244~350)과 표식 판정(537~807)이 편집 대상 `FILE_PATH` 를 전혀 연결하지 않는다 — 미리뷰 문서 1건이면 소스 전체 잠금 (실측 28회, 소스 주석 772~777이 이미 자인).

- **관련성 기준 (사용자 확정)**: 활성 작업 기준서(`select-active-dod` 경유)가 참조하는 문서의 pending 만 **차단**. 관련성 판정은 결정론적 — pending 표식의 `path=` (절대/상대 두 표기)가 활성 DoD 본문에 등장하거나, DoD 의 `plan ref:` 문서 본문에 등장하면 관련.
- 무관 문서의 pending 은 **비차단 경고**로 강등 (문서 경로·리뷰 명령 안내 포함 — 가시성 유지).
- 활성 DoD 부재 시: 소스 편집은 어차피 상류(활성 기준서 요구)에서 차단되므로 이 게이트의 전역 차단 분기는 도달 불가 — 방어적으로 "pending 전건 경고" 로 통일.
- 기존 `tests/` 하드코딩 면제(783~796)는 새 관련성 검사로 일반화되면 제거 검토 (동작 동등성 테스트로 확인).

### 3. 결함 3 — 커밋 게이트가 문자열 git 을 실제 커밋으로 오인

커밋 탐지 정규식이 `grep -qE` **행 단위** 평가라, (a) heredoc 으로 파일에 **기록될 텍스트**의 줄머리 `git commit` 이 실행으로 오인되고 (b) `cd <샌드박스> && git commit` 의 대상 저장소를 판별할 수단이 없다.

- (a) **무조건 수리**: 커밋 탐지 매처 쌍둥이(`git-subcommand-model.sh` 의 `git_clause_invokes`, `bash-guard-infra.sh` 의 `command_invokes`)가 heredoc 본문을 매칭 대상에서 제외 — `<<[-]?['\"]?WORD` 시작~종결자 줄 범위를 매칭 전에 소거(소거는 매칭 입력에만, 원문 불변). 인용/비인용 종결자, 다중 heredoc, 종결자 미출현(불완전 명령) 케이스를 fail-closed 로 (소거 불확실하면 소거하지 않고 기존 판정 유지).
- (b) **리터럴 경로만 면제 (사용자 확정)**: 같은 명령 내 선행 `cd` 절의 인자가 **리터럴 절대경로**(변수·치환·상대경로 불포함)이고 정규화 결과가 `PROJECT_DIR` 밖이면, 이후 절의 git 커밋을 게이트 면제. 변수(`cd "$SB"`)·명령 치환·상대경로·불명은 **기존대로 차단**. `cd` 이후 다시 저장소 안으로 `cd` 하는 절이 나오면 면제 해제.
- 탐지 SSOT 원칙 유지 — 로직은 lib 에 두고 소비자 3곳(classifier/dispatcher/커밋 게이트)이 공유. 두 매처의 판정 동등성을 테스트로 고정.

### 범위 외

- 표식 파일 자동 정리·마이그레이션 (판정 제외만 수행).
- 변수 해석·실행 추적 기반 대상 저장소 판별 (어휘 모델 유지 — 리터럴만 면제).
- pending 표식 freshness/content_sha 재설계 (기존 백로그 별건).
- 적대적 우회 하드닝 (위협 모델: 정직한 에이전트 규율 — 기존 확정 유지).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/hooks/pre-edit-dod-gate.sh` | 결함 1·2 — 경로 소속 검사 연결 + 관련성 기반 차단/경고 분리 |
| `plugins/rein-core/hooks/lib/git-subcommand-model.sh` | 결함 3 — heredoc 본문 소거 + 리터럴 cd 면제 판정 (SSOT) |
| `plugins/rein-core/hooks/lib/bash-guard-infra.sh` | 결함 3 — 쌍둥이 매처가 SSOT 소거 헬퍼 공유 |
| `plugins/rein-core/hooks/pre-bash-test-commit-gate.sh` | 결함 3 — 커밋 감지 1회 계산 + 리터럴 밖 cd 면제 배선 (표식/커버리지/메시지 3게이트 공유) |
| `tests/hooks/test-pre-edit-dod-gate-repo-scope.sh` | 신규 — 결함 1·2 행위 기반 (cross-repo 표식·무관 문서 경고 강등·관련 문서 차단 유지) |
| `tests/hooks/test-git-mention-vs-invoke.sh` | 신규 — 결함 3 행위 기반 (heredoc 텍스트 통과·미종결 fail-closed·리터럴 cd 면제·변수 cd 차단·서브셸 닫힘 해제) |
| `tests/hooks/test-{spec-review-gate,pre-edit-dod-gate-sr-1-b,pre-edit-dod-gate-spec-tests-exempt,teach-forward-gates}.sh` | 갱신 — 활성 DoD 가 문서를 참조하도록 seeding 교체 (원래 검증 의도를 새 관련성 계약 위에서 유지) |
| `tests/hooks/test-pre-bash-test-commit-gate.sh` | 갱신 — `cd /other && git commit` 케이스를 GSD-3 재분류(면제)로 교체 + 변수 cd fail-closed 케이스 신설 (SX 구멍 보존) |

## 검증 기준

- [ ] 타 저장소 경로 표식이 있어도 이 저장소 편집이 차단되지 않고, 비차단 경고가 나온다.
- [ ] 활성 작업이 참조하는 문서의 pending 은 **여전히 차단**한다 (설계→코딩 순서 강제 보존 — FN 금지).
- [ ] 무관 문서의 pending 은 차단 없이 경고만 나온다 (임시 파일·scripts/ 편집 통과).
- [ ] heredoc 본문의 `git commit` 줄이 커밋으로 판정되지 않는다 (단일/다중/인용 종결자, 미종결 fail-closed).
- [ ] 리터럴 저장소 밖 `cd && git commit` 은 면제, 변수 `cd "$VAR"` 는 기존대로 차단된다.
- [ ] 실제 커밋(`git commit`, 저장소 안)은 종전과 동일하게 판정된다 — 기존 탐지 테스트 전량 무회귀.
- [ ] 두 매처(모델 lib / 가드 infra)의 판정이 동일하다 (동등성 테스트).
- [ ] 테스트는 행위 기반 — 실제 훅 실행 + 종료코드/JSON deny 단언 (source 후 내부 변수 검사 금지).
- [ ] `bash -n` 통과 + 훅 관련 기존 테스트 전량 무회귀.
- [ ] 코드 리뷰 + 보안 리뷰 통과.

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 구현 (게이트 동작 변경 위험 경로, 변경 지점 좁게 유지)
- skills: `rein:codex-review`, `rein:security-reviewer`
- mcps: 없음
- rationale: 결함 1·2 는 같은 파일의 인접 경로라 순차, 결함 3 은 파일이 분리되나 매처 쌍둥이 정합을 한 손에서 유지하는 것이 안전. 직전 사이클과 동일하게 행위 기반 테스트로 새 계약을 먼저 고정한 뒤 진행

approved_by_user: true

### 완료 기록

작업 기준서: `trail/dod/dod-2026-08-05-gate-scope-defects.md`
입력: 2026-08-04 사이클 실측 백로그 3건. 사용자 결정: ①이 3건을 연쇄 드리프트
재설계보다 선행 ②미리뷰 문서는 "현재 작업 관련만 차단, 무관은 경고" ③샌드박스
git 은 "이동 대상이 리터럴로 확실할 때만 면제".

## 공통 뿌리와 수리

세 결함 모두 어휘적 판정(문자열/표식 존재)을 의미적 판정(현재 저장소·현재 작업에
실제 영향)으로 취급했다. 필요한 패턴(경로 소속 검사·절 형태 검증)은 저장소에 이미
있었고, 수리는 발명이 아니라 연결이었다.

1. **저장소 경계** (`pre-edit-dod-gate.sh`): 리뷰 표식의 대상 경로를 전부 물리
   정규화(realpath)로 판정 — 저장소 밖(타 저장소 문서·저장소 안 symlink 가 밖을
   가리키는 경우 포함)이면 판정 제외 + 비차단 경고. 표식 파일은 삭제하지 않음.
2. **작업 관련성** (같은 파일): 미리뷰/stale 문서는 **선택된 활성 작업 기준서**
   (선택기 결과 우선, 특정 불가 시 전 활성 폴백)가 참조(본문 상대경로/파일명,
   또는 plan ref 문서 — 참조값은 첫 토큰만)할 때만 차단. 무관 문서는 경고 강등.
   말뭉치 1회 수집 캐시로 표식당 비용 grep 2회. tests/ TDD 면제 유지.
3. **문자열 vs 실행** (`git-subcommand-model.sh` + 쌍둥이 매처 + 커밋 게이트):
   - heredoc **본문** 소거(매칭 입력에만) — 인용(줄 넘김 지속)/워드 시작 주석/
     산술 `((...))` 인지 문자 스캐너, 인용 종결자 검증·소비, 미종결 fail-closed.
   - 샌드박스 커밋 면제 — 좁은 형태 계약 전부 충족 시만: 한 줄, `&&` 만(그 외
     구분자·backtick·`$`·`<` 전무, 인용 불문), 첫 절 리터럴 절대경로 cd, 후속
     이동(cd/pushd/popd) 금지, `-C`/`GIT_DIR` 계열 토큰 전무, python realpath
     물리 검증 + **연결형 워크트리 판별**(.git 파일의 gitdir 이 이 저장소를
     가리키면 면제 불가) 최종 관문. 실패는 전부 면제 없음(fail-closed).
   - 3게이트(coverage/리뷰 표식/커밋 메시지 포맷) 공유 면제 + NOTICE 방출.
   - 기존 보안 케이스 `cd /other && git commit`(구: 무조건 차단)은 사용자 확정
     계약으로 재분류 — 변수 cd 등 증명 불가는 fail-closed 유지.

## 리뷰 이력

| 회차 | 리뷰어 | 판정 | 핵심 |
|---|---|---|---|
| 1 | codex | 수정 필요 | High 3 (cd 추적이 실행 흐름·인용 무시 — 재현 5조합 / 인용·주석 속 가짜 heredoc 이 실제 커밋 소거 / plan ref 절 표기 파싱 실패) + Medium 2 (핫패스 비용·검증 공백) |
| 2 | codex | 수정 필요 | High 4 (저장소 symlink 별칭 면제 / 산술 시프트 오인 + 인용 종결자 미소비 상태 오염 / 말뭉치가 선택 기준서 아닌 전 미완료 합산 / 표식 텍스트 fast-path 의 내부 symlink 미판정) |
| 3 | sonnet 대체 (codex 사용량 한도 8/10 까지 — 실행 실패라 규정 대체 경로) | **통과** | 역변이 검증: 물리 검증 무력화 사본에서 별칭 테스트가 실제 실패함을 확인 |
| 4 (델타) | sonnet 대체 | **통과** | 보안 지적 수리분 — 역변이로 수리 전 형태가 결함 재현됨을 확인 |

**보안 리뷰**: 1차 수정 필요 — **연결형 워크트리가 샌드박스로 면제**(경로는 밖,
커밋은 이 저장소 브랜치 — 격리 실측 재현, 신규 FN). gitdir 판별로 수리 후 델타
재검토 **통과** (동일 프로브 재실행으로 결함 소멸·기존 경계 불변 확인). 비차단
참고 3건: 소거기의 작위적 오인 창 / 경고 강등은 확정 계약 / gitdir 부재 비정형
파일 경로는 git 이 커밋 자체를 거부해 구조적 무해.

## 검증 상태

- 신규 스위트: 저장소 경계·관련성 13건 + 문자열 vs 실행 25건 (역변이 검증 2종,
  실물 워크트리·symlink·산술·인용 오염 재현 포함) 전량 통과.
- 갱신 스위트 5개 포함 전체 훅 스위트 106개 중 105개 통과 — 유일 실패
  `test-background-jobs-registered.sh` 는 HEAD 베이스라인에서도 실패하는 기존
  결함(등록 검사, 본 변경 무관, 백로그). 전체 스킬 스위트 통과.
- dev 커밋 `2ad12a5`. 배포(main 머지·태그·공개 반영)는 미결정 — 게이트 동작
  변경(차단 축소 3곳 + 면제 신설)이므로 버전 등급 결정 필요.

## 남은 관찰 (백로그)

- `test-background-jobs-registered.sh` 기존 실패 (hooks 등록 검사 — 본 사이클 밖).
- `test-post-edit-meta-check-perf.sh` 경계선 성능(200ms 상한 vs 실측 ~201ms,
  부하 민감 — 간헐 실패).
- 코드 리뷰 3회차 비차단: 표식 경로 판정 python 블록의 SSOT 중복(4곳 유사)·
  경고 문자열 절단의 다중바이트 경계.

---
<!-- source: 2026-08-05-release-v165.md -->
## release-v165

### DoD

- 날짜: 2026-08-05
- slug: release-v165
- 유형: 릴리스 (버전 표면 + 변경 로그 + main 선별 머지 + 태그 + 공개 반영)
- 입력: dev 커밋 4건 (`76e4c67` 리뷰 회차 예산 / `2ad12a5` 게이트 판정 범위 3건 + trail 2건)
- 사용자 결정: 배포 진행 승인 + 등급 **patch (v1.6.5)** (기계 판정 minor 를 사용자 결정으로 하향 — v1.6.3/1.6.4 관례와 동일)

## 범위

- 버전 표면 2곳 동기화: `plugins/rein-core/.claude-plugin/plugin.json` + `scripts/rein.sh` `VERSION=` → 1.6.5
- `CHANGELOG.md` user-facing 엔트리 (Rule C — 내부 디테일 제외)
- `README.md` / `README.ko.md` 최신 릴리스 1건 요약 갱신 (KR/EN 동등)
- dev 릴리스 커밋 → push → main 선별 체크아웃 머지 (branch-strategy 포함 목록만) → `v1.6.5` 태그 → push → publish/mirror 자동 워크플로 확인 → 공개 저장소 검증
- 완료 후 릴리스 기록 + index 갱신

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/.claude-plugin/plugin.json` | version 1.6.5 |
| `scripts/rein.sh` | VERSION=1.6.5 |
| `CHANGELOG.md` | v1.6.5 엔트리 |
| `README.md` / `README.ko.md` | 최신 릴리스 요약 교체 |

## 검증 기준

- [ ] 버전 표면 2곳 값 일치 (publish 게이트 통과)
- [ ] CHANGELOG 는 user-facing 항목만
- [ ] KR/EN README 동등 (미번역 잔존 0)
- [ ] main 머지가 선별 체크아웃 절차 준수 (full merge 금지, 단방향)
- [ ] 태그·공개 저장소 반영 확인 (public plugin.json 버전, 내부 기록 제거 상태)

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 수행 (릴리스 절차)
- skills: 없음 (리뷰 게이트는 버전 표면 편집 시 자동 발동분 처리)
- mcps: 없음
- rationale: 정형화된 릴리스 절차 — branch-strategy.md / versioning.md 체크리스트 순서 실행

approved_by_user: true

### 완료 기록

작업 기준서: `trail/dod/dod-2026-08-05-release-v165.md`

## 결과

- **v1.6.5 배포 완료** — dev `d923bde`(bump)~`6121cc9` / main `0f191e1` / tag `v1.6.5` /
  public clean `cd76183` (public main HEAD 와 태그 대상 일치 — strip 검증) /
  publish·mirror 워크플로 success / public plugin.json 1.6.5 도착 /
  내부 기록(trail·AGENTS.md) 404 strip 확인 / GitHub Release Latest 등록.
- 번들: 리뷰 회차 예산(상한 5·연장 가시화·문서 리뷰 전용 판정·카운터 경로 하드닝)
  + 게이트 판정 범위 결함 3건(표식 저장소 경계 물리 판정·무관 문서 경고 강등·
  heredoc/샌드박스 커밋 구분, 연결형 워크트리 판별 포함).
- 등급: 기계 판정 minor → **사용자 결정 patch** (CHANGELOG 에 결정 주체·기계 판정
  병기 — v1.6.2~1.6.4 관례 유지).

## 릴리스 중 관찰

- **codex 사용량 한도** (8/10 9:58AM 복구)로 릴리스 델타 리뷰부터 sonnet 대체
  경로 사용 — 대체 사유는 리뷰 표식에 기록됨. 다음 리뷰 사이클도 한도 복구
  전이면 동일 경로.
- main 체크아웃 직후, 설치된 구버전(1.6.4) 훅 + main 의 옛 표식 상태가 **이번
  릴리스로 고친 결함 그대로**(타 저장소 표식의 전역 차단 — 저장소 밖 임시 폴더
  쓰기까지 차단) 발화 — 수리 필요성의 실증. 설치 플러그인은 marketplace 갱신
  주기에 따라 1.6.5 로 올라가면 해소.
- README 이전-릴리스 줄 롤링 누락을 대체 리뷰어가 잡아 릴리스 전 반영 (최신/이전
  2줄 롤링 패턴 유지).
- Bash 루프+파이프 조합 일부에서 PATH 유실로 git 미발견 — 절대 경로(/usr/bin/git)
  로 우회 (환경 요인, rein 무관 추정 — 재발 시 조사).

## 다음

- 다음 작업: ①설계→계획→테스트 연쇄 드리프트 구조 재설계 ②산출물 경로 재배치.
- 참고 입력: `docs/reports/2026-08-05-rein-problem-audit.md` (자동 감사 산출물 —
  "단순화 릴리스" 제안, 사용자 검토 대기).

---
<!-- source: 2026-08-05-session.md -->
## session

### 완료 기록

- 날짜: 2026-08-05
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
(오늘 커밋 없음)

## 변경된 파일 (worktree + staged, 미커밋 전체)
plugins/rein-core/agents/plan-writer.md
plugins/rein-core/agents/spec-writer.md
plugins/rein-core/scripts/rein-codex-review.sh
plugins/rein-core/skills/codex-review/SKILL.md
scripts/rein-codex-review.sh
tests/skills/run-all.sh
trail/daily/2026-07-20.md
trail/daily/2026-07-21.md
trail/daily/2026-07-22.md
trail/daily/2026-07-23.md
trail/daily/2026-07-24.md
trail/daily/2026-07-27.md
trail/dod/.codex-reviewed
trail/dod/.incident-skill-trace.log
trail/dod/.security-reviewed
trail/dod/.spec-reviews/0c6627af16a5738d.reviewed
trail/dod/.spec-reviews/1d71594b495893ac.reviewed
trail/dod/.spec-reviews/23b9b1338f81f951.reviewed
trail/dod/.spec-reviews/3c9a3fcf7c953b5b.reviewed
trail/dod/.spec-reviews/5c87dd8dba098148.reviewed
trail/dod/.spec-reviews/6446cd2610607847.reviewed
trail/dod/.spec-reviews/68634ef0ace760b4.reviewed
trail/dod/.spec-reviews/766c547f483770b9.reviewed
trail/dod/.spec-reviews/8fd4f814d08c3a59.reviewed
trail/dod/.spec-reviews/9c0a74c428846cdc.reviewed
trail/dod/.spec-reviews/ac8d9997a1cb7298.reviewed
trail/dod/.spec-reviews/acc9a28bc3f1d90b.reviewed
trail/dod/.spec-reviews/af3caf1da4fcf76e.reviewed
trail/dod/.spec-reviews/ba25badfcf34bbc2.reviewed
trail/dod/.spec-reviews/bbb63ed5e4cc5c89.reviewed
trail/dod/.spec-reviews/be4b32212d8b0280.reviewed
trail/dod/.spec-reviews/bf4773b05b012432.reviewed
trail/dod/.spec-reviews/cd5ae090017b91b0.reviewed
trail/dod/.spec-reviews/cf5e0cddc2807680.reviewed
trail/dod/.spec-reviews/d38354899951e002.reviewed
trail/dod/.spec-reviews/d8716d1ad1d5238c.reviewed
trail/dod/.spec-reviews/da26a2ce4255b16d.reviewed
trail/dod/.spec-reviews/e1ce3084a24e3d24.reviewed
trail/dod/.spec-reviews/e57079fcc53989af.reviewed
trail/dod/.spec-reviews/f15853b9caf25d9d.reviewed
trail/dod/.spec-reviews/fb48514f3c55e0ff.reviewed
trail/dod/.spec-reviews/fcea31c3b7c66662.reviewed
trail/dod/COMPLETED_ARCHIVE.md
trail/dod/dod-2026-07-27-persona-select-create-entry.md
trail/dod/dod-2026-07-27-review-watchdog-false-stall.md
trail/inbox/2026-07-27-backlog-next-two-cycles.md
trail/incidents/blocks.jsonl
trail/index.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.


---
## 2026-08-07
# Daily Summary: 2026-08-07

---
<!-- source: 2026-08-07-v1-safety-prerelease.md -->
## v1-safety-prerelease

### DoD

- 날짜: 2026-08-07
- slug: v1-safety-prerelease
- 유형: 버그/보안 수정 + 메인테이너 인프라 (릴리스 번들)
- 입력: `docs/reports/2026-08-05-rein-problem-audit.md` 문제 1(긴급)·문제 2(높음) + v2 설계 문서 §27 (v1 Safety Pre-Release) + 2026-08-07 읽기 전용 정찰
- 사용자 결정 (2026-08-07): ① v2 재구조화 착수 전 안전 조치를 v1 소릴리스로 선행 (감사 보고서의 "게이트 오판 결함 3건"은 v1.6.5 로 기배포 — 본 번들 제외) ② 기존 248KB 추적 로그는 이번에 손대지 않음 — 신규 기록부터 마스킹 적용 (원문 잔존 위험은 문서화로 수용, 후속 결정 가능) ③ 리뷰는 codex 한도 복구(8/10) 전 대체 리뷰어 경로로 즉시 진행.

## 배경

감사 보고서 실증: ① 보안 가드가 차단한 Bash 명령의 **원문 전체**가 git 추적 파일에 영구 기록된다 — 토큰/비밀번호가 인라인 포함된 명령을 차단하는 순간 가드 자신이 비밀값 저장 경로가 된다 (현재 248KB, 393건, 테스트 노이즈 다수 혼입). ② 테스트 30,000줄이 있으나 CI 는 수동 실행 전용 + hooks/scripts 스위트만 실행 — push/PR 회귀 검출 없음. ③ 태그 push 가 사전 검증 없이 배포를 시작한다. ②는 v2 마이그레이션 Phase 0(행위 고정)의 전제 조건이기도 하다.

## 범위

### 1. 차단 로그 민감정보 봉합 (긴급 — 사용자 노출 hook 동작)

`plugins/rein-core/hooks/lib/bash-guard-infra.sh` 의 `log_block()` (호출자: 안전 가드·커밋 게이트·편집 게이트).

- **추적 파일(`trail/incidents/blocks.jsonl`)에 명령 원문 기록 중단.** 구조화 레코드만 기록: `hook`, `reason`, 명령 동사/작업 분류, 마스킹된 대상 표현, `source` (test 여부).
- **마스킹**: 기록 전 token / password / secret / Authorization 헤더 / URL credential(`user:pass@`, `?token=`) / API key 패턴을 `<REDACTED>` 치환. 마스킹 불확실 시 보수적으로 대상 필드 전체 마스킹 (fail-closed).
- **원문은 git 비추적 로컬 로그로 이동**: `.rein/logs/blocks-raw.jsonl` (gitignore 등재 + `git check-ignore` 검증). 단순 크기 상한 회전(마지막 N건 유지).
- **테스트발 이벤트 태깅·집계 제외**: 테스트 하니스 공통 셋업에 env 표식 추가 → `source: test` 기록, 반복 경고 카운트와 incident 집계에서 제외.
- **누적 카운트 산정 개선**: 카운트는 test 제외 레코드만 대상 (기존 전체 파일 재스캔 로직 유지하되 대상 축소).
- **기존 248KB 추적 파일**: 사용자 결정 — **이번 사이클에서 불변** (신규 기록만 마스킹 형식). 원문이 현재 파일·git 히스토리에 잔존하나 public mirror 는 운영 기록 전체 strip 이라 외부 노출 없음 — origin 사설 저장소 한정 위험으로 수용, 이관/재작성은 후속 결정 항목으로 남김.
- legacy `trail/incidents/blocks.log` 도 여전히 기록 중이면 동일 정책 적용.

### 2. 전체 자동 CI (메인테이너 인프라)

`.github/workflows/tests.yml`:

- 트리거 추가: `push` (dev) + `pull_request` (main·dev 대상), `workflow_dispatch` 유지. "수동 실행 전용" 정책 주석은 본 사용자 결정으로 폐기 — 주석 갱신.
- 누락 스위트 추가: `tests/skills/run-all.sh`, `tests/rules/run-all.sh`, `tests/agents/run-all.sh`, `tests/rein-test.sh` (CLI), `tests/integration/` (러너 부재 시 `run-all.sh` 신설), `tests/cli/test-install.sh`.
- `workflow_call` 트리거 추가 — 배포 preflight 가 재사용 (감사 보고서 "reusable workflow" 권고).
- Windows advisory 매트릭스는 현행 유지 (advisory — 릴리스 비차단).

### 3. 배포 사전검증 연결 (메인테이너 인프라)

`.github/workflows/publish-plugin.yml`:

- preflight job 신설: 재사용 테스트 워크플로 호출 + `scripts/rein-check-plugin-drift.py` 실행.
- publish job 에 `needs: preflight` — 사전검증 실패 시 배포 미실행.

### 4. CI 초록 전제 수리 (구현 중 발견 — 기존 실패, 본 변경과 무관함을 HEAD 격리 실행으로 확인)

②를 연결하는 순간 CI 가 빨간불로 시작하면 알람 피로(감사 보고서 문제 6)가 재현되므로, 신규 편입 스위트의 기존 실패를 함께 수리:

- **통합 테스트 fixture 드리프트**: `test-fresh-design-spec-review-no-fallback.sh` 시나리오 3 이 세션 시작 훅의 부트스트랩 표식 계약(v1.2.0~, `.rein/project.json` + `trail/index.md` 요구)을 못 따라와 조기 exit 로 실패 — fixture 에 표식 2개 seed (테스트 전용 수리, 제품 동작 불변).
- **라우팅 맵 바이트 예산 초과**: `routing-map.md` 875B > 800B 상한 (v1.6.3 알려진 백로그). 표·의미 불변, 중복 안내 문구만 압축 → 770B. 관련 스위트(projection·emit·rules·agents) 전량 통과 확인.
- **죽은 설치 테스트 CI 제외**: `tests/cli/test-install.sh` 는 v1.0.1 에서 삭제된 `install.sh` 를 대상으로 하는 사문화 테스트 — CI 편입에서 제외, 파일 삭제 여부는 후속 백로그 (사용자 결정).

### 범위 외

- 감사 보고서 게이트 오판 결함 3건 (v1.6.5 기배포).
- git 히스토리 원문 정화 (파괴적 — 필요 시 별도 결정).
- incident epoch 분리 (해소 후 재발 신규 계산 — 감사 개선안 6번, 후속 백로그).
- mirror workflow 의 preflight 연결 (배포 경로 우선 — 후속).
- `log_block` 전체 파일 재스캔의 성능 재설계 (대상 축소로 완화만 — v2 상태 저장소에서 근본 해소).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/hooks/lib/rein-log-block.py` | 신규 — ① 마스킹·분리 기록·live-only 카운트 SSOT 헬퍼 |
| `plugins/rein-core/hooks/lib/bash-guard-infra.sh` | ① — `log_block()` 이 SSOT 헬퍼 위임 (command 모드), `bg_infra_init` 에 헬퍼 경로 배선 |
| `plugins/rein-core/hooks/pre-edit-dod-gate.sh` | ① — 자체 `log_block()` 도 동일 헬퍼 위임 (path 모드) |
| `plugins/rein-core/scripts/rein-aggregate-incidents.py` | ① — source=test 레코드 incident 집계 제외 |
| `scripts/rein-aggregate-incidents.py` | ① — fallback 본 동기 (drift parity) |
| `.gitignore` | ① — `/.rein/logs/` 비추적 등재 |
| `tests/hooks/lib/test-harness.sh` | ① — `REIN_TEST_MODE=1` export (test 출처 태깅) |
| `tests/hooks/test-bash-guard-log-redaction.sh` | 신규 — ① 행위 기반 검증 10건 |
| `.github/workflows/tests.yml` | ② — push(dev)/PR/workflow_call 트리거 + 전 스위트 + 비용 제어 매트릭스 |
| `tests/integration/run-all.sh` | 신규 — ② 통합 스위트 러너 |
| `.github/workflows/publish-plugin.yml` | ③ — preflight 2job(tests 재사용 + drift) `needs` 배선 |
| `tests/integration/test-fresh-design-spec-review-no-fallback.sh` | ④ — 부트스트랩 표식 fixture 수리 |
| `plugins/rein-core/rules/routing-map.md` | ④ — 바이트 예산 트림 875B→770B (표·의미 불변) |

## 검증 기준

- [ ] 비밀값 패턴이 포함된 명령을 차단해도 추적 파일에 원문·비밀값이 기록되지 않는다 (행위 테스트 — 실제 훅 실행 + 기록 내용 단언).
- [ ] 원문 로그는 git 비추적 경로에만 남는다 (`git check-ignore` 확인 포함).
- [ ] 테스트발 차단이 반복 경고 임계 카운트에 포함되지 않는다.
- [ ] 차단 동작 자체(exit 2 / JSON deny)와 경고 임계 동작 무회귀 — 기존 가드 테스트 전량 통과.
- [ ] CI: dev push 에서 전 스위트가 자동 실행되고 성공한다 (본 번들 커밋의 실제 workflow 실행으로 확인).
- [ ] publish 가 preflight 실패 시 실행되지 않는 구조다 (`needs` 배선 + workflow 구문 검증; 실제 태그 경유 검증은 본 번들 릴리스 태그에서 확인).
- [ ] `bash -n` 통과.
- [ ] 코드 리뷰 + 보안 리뷰 통과 (①은 보안 민감 경로 — 보안 리뷰 필수).

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 구현 (①은 게이트 인접 보안 로깅 경로라 변경 지점을 좁게 유지, ②③은 소규모 workflow 편집이라 병렬 dispatch 이득이 작음. 병렬 평가: ①↔②③ 파일 disjoint 로 병렬 가능하나 ②③ 합산 규모가 작아 순차 채택)
- skills: `rein:codex-review`, `rein:security-reviewer`
- mcps: 없음
- rationale: 직전 사이클(게이트 결함 3건)과 동일 패턴 — 행위 기반 테스트로 새 계약을 먼저 고정 후 구현. codex 사용량 한도 복구는 8/10 — 그 전 진행 시 리뷰 대체 경로(§4 Sonnet fallback, 사유 기재) 사용.

approved_by_user: true

### 완료 기록

- 날짜: 2026-08-07
- DoD: `trail/dod/dod-2026-08-07-v1-safety-prerelease.md`
- 커밋: dev `3e1213a` (15 files, +678/-70)
- 배경: v2 재구조화(Agent Governance Runtime) 착수 전 감사 보고서(`docs/reports/2026-08-05-rein-problem-audit.md`) 문제 1(긴급)·2(높음) 선행 봉합 — 사용자 확정 순서.

## 완료 내역

1. **차단 로그 민감정보 봉합** (`plugins/rein-core/hooks/lib/rein-log-block.py` 신설 SSOT): 추적 파일(`trail/incidents/blocks.jsonl`)에는 명령 동사+SHA256 12자만, 마스킹 원문은 비추적 `.rein/logs/blocks-raw.jsonl`(1000→500줄 회전). 두 log_block 호출자(bash-guard-infra=command 모드, pre-edit-dod-gate=path 모드) 위임. `REIN_TEST_MODE=1` → source=test 태깅, 반복 경고·incident 집계·세션 종료 advisory 전부 제외 (legacy 무필드=live 취급, FN 방지). aggregate 예시 수집에서 legacy 원문은 `<legacy-target-redacted>` 치환 (신규 클론/worktree 재유출 경로 봉합 — 보안 리뷰 Medium).
2. **자동 CI** (`.github/workflows/tests.yml`): dev push/PR/workflow_call 자동 실행, 7개 스위트(hooks/scripts/skills/rules/agents/CLI/integration) 편입. dev push=ubuntu만, PR/dispatch/태그=macOS 포함(비용 제어 fromJSON 매트릭스). `permissions: contents: read` 명시. `tests/integration/run-all.sh` 신설.
3. **배포 preflight** (`publish-plugin.yml`): preflight 2 job(tests 재사용 + drift check) → publish `needs` 배선.
4. **CI 초록 전제 수리** (기존 실패 — HEAD 격리 재현으로 본 변경 무관 확인): 통합 fixture 부트스트랩 표식 2종 seed, routing-map 875B→770B 압축(표·의미 불변), 죽은 설치 테스트(`tests/cli/test-install.sh`, v1.0.1 삭제된 install.sh 대상) CI 제외.

## 검증·리뷰

- 신규 행위 테스트 13건 (redaction 12 + aggregate T9) + 7개 스위트 전량 GREEN.
- 코드 리뷰: codex 사용량 한도(복구 8/10 09:58)로 **sonnet-fallback 3라운드** — R1 NEEDS-FIX(High 2: 마스킹 `\b` 밑줄 우회로 `GITHUB_TOKEN=` 류 통과, 신규 테스트 러너 미등록) → 수리 → R2 PASS → 보안 후속 3건 델타 R3 PASS. 통과 기록에 대체 리뷰어·사유 명기.
- 보안 리뷰: PASS (2026-08-07T09:57:26Z 재발급). Medium 1(legacy 예시 재유출)·Low 2(advisory 필터, CI 토큰 권한) 즉시 반영, 잔여는 백로그.

## 백로그 (후속, 전부 비차단)

- raw 로그 마스킹 정교화: 과다(`author:` 류 오탐 화이트리스트) + 과소(공백 구분 `--password s3cret`·부착형 `-ps3cret` 패턴 추가) — 묶음 처리 권장.
- 추적 레코드 `#<sha12>` → 로컬 salt HMAC 교체 검토 (약한 비밀 사전공격 확인 채널 차단) 또는 트레이드오프 수용 문서화.
- `rein-aggregate-incidents.py:409` 부근 advisory-summary 독스트링의 `source` 필드 표기를 `hook` 으로 정정 (사전 존재 표기 오류 — 신규 source 태그와 명칭 충돌).
- `tests/cli/test-install.sh` 파일 삭제 여부 — 사용자 결정 대기.
- 기존 248KB `blocks.jsonl` 이관/정화 — 사용자 결정 "이번엔 그대로" (2026-08-07). 후속 결정 시 aggregate legacy 치환이 정화 무력화를 막아줌.
- raw 로그 0600 생성 (Info, 선택 하드닝).
- **릴리스 태그 시 1회 확인**: preflight 로그에서 macOS job 실행 여부 육안 확인 (workflow_call 하 매트릭스 조건 실측).

## 릴리스 상태

dev 커밋까지 완료. 버전 승격(patch, v1.6.6 후보)·main 선별 체크아웃·태그·push·publish 는 **사용자 승인 대기**.

---
<!-- source: 2026-08-07-release-v166.md -->
## release-v166

### DoD

- 날짜: 2026-08-07
- slug: release-v166
- 유형: 릴리스 실행 (버전 승격 + main 선별 체크아웃 + 태그 + push + publish 검증)
- 입력: `trail/dod/dod-2026-08-07-v1-safety-prerelease.md` 사이클 완료분 (dev `3e1213a`+`3ab5ca2`, 리뷰 3R+보안 PASS)
- 사용자 결정 (2026-08-07): 릴리스와 v2 설계 정식화 병렬 진행. 등급은 patch (v1.6.6) — 사용자 노출 변경은 hook 로그 동작(버그/보안 수정 계열).

## 범위

1. 버전 승격 (patch): `scripts/rein.sh` VERSION 1.6.5→1.6.6 + `plugins/rein-core/.claude-plugin/plugin.json` version 동기 (VER-1 두 표면 규칙).
2. `CHANGELOG.md` — user-facing 항목만: 차단 로그 민감정보 봉합 (사용자 저장소 hook 동작 변경). CI/preflight 는 메인테이너 인프라라 제외 (Rule C).
3. `README.md`/`README.ko.md` — 버전 히스토리 최신 1건 엔트리 (1~2줄, parity 유지).
4. main 선별 체크아웃 — **격리 worktree 에서** (dev tree 는 병렬 스펙 작업으로 dirty 가능): branch-strategy 포함 목록의 변경 파일만. 태그 `v1.6.6`, push origin dev/main/tag.
5. publish 검증 — **preflight 첫 실전 구동**: preflight tests(macOS job 실행 여부 육안 확인 — 직전 사이클 이월 검증 항목)+drift 통과 후 publish 실행 확인, mirror strip 확인, public tag clean 확인.

6. **hotfix (릴리스 중 편입, 2026-08-07)**: 첫 Linux CI(dev push + preflight)가 `tests/hooks/test-state-fast-path-skip.sh` T9 로 잡아낸 `plugins/rein-core/hooks/lib/state-machine.sh` `acquire_state_lock()` flock 경로 fail-open 수리 — open/flock 실패 미검사 return 0 + timeout 계약 미구현 (macOS 는 flock 부재로 mkdir 경로만 검증돼 은닉). exec 실패·flock 실패 전파 + `-w` timeout 적용. 태그 v1.6.6 은 수리 포함 커밋으로 재지정 (미배포 상태에서의 이동 — publish 는 preflight 차단으로 미실행이었음).

### 범위 외

- 코드 변경 (직전 사이클에서 완료 — 본 DoD 는 배포 절차만).
- v2 설계 정식화 (병렬 트랙 — 별도 산출물).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `scripts/rein.sh` | VERSION 1.6.6 |
| `plugins/rein-core/.claude-plugin/plugin.json` | version 1.6.6 |
| `CHANGELOG.md` | v1.6.6 user-facing 엔트리 |
| `README.md` / `README.ko.md` | 버전 히스토리 엔트리 (parity) |

## 검증 기준

- [ ] 두 버전 표면 값 일치 (1.6.6).
- [ ] CHANGELOG 는 user-facing 만 (내부 CI 항목 제외).
- [ ] README KR/EN 엔트리 동기.
- [ ] 릴리스 델타 리뷰 통과 (버전·문서 변경분 — codex 한도 중 대체 경로, 사유 기재).
- [ ] main 커밋이 포함 목록만 담음 (`git status --short` 예상 외 파일 0).
- [ ] tag push 후 preflight 가 실행되고 **통과 후에만** publish 실행 (Actions 로그 확인).
- [ ] preflight 에서 macOS job 실행 확인 (이월 검증 항목).
- [ ] mirror: public repo 에 내부 기록(trail/ 등) strip 확인, public tag 가 clean commit 을 가리킴.
- [ ] Rule B: 오늘 main 머지 첫 회.

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 (릴리스 절차는 git 상태 조작이라 위임 부적합)
- skills: `rein:codex-review` (델타, 대체 경로), 보안은 버전·문서-only 커밋 자동 면제 대상
- mcps: 없음
- rationale: v1.6.5 릴리스와 동일 패턴. 병렬 스펙 트랙과 파일 disjoint — main 작업만 worktree 격리로 충돌 차단

approved_by_user: true

### 완료 기록

- 날짜: 2026-08-07 (~08-08 새벽 종결)
- DoD: `trail/dod/dod-2026-08-07-release-v166.md`
- 결과: dev `a754061` / main `b36a933` / tag `v1.6.6` / public `aca4535`(clean, HEAD=tag) / public plugin.json 1.6.6 / trail strip 404 / Release Latest 생성. patch (사용자 결정, Rule A 기계판정도 patch — user-facing 버그/보안 수정).

## 릴리스 서사 — preflight 가 제 몫을 한 첫 릴리스

태그 push 4회 (재지정 3회 — 전부 publish 미실행 상태의 이동이라 안전), preflight 가 결함 배포를 3번 차단:

1. **1차 차단**: 첫 Linux CI 가 `state-machine.sh` `acquire_state_lock()` flock 경로 **fail-open 제품 결함** 적발 (open/flock 실패 무시 return 0 + timeout 미구현 — WSL2/Linux 실영향, macOS 는 flock 부재로 은닉). 수리 + `-w` timeout 구현.
2. **2차 차단**: job pid 정착창 테스트 flake (status→meta→pid제거 순서의 관측 창) + DM8 골든 ISO 정규화가 Z형 미수용. 둘 다 테스트 수리 (래퍼 순서 변경 대안은 사망판정 의미론 리스크로 기각 — 리뷰 코드근거 확인).
3. **3차 차단**: main 트리 의존 테스트 3건 (dev 전용 plan·settings·branch-strategy 실파일 의존 — 태그 preflight 는 main 트리에서 돈다) + macOS 러너 PyYAML 부재로 정책 로더 fail-open (정책 토글 테스트 실패) + routing-map 표 파서의 awk 다중바이트 구현편차 (8행 중 4행 유실). SKIP 가드 3건 + CI python/PyYAML 결정론 고정 + 파서 바이트 안전 재작성.
4. **4차: 전체 통과** — ubuntu·macOS 초록 + drift 통과 → publish 성공. macOS preflight 실행 확인 (이월 검증 항목 종결).

리뷰: 릴리스 사이클 sonnet 대체 누적 7회차 전부 PASS (codex 한도 8/10 복구 전 — 사유 기록), 보안 재발급 7회. main worktree 7스위트 전체 배터리 시뮬레이션(리뷰어 독립 재현 포함 2회) 이 4차 통과를 사전 보증.

## 교훈 (재발 방지 코어)

- **태그 preflight = main 트리 실행.** dev 초록 ≠ main 초록. 릴리스 전 main worktree 전체 배터리 시뮬레이션을 표준 절차로 (이번 3차부터 적용해 4차를 1회로 종결).
- 첫 CI 도입은 "숨어 있던 환경 의존"의 일괄 청산이다: OS 편차(flock/awk), 러너 이미지 의존(PyYAML), 트리 의존(dev 전용 파일), 타이밍(정착창), 로케일(ISO Z형). 전부 실결함 또는 테스트 결함이었고 오탐 0.
- CI 스위트 스텝에 `!cancelled()` — 실패 전량이 한 런에 드러나야 양파 까기가 안 됨.

## 백로그 (누적, 전부 비차단)

- PyYAML 부재 시 정책 opt-out 무경고 무시 — 로더에 stderr 1줄 안내 (보안 리뷰어 제안, "침묵 실패" 클래스).
- drift-boundary T8 의 main 트리 의존 (preflight 경로 밖).
- 표 파서 헤더 2앵커 순서 불문 — 향후 행 추가 시 이론적 오매칭 (advisory).
- CI preamble 실패 시 후속 스텝 중복 노이즈 / pyyaml 버전 고정 검토.
- Windows advisory job 실패 상존 (python stub 계열 — 설계상 비차단, 별도 사이클).
- (안전 소릴리스分 백로그 7건은 `trail/inbox/2026-08-07-v1-safety-prerelease.md`)

## 병렬 트랙 (v2)

같은 세션에서 v2 설계 정식화 완료 — brainstorm 확정본 + spec 42 Scope Items (`docs/specs/2026-08-07-rein-v2-governance-orchestration.md`), 문서 리뷰 대체 경로 2회차 PASS + 표식. 다음: plan 단계 (codex 복구 8/10 이후 권장).


---
## 2026-08-08
# Daily Summary: 2026-08-08

---
<!-- source: 2026-08-08-mirror-publish-strip.md -->
## mirror-publish-strip

### DoD

- 날짜: 2026-08-08
- slug: mirror-publish-strip
- 유형: 버그 수정 (v1.6.6 릴리스 후속 — 공개 저장소 외관 결함)
- 입력: 2026-08-07 실측 — public rein 에 미러 push 5회마다 0s 실패 런. 원인: 개편된 publish 워크플로가 reusable `tests.yml` 을 참조하는데 미러가 public 에서 `tests.yml` 만 strip → public 의 워크플로 파일이 invalid (없는 파일 참조) → push 마다 실패 런 생성.

## 범위

1. `.github/workflows/mirror-to-public.yml` — strip 목록에 `publish-plugin.yml` 추가 (배포는 rein-dev 전용, public 에 있을 이유 없음). **[적용 완료 — 본 기준서 작성 전 편집분]**
2. `.claude/rules/branch-strategy.md` — publish 워크플로 행에 public strip 방침 명기 (dev 전용 정책 문서).
3. main 반영 (mirror 워크플로는 main 포함 대상) → 미러 재실행으로 public 에서 publish 워크플로 제거 확인 → 이후 push 에 실패 런 미생성 검증.

### 범위 외

- 기존 실패 런 5건의 이력 삭제 (GitHub 이력은 남음 — 신규 발생 차단만).
- public 의 다른 워크플로 정비 (issue-triage 는 정상 유지).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `.github/workflows/mirror-to-public.yml` | strip 목록에 publish-plugin.yml 추가 |
| `.claude/rules/branch-strategy.md` | strip 방침 문서화 (dev 전용) |

## 검증 기준

- [ ] mirror 워크플로 YAML 파싱 통과.
- [ ] main 반영 후 미러 성공 + public `.github/workflows/` 에 publish-plugin.yml 부재.
- [ ] 이후 미러 push 에서 public 실패 런 미생성.
- [ ] 코드 리뷰 통과 (소규모 델타 — 대체 경로, 사유 기재).

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 (2파일 소규모 수리)
- skills: `rein:codex-review` (대체 경로)
- mcps: 없음
- rationale: v1.6.6 릴리스 검증 항목(미러 strip 확인)에서 발견된 후속 결함의 즉시 봉합

approved_by_user: true

### 완료 기록

- 날짜: 2026-08-08
- DoD: `trail/dod/dod-2026-08-08-mirror-publish-strip.md`
- 커밋: dev `d324ba4` / main `1256b32` / 미러 success (14s)

## 내용

v1.6.6 릴리스 후속 — public rein 에 미러 push 마다 0s 실패 런 5건 발생. 원인: 개편된 publish 워크플로가 reusable `tests.yml` 을 참조하는데 미러가 public 에서 `tests.yml` 만 strip → public 의 워크플로 파일이 invalid. 수리: strip 목록에 `publish-plugin.yml` 추가 (배포는 rein-dev 전용) + branch-strategy 방침 문서화.

## 검증

- 미러 재실행 success 후 public 의 `.github/workflows/publish-plugin.yml` 404 확인 — 새 실패 런 미생성 (기존 5건은 이력으로 잔존, 삭제 불가).
- 리뷰 1회차 PASS (대체 경로) — 부수 확인: public 중복 publish 는 토큰 활성화 시 Anthropic API 이중 POST 위험이었음 → 이번 strip 이 기능 손실 없이 제거 (rein-publish.sh 는 어디서 돌든 push 안 함, 산출물은 아티팩트 전용). 보안 확인 PASS.

---
<!-- source: 2026-08-08-session.md -->
## session

### 완료 기록

- 날짜: 2026-08-08
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
a754061 fix(tests): routing-map 표 파서 바이트 안전 재작성 — awk 다중바이트 편차 종결

## 변경된 파일 (worktree + staged, 미커밋 전체)
trail/dod/dod-2026-08-07-v1-safety-prerelease.md
trail/inbox/2026-08-07-v1-safety-prerelease.md
trail/incidents/blocks.jsonl
trail/index.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.

---
<!-- source: 2026-08-08-v2-phase0.md -->
## v2-phase0

### DoD

- 날짜: 2026-08-08
- slug: v2-phase0
- 유형: 구현 (Phase 단위 실행 — plan D2: 한 Phase 착수 = DoD 1건)
- plan ref: docs/plans/2026-08-08-rein-v2-governance-orchestration.md
- 입력: 사용자 지시 (2026-08-08): "모델 개발 들어가자. 넌 오케스트레이팅만 하고 다른 오빠들 불러서 일을 시켜" — v2 구현 착수 승인 + 오케스트레이터 전담(직접 편집 금지, 서브에이전트 워커 실행).
- covers: [v1-behavior-suite-stays-green-in-ci-throughout-pre-authority-phases]

## 범위

plan 의 Phase 0 heading 전체 (Task 0.1 + Task 0.2). 웨이브 dispatch 는 `rein:parallel-execute` 절차를 따른다 (schedule emitter step 1~2).

1. **Task 0.1 (`p0-baseline`, mutating 단독 웨이브)** — 저장소 루트 `tests/**` 7개 스위트 전체 실행 GREEN 확인 + 최신 dev push CI GREEN 링크 확인 + `docs/reports/v2-phase-gates.md` 대장 개설 (기준선: 실행 커밋 sha·스위트별 결과·CI run 링크).
2. **Task 0.2 (`p0-branch-strategy`, edit_only 웨이브)** — v2 신규 5경로 (`plugins/rein-core/{rein,bin,policies,schemas,tests}/**`) 의 main 머지/plugin tarball/public mirror 포함 여부를 `.claude/rules/branch-strategy.md` 포함 표에 추가 결정.

### 범위 외

- Phase 1 착수 (별도 DoD — SPIKE-1 이 Phase 1 본구현 진입 게이트).
- v1 스위트 RED 시 수리 (발견 시 v2 착수 중단 후 별도 수리 사이클 — plan Task 0.1 Step 1).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `docs/reports/v2-phase-gates.md` | 신규 — Phase 게이트 대장 + Phase 0 기준선 |
| `.claude/rules/branch-strategy.md` | 수정 — v2 신규 5경로 배포 규정 추가 |

## 검증 기준

- [ ] v1 행위 스위트 7종 전체 GREEN (RED 발견 시 착수 중단·보고).
- [ ] `docs/reports/v2-phase-gates.md` 존재 + 기준선(sha·스위트 결과·CI 링크) 기록.
- [ ] branch-strategy 포함 표에 신규 5경로 전부 포함/제외 결정 존재.
- [ ] 각 웨이브 델타가 선언 scope 부분집합 (parallel-execute barrier 검증 통과).
- [ ] 웨이브당 1커밋, plan 명시 커밋 메시지 사용.

## 라우팅 추천

- agent: 워커 dispatch = 서브에이전트 (mutating 1건 단독 + edit_only 1건). 부모(메인 세션)는 오케스트레이션·barrier 검증·커밋만 소유.
- skills: `rein:parallel-execute` (웨이브 스케줄·dispatch 계약·barrier 통합 SSOT). 리뷰는 docs-only 변경이라 codex 리뷰 경로 유지하되 한도 시 §4 fallback.
- mcps: 없음
- rationale: 사용자가 오케스트레이터 전담 + 워커 위임을 명시 지정. plan 실행 전략 v2 존재 → parallel-execute 가 표준 경로.

approved_by_user: true

### 완료 기록

- DoD: `trail/dod/dod-2026-08-08-v2-phase0.md` / plan: `docs/plans/2026-08-08-rein-v2-governance-orchestration.md` Phase 0
- 실행 방식: 오케스트레이터 전담 (사용자 지시) — 부모는 dispatch·barrier 검증·커밋만, 편집·측정은 서브에이전트 워커 2건.

## 결과

1. **Task 0.1 (`p0-baseline`)** — v1 행위 스위트 7종 (hooks 107 / scripts 62 / skills 16 / rules 10 / agents 5 / CLI 15 asserts / integration 3) 전부 로컬 GREEN (실행 커밋 `68d3439`). 최신 dev push CI (tests.yml, `36e45c5`) success. `docs/reports/v2-phase-gates.md` 게이트 대장 개설 (append 계약 명시). 커밋 `4be8bca`.
2. **Task 0.2 (`p0-branch-strategy`)** — v2 신규 5경로를 branch-strategy 포함 표에 추가. `rein/**`·`bin/**`·`policies/**`·`schemas/**` = main+tarball+public 포함. `plugins/rein-core/tests/**` = **tarball 포함 (제외 메커니즘 부재 — 용량 수용)**: `rein-publish.sh` 의 `tar -C plugins -- rein-core` 가 디렉토리 전체를 묶고 `--exclude`/ignore 파일 부재를 실측 — plan 의 "제외 유력" 후보를 사실 확인 후 뒤집어 기록 (제외하려면 메커니즘 신설 선행). 커밋 `002126e`.

## barrier 검증

- 두 웨이브 모두 델타 = 선언 scope 부분집합 (각 1파일). 웨이브당 1커밋.
- 워커 특이사항: Task 0.1 실행 중 rein 훅이 trail 회전을 자동 수행 → 워커가 계약대로 trail/ 복원 후 종료 (scope 오염 없음).

## 다음

- Phase 1 착수 (별도 DoD `dod-2026-08-08-v2-phase1.md`) — scaffold → SPIKE-1 콜드스타트 게이트가 본구현 진입 결정.

---
<!-- source: 2026-08-08-v2-phase1.md -->
## v2-phase1

### DoD

- 날짜: 2026-08-08
- slug: v2-phase1
- 유형: 구현 (Phase 단위 실행 — plan D2: 한 Phase 착수 = DoD 1건)
- plan ref: docs/plans/2026-08-08-rein-v2-governance-orchestration.md
- 입력: 사용자 포괄 지시 (2026-08-08): "모델 개발 들어가자. 넌 오케스트레이팅만 하고 다른 오빠들 불러서 일을 시켜" — v2 개발 전체 착수 + 오케스트레이터 전담. Phase 0 완료 (`trail/inbox/2026-08-08-v2-phase0.md`) 후 연속 진행.
- covers: [spike-cold-start-fixes-nfr-from-measurement-against-v1-hook-baseline, block-decision-carries-policy-missing-requirements-and-evidence-refs, policy-loader-accepts-only-four-fields-and-rejects-unknown-keys-at-load, requirement-registry-rejects-project-declared-custom-requirement-names, evidence-with-mismatched-policy-version-is-invalid-unless-compat-declared, evaluator-returns-allow-with-reason-when-no-requirement-matches-event, failure-mode-triggers-only-on-evaluation-failure-never-on-missing-evidence, platform-adapter-normalizes-claude-hook-names-so-kernel-imports-no-platform-symbols, stop-event-downgrades-ask-user-to-block-with-guidance-inside-claude-adapter]

## 범위

plan 의 Phase 1 heading 전체 (Task 1.1 ~ 1.9). Authority 는 v1 유지 — Phase 1 의 v2 코드는 hooks.json 에 연결하지 않는다 (테스트가 runtime 직접 구동). 웨이브는 schedule emitter step 3~8 순서를 따른다:

1. `p1-scaffold` (edit_only) — Task 1.1 walking skeleton (bin/rein 왕복 + 측정 6종 경로).
2. `p1-spike1` (mutating 단독) — Task 1.2 SPIKE-1 콜드스타트 실측·NFR 확정. **이 판정 전 Task 1.3 이후 착수 금지.** spec 개정 분기 시 plan 실행 중단 + 사용자 보고.
3. `p1-kernel-core` + `p1-policy-loader` + `p1-requirement-registry` + `p1-evidence-versioning` (edit_only 4건 병렬) — Task 1.3~1.6.
4. `p1-evaluator` (edit_only) — Task 1.7.
5. `p1-platform-adapter` (edit_only) — Task 1.8.
6. `p1-ci-integration` (mutating 단독) — Task 1.9 CI 편입 (push + CI 확인 포함).

### 범위 외

- Phase 2 착수 (별도 DoD — SPIKE-2 가 진입 게이트).
- SPIKE-1 초과 시 아키텍처 재검토·spec 개정 (발생 시 중단·사용자 보고 후 별도 사이클).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/bin/rein` | 신규 — 훅 어댑터 진입점 (self-location) |
| `plugins/rein-core/rein/**` (package 골격 + kernel/engine/platform 모듈) | 신규 — plan Task 1.1·1.3~1.8 Files 목록 |
| `plugins/rein-core/tests/{unit,contract,perf}/**` | 신규 — v2 스위트 (stdlib unittest) |
| `docs/reports/v2-spike1-coldstart.md` | 신규 — SPIKE-1 측정 리포트 |
| `docs/reports/v2-phase-gates.md` | 수정 — SPIKE-1 판정 + 진입 기록 append |
| `.github/workflows/tests.yml` | 수정 — v2 스위트 step 추가 |

## 검증 기준

- [ ] 각 웨이브 델타 ⊆ 선언 scope (barrier 검증), 웨이브당 1커밋 + plan 명시 커밋 메시지.
- [ ] v2 스위트 실행 명령 `python3 -m unittest discover -s plugins/rein-core/tests` 웨이브 경계마다 GREEN.
- [ ] SPIKE-1 리포트에 판정 3분기 중 하나 명시 기록 (확정 / 최적화 편입 후 재측정 / spec 개정=중단·보고).
- [ ] kernel 격리: kernel/ 모듈에 platform 심볼 import 0건 (계약 테스트).
- [ ] CI run 에 v1·v2 두 step 각각 GREEN (Task 1.9).
- [ ] 코드 커밋은 리뷰·보안 리뷰 통과 후 (codex 한도 중이면 §4 Sonnet fallback, 사유 기재).

## 라우팅 추천

- agent: 워커 dispatch = 서브에이전트 (edit_only 병렬 + mutating 단독). 부모는 오케스트레이션·barrier·테스트·리뷰 조율·커밋 소유.
- skills: `rein:parallel-execute` (스케줄 SSOT) + `rein:codex-review` (코드 웨이브 리뷰 — 한도 시 `rein:code-reviewer` fallback) + `rein:security-reviewer` (보안 대상 파일).
- mcps: 없음
- rationale: 사용자가 오케스트레이터 전담을 명시. plan 실행 전략 v2 + TDD (실패 테스트 선행) 준수.

approved_by_user: true

### 완료 기록

- DoD: `trail/dod/dod-2026-08-08-v2-phase1.md` / plan: `docs/plans/2026-08-08-rein-v2-governance-orchestration.md` Phase 1 (Task 1.1~1.9)
- 실행 방식: 오케스트레이터 전담 (사용자 지시) — 워커 서브에이전트 8명(구현 6+CI 편집 1+재수정 재개 1) + 독립 리뷰어 5회 + 보안 리뷰어 4회. 부모는 dispatch·barrier 검증·리뷰 조율·커밋·push 만.

## 웨이브별 결과 (전부 barrier 검증: 델타 ⊆ 선언 scope, 웨이브당 1커밋)

1. **Task 1.1 scaffold** (`73fbbc8`) — bin/rein 왕복 골격 + 측정 6종 경로. 워커가 **plan 러너 명령의 false-green 함정 발견**(`__init__.py` 부재 시 0 tests+OK)하고 blocked 정직 보고 → 부모가 8개 스위트 디렉토리 빈 `__init__.py` 9건 보수. 리뷰 Low 4건 중 2건 수정 (policy 디렉토리 OSError 명시화·테스트 env 격리).
2. **Task 1.2 SPIKE-1** (`55d718e`) — **판정 (a) NFR 실측 확정**: v2 왕복 p95 cold 29.35/warm 29.95ms vs v1 훅 체인 344.59/346.67ms (**≈11.5×**, 리뷰어 독립 30회 재측정 재현 ≈11.7×). 최적화 편입·재검토 분기 불필요. 게이트 재확인이 CLI 단독 기록으로 온 것을 부모가 **7종 전체 배터리 재실행으로 보완** (기준선-재확인 배터리 동일성). harness 의 requirement 이름 forward-compat 결함(Task 1.5 폐쇄 도입 시 게이트 재측정 파손) 사전 수리.
3. **Task 1.3~1.6 병렬 4워커** (`fe52091`, 12파일 1,737줄) — kernel 모델(Domain Event 금지·Decision 5필드)/폐쇄 로더(D3 파서 `yaml_subset.py` 분리, 거부 구문 8종 fixture)/requirement 5종 registry/evidence version 결속. 통합 리뷰 M1: **5종 폐쇄가 로드 경로에 미배선**(병렬 seam) → 로더에 즉시 배선+e2e. L: 문자열 오용 fail-open 2건·tab 문구. 재리뷰 PASS.
4. **Task 1.7 evaluator** (`2a2eb88`) — 경계 3종 (미매칭 ALLOW+reason / **evidence 부재=정상 BLOCK, failure_mode 미적용** / 판단 불능 시만 closed·open·ask_user, 미지 mode fail-closed). runtime 재배선 (구 파서 삭제·PolicyLoadError 단일화·kernel `__init__` re-export). 리뷰 M-1 **복수 policy 매칭 시 BLOCK 오귀속** → 워커 재개(SendMessage)로 소유자 귀속 수리 + 상호작용 테스트, 재리뷰 PASS(역순·중복 edge 실측).
5. **Task 1.8 adapter** (`06ba87a`) — 매핑 9종 (§34 원칙, session 계열 3종 EVENT_TYPES 확장 6→9), Stop 강등 (ASK_USER→BLOCK+안내, v1 훅 응답 shape 정합), 계약 테스트 4종 (native 이름 전 패키지 스캔·AST 격리·동적 import 금지·stdlib platform 비오염 — 리뷰 권고 하드닝 2건 반영).
6. **Task 1.9 CI 편입** (`e3c9a1e`, push) — tests.yml 에 v2 별개 step. **CI run success**: <https://github.com/JayJihyunKim/rein-dev/actions/runs/31254283699> — v1 7 step + v2 step 전부 GREEN (완료 판정 충족).

## 리뷰 경로

codex 사용량 한도 (복구 8/10 09:58) → 전 웨이브 §4 대체 경로 (독립 리뷰어 에이전트 + 표식에 사유 기재). 발견→수정→재리뷰 사이클 준수 (수정과 도장 같은 회차 금지). 보안 리뷰 4회 전부 통과 (차단급 0).

## 이월 (Phase 마감 시 or 후속)

- 에러 메시지 언어 혼재 (KR/EN) + import 스타일 통일 — 사용자 언어 정책 결정 필요.
- frozen dataclass 의 dict 필드 방어 복사 (Event.payload/Evidence.metadata).
- yaml_subset 재귀 깊이 상한 (RecursionError → 명시 에러 변환, 보안 Low).
- SPIKE-1 harness 의 timeout/json 에러 경로 verbosity (Low 2건).
- 격리 테스트 eval/exec 토큰 추가 하드닝 (선택 — 위협모델상 보류 가능).

## 다음

- Phase 2 착수 (별도 DoD) — 진입 게이트 = Task 2.0 SPIKE-2 (v1 Shadow 기록 비용 실측, Task 2.8 실행 여부 결정).

---
<!-- source: 2026-08-08-v2-plan.md -->
## v2-plan

### DoD

- 날짜: 2026-08-08
- slug: v2-plan
- 유형: plan 작성 (설계 산출물 — 코드 변경 없음)
- 입력: `docs/specs/2026-08-07-rein-v2-governance-orchestration.md` (문서 리뷰 2회차 통과 + 표식). 사용자 지시 (2026-08-08): "대체 리뷰어로 계획서 작성해. 대체 리뷰어는 opus 에이전트 새로 띄워서".

## 범위

1. `docs/plans/2026-08-08-rein-v2-governance-orchestration.md` — spec 의 42 Scope Items 전량을 커버하는 구현 계획: coverage 매트릭스(covers 메타데이터), Phase 구조(Safety Release 완료 반영, Phase 0~7), SPIKE 게이트 3건의 태스크화, 실행 전략(의존 그래프·병렬 가능성·scope 선언).
2. 계획 검증기(coverage validator) 통과.
3. 문서 리뷰 — codex 사용량 한도(복구 8/10 09:58)로 **신규 Opus 에이전트** 를 독립 리뷰어로 사용 (사용자 지정). 문서 리뷰 축(결정 건전성·범위·추적성)만 판정, 리뷰어는 문서 수정 금지, 지적은 작성자(주 세션)가 보완 후 재리뷰. 통과 시 문서 리뷰 표식 생성 (대체 리뷰어 식별자 기재).

### 범위 외

- v2 구현 착수 (계획서 통과 후 별도 사이클 — SPIKE-1 이 Phase 1 진입 게이트).
- spec 자체의 재개정 (계획 작성 중 spec 결함 발견 시 별도 보완 사이클).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `docs/plans/2026-08-08-rein-v2-governance-orchestration.md` | 신규 — v2 구현 계획 |

## 검증 기준

- [ ] 계획이 spec 42 Scope Items 전량을 covers 매트릭스로 커버 (검증기 통과 — 누락 0).
- [ ] SPIKE 3건이 게이트 위치·측정·판정 기준과 함께 태스크로 존재.
- [ ] 실행 전략 절 존재 (의존/병렬/scope — 병렬 실행 스킬이 읽는 v2 형식).
- [ ] Opus 독립 리뷰 통과 + 표식 생성 (리뷰어 식별자 기재).
- [ ] 코드 변경 없음 (docs + trail 만).

## 라우팅 추천

- agent: `rein:plan-writer` (계획 작성 전담 — coverage 매트릭스 + 검증기 경로)
- skills: 문서 리뷰는 codex 한도로 신규 Opus 에이전트 대체 (사용자 지정)
- mcps: 없음
- rationale: routing map 의 plan 작성 → plan-writer 표준 경로. 리뷰어만 사용자 지정 대체

approved_by_user: true

### 완료 기록

- 날짜: 2026-08-08
- DoD: `trail/dod/dod-2026-08-08-v2-plan.md`
- 커밋: dev `1219b81`
- 산출물: `docs/plans/2026-08-08-rein-v2-governance-orchestration.md` (45태스크 · 8 Phase · 32웨이브)

## 내용

spec(42 Scope Items)의 구현 계획화 — 설계→계획 체인 완성. plan-writer 작성 + 신규 Opus 에이전트 독립 리뷰(codex 한도, 사용자 지정 대체) 3라운드:

- **R1 NEEDS-FIX**: High 2(태스크 scope 가 Steps 보다 좁은 자기모순 4건 — 계획이 코드화할 규칙에 스스로 위배 / 설정 파서 subset 정의가 자기 파일을 못 읽음) + Medium 8 + Low 5 + 자진신고 판정 3(설정 YAML 유지 조건부 승인·단일 plan 승인·패키지명 충돌 기전 교정) + 구현 이관 4.
- **R2 NEEDS-FIX**: R1 전량 닫힘 + 신규 3 (관문 그래프 의존 복원 요구 — "기계가 읽지 않는 산문 게이트" 클래스 재발 지적이 백미).
- **R3 PASS**: 전량 종결. 리뷰어가 검증기·스케줄 재실행으로 순서 해소를 실측 재현.

주요 계획 결정: D1 설정 위치 / D2 단일 umbrella plan(기본값 의도적 이탈+근거) / D3 YAML subset 내장 파서(fail-closed, py3.9 호환) / D4 테스트 디렉토리 확장 / D5 워커는 v1 에이전트 매핑 재사용+dispatch 계약 / D6 Phase 4 의도적 직렬화 / D7 policy 디렉토리 공존. SPIKE 게이트 3건이 Phase 1/2/5 진입 차단.

## 다음

**v2 Phase 0 실행** (v1 행위 계약 기준선 + branch-strategy 신규 경로 결정) — 착수는 사용자 승인 후. codex 복구(8/10) 시 spec+plan 묶음 codex 재검토(이중 확인) 선택지 있음.


---
## 2026-08-09
# Daily Summary: 2026-08-09

---
<!-- source: 2026-08-09-v2-phase2.md -->
## v2-phase2

### DoD

- 날짜: 2026-08-08
- slug: v2-phase2
- 유형: 구현 (Phase 단위 실행 — plan D2: 한 Phase 착수 = DoD 1건)
- plan ref: docs/plans/2026-08-08-rein-v2-governance-orchestration.md
- 입력: 사용자 포괄 지시 (2026-08-08): "모델 개발 들어가자. 넌 오케스트레이팅만 하고 다른 오빠들 불러서 일을 시켜" + Phase 1 완료 후 "진행" (2026-08-08). Phase 1 완료: `trail/inbox/2026-08-08-v2-phase1.md`.
- covers: [spike-shadow-capture-decides-v1-insertion-by-latency-and-coverage-thresholds, worktree-and-staged-changesets-produce-content-digest-independent-of-mtime, evidence-expires-when-subject-digest-changes-after-creation, fact-resolver-computes-only-policy-demanded-facts-once-per-cycle, command-classifier-labels-compound-and-expansion-commands-unknown-without-guessing, change-tags-map-paths-to-code-docs-sensitive-and-never-decide-directly, runtime-state-rebuilds-equivalent-decisions-after-sqlite-deletion, local-runtime-storage-stays-untracked-and-new-files-created-mode-0600, masking-engine-serves-both-shadow-sanitization-and-block-log-redaction-as-ssot, shadow-capture-stores-masked-representation-never-raw-commands-from-capture-stage, corpus-import-rejects-cases-failing-sanitization-validation]

## 범위

plan 의 Phase 2 heading 전체 (Task 2.0 ~ 2.8). 웨이브는 schedule emitter step 9~12:

1. **`p2-spike2` (mutating 단독)** — Task 2.0 SPIKE-2 게이트: v1 hook capture 계층 프로토타입의 비용 실측 (구현 표면·latency 증가 p95·포착률). **판정 전 Task 2.1 이후 착수 금지.** 판정: latency p95 +10% 이내 또는 절대 +50ms 이내(관대한 쪽) + 주요 decision 계열 포착 누락 없음 → 채택 (Task 2.8 실행) / 부결 → Phase 6 이월 (Task 2.8 no-op 완료 처리로 후속 웨이브 해제).
2. **edit_only 6건 병렬** — Task 2.1~2.6 (changeset digest / lazy fact resolver / 명령 분류기 / change tags / SQLite storage / 마스킹 SSOT).
3. **`p2-shadow-capture` (edit_only)** — Task 2.7 Shadow Capture + Corpus 반입 검증.
4. **`p2-capture-v1-insert` (mutating 단독, SPIKE-2 채택 시에만)** — Task 2.8 v1 hook capture 삽입 (live hook 수정 — 워크트리 격리 권장, 이관 노트 4).

### 범위 외

- Phase 3 착수 (별도 DoD — Task 3.0 진입 게이트).
- SPIKE-2 부결 시 Task 2.8 (Phase 6 이월 — 게이트 기록에 skip 상태 명시).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `plugins/rein-core/tests/perf/spike2_capture_cost.sh` | 신규 — SPIKE-2 측정 harness |
| `docs/reports/v2-spike2-shadow-capture.md` | 신규 — 측정 리포트·판정 |
| `docs/reports/v2-phase-gates.md` | 수정 — Phase 2 진입 기록 append |
| `plugins/rein-core/rein/{kernel,engine,platform,shadow}/**` 해당 모듈 | 신규/수정 — plan Task 2.1~2.7 Files 목록 |
| `plugins/rein-core/tests/{unit,contract,shadow}/**` | 신규 — 각 태스크 테스트 |
| (조건부) v1 hook capture 삽입 대상 | Task 2.8 — SPIKE-2 채택 시에만, plan Files 목록 |

## 검증 기준

- [ ] SPIKE-2 리포트에 채택/이월 판정 명시 + 게이트 대장에 v1 스위트 7종 GREEN 재확인 (기준선 동일 배터리).
- [ ] 각 웨이브 델타 ⊆ 선언 scope, 웨이브당 1커밋 (리뷰·보안 통과 후).
- [ ] v2 스위트 전체 GREEN 유지 + v1 행위 불변 (Phase 2 는 v1 게이트 동작에 영향 금지 — Task 2.8 채택 시 그 삽입만 예외적 v1 접촉).
- [ ] 저장소 로컬 런타임 파일 비추적 + 신규 파일 0600 (해당 태스크 계약).

## 라우팅 추천

- agent: 워커 dispatch = 서브에이전트 (mutating 2 + edit_only 7). 부모는 오케스트레이션·barrier·리뷰 조율·커밋 소유.
- skills: `rein:parallel-execute` + 리뷰 대체 경로 (codex 한도 시 §4 fallback, 사유 기재) + `rein:security-reviewer`.
- mcps: 없음
- rationale: Phase 0·1 과 동일 운영 패턴 (사용자 지정 오케스트레이터 전담). SPIKE-2 가 Phase 2 진입 게이트.

approved_by_user: true

### 완료 기록

- DoD: `trail/dod/dod-2026-08-08-v2-phase2.md` / plan: Phase 2 (Task 2.0~2.8)
- 실행 방식: 오케스트레이터 전담 (부모는 dispatch·barrier·리뷰 조율·커밋만).

## 완료 (커밋됨)

- **Task 2.0 SPIKE-2 게이트** (`83fa3cd`) — **채택 판정**: latency 증가 p95 최대 +10.62ms (4시나리오 +5.37~+19.53%, 전부 절대 +50ms 기준 이내 — "관대한 쪽" OR 조항 적용), 포착률 11/11 (마스킹·0600 확인), 구현 표면 3파일 +101줄. live hook 무수정(temp 사본 shim) 실측 검증. v1 7종 GREEN 재확인. → **Task 2.8 실행 확정, Phase 6 이월 없음**. 리뷰 info 4건 중 수치 분해 표기 오류 2건 + 하네스 stderr 누출 1건 수정 반영. 보안 PASS (후속 권고 3건은 Task 2.8 인수 조건 후보: 마스킹 클래스별 fixture·0600 을 판정 조건에 포함·append 전 권한 보장).

## 진행 중 (미커밋 — 워킹트리 17파일)

- **Task 2.1~2.6 6워커 병렬 완료** — 전원 completed, 전체 244건 GREEN + 컴파일 OK (부모 barrier 실측). 델타 = 선언 scope 합집합 일치.
  - 2.1 내용 기반 digest (mtime 무개입, review 후 수정 시 근거 만료) / 2.2 lazy fact resolution (요구 시점 1회 캐시) / 2.3 보수적 명령 분류기 (corpus 52건, 화이트리스트 방식, 과소 판정 0) / 2.4 change tags (배포 기본값 `policies/tags.yaml` + 비직결 계약) / 2.5 SQLite storage (재구축 계약·0600/0700·비추적) / 2.6 마스킹 SSOT (v1 5건 이식 + 3클래스 + 오탐 방지).
  - **워커 2.2 의 결정 (리뷰 검증 대상)**: fact 해석 실패를 저장 손상과 동열의 "판단 불능" 으로 승격 (부재 위장 금지). 단 evaluator when-매칭 경로 통합은 evaluator 소유라 이관 — 이 이음새가 통합 리뷰 최우선 항목.
- **통합 리뷰 3회차 진행 중** — 1차 리뷰어 스트림 정지(10분 무진행), 2차(재개) 모델 한도 소진, 3차 sonnet 교체 투입 (검증 범위 압축: 이음새 실측 + 명령 분류기 우회 반례 + 재현).

## 통합 리뷰 판정 (3회차, sonnet 교체분) — PASS + Medium 1 반영

- **Medium (반영 완료)**: `rein/engine/context.py` 의 `FactResolutionError` docstring 이 "when-경로 예외 전파가 조용한 오판보다 보수적" 이라 주장했으나 **실측상 반대**. 리뷰어 재현: 예외가 evaluator 를 통과해 호출자까지 전파 → `bin/rein` main() 은 ValueError 만 잡으므로 traceback + **exit 1** → 이 저장소 hook 규약 (`pre-bash-safety-guard.sh` 주석) 상 **exit 1 = non-blocking(통과)** → fail-closed 가 아니라 **조용한 ALLOW**. 현재는 `runtime.evaluate` 가 resolver 를 배선하지 않고 어떤 hook 도 `bin/rein` 을 호출하지 않아 도달 불가(무해). → 근거 없는 안전 주장을 삭제하고 **미해결 위험 + 봉합 조건**(resolver 실배선 전 ①when-경로를 failure_mode 분기로 태우거나 ②bin/rein 에 미포착 예외→fail-closed BLOCK 매핑) 을 docstring·테스트 주석에 명시. 244건 GREEN 유지.
- **Low (추적)**: `runtime.evaluate` 가 `fact_resolvers` 를 노출하지 않아 lazy resolution 이 아직 실사용 경로 미도달 (Task 2.2 scope 상 정상 — 다음 배선 태스크 전 잊지 않도록 기록).
- 명령 분류기 우회 시도 **21종 전부 차단** (인용 은닉·유니코드 유사문자·행 구분자·주석·git 전역 옵션 등, 3건은 실제 bash 실행 대조) — 반례 없음. kernel 격리·plan 준수·재현(244 GREEN + 왕복) 이상 없음.
- 리뷰어 3회 교체 실측: 1차 스트림 정지(10분 무진행) → 2차 재개 후 모델 한도 → 3차 sonnet 완주. 대체 사유는 표식에 기재.

## 미결 사건 결정 (사용자, 2026-08-09) — **오진단 → 정정 → 수리 취소**

- 사건: 작업 문서 게이트가 편집 차단 (누적 11회).
- **1차 진단 (틀림)**: "자정 경과로 전날 DoD 가 당일 기준 미인정" → 사용자가 이 전제로 "게이트를 고친다" 승인.
- **정정 (코드 확인 결과)**: `pre-edit-dod-gate.sh` 에 **날짜 비교 로직이 없다**. 유일한 날짜 사용은 차단 안내문의 파일명 *제안* (`dod-$(date +%F)-<slug>.md`, 1241행) 뿐. pending 판정은 순수 slug↔inbox 매칭이다 — `dod-*-<slug>.md` 에 같은 slug 의 inbox 파일이 있으면 **완료**로 본다.
- **진짜 원인**: 세션 종료 게이트 대응으로 **진행 중인 Phase 2 의 완료 기록(`trail/inbox/2026-08-09-v2-phase2.md`)을 미리 작성** → phase2 DoD 가 완료로 판정 → 활성 작업 0 → 소스 편집 차단. (직전 rotation 으로 다른 08-08 DoD/inbox 쌍이 함께 이동해 phase2 만 남은 상태였다.) 이는 이미 기록된 실수 클래스 (완료 기록은 모든 게이트+커밋 통과 후에) 의 재발이다.
- **사용자 재결정 (오진단 정정 후)**: **게이트 수리 취소.** 게이트는 정상 동작 — 고칠 것은 작업 순서. 진행 중 기록은 완료 기록과 구분되게 다루고, 완료 기록은 관문 통과 후에만 쓴다.
- 이어짓기 DoD `trail/dod/dod-2026-08-09-v2-phase2-cont.md` 는 유효 (당일 작업 기준서로 계속 사용).
- **교훈**: 게이트가 막았을 때 안내문의 "오늘 날짜로 만드세요" 표현만 보고 날짜 문제로 단정했다. 차단 원인은 **판정 로직을 직접 읽어** 확인해야 한다 — 안내문은 해결책 제안이지 원인 설명이 아니다.

## 다음

1. (진행) 보안 리뷰 → Task 2.1~2.6 웨이브 커밋.
2. **게이트 수리 사이클** — 사용자 결정분. 활성 작업 문서의 날짜 경계 인정 (fail-closed 유지: "활성 마커가 가리키는 문서가 실재 + 미완료" 조건은 그대로).
3. Task 2.7 Shadow Capture + Corpus 반입 검증 (edit_only 단독).
4. Task 2.8 v1 hook capture 삽입 (mutating 단독, live hook 수정 — 워크트리 격리 권장, SPIKE-2 보안 권고 3건 인수 조건 반영).


