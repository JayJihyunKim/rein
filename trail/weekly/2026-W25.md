# Weekly Summary: 2026-W25

---
## 2026-06-15
# Daily Summary: 2026-06-15

---
<!-- source: 2026-06-15-automode-doc-fix-metacheck-untracked.md -->
## automode-doc-fix-metacheck-untracked

### DoD

- 날짜: 2026-06-15
- 유형: fix
- Scope ID: AMD-1-automode-doc-scope, MCU-1-metacheck-untracked-window
- 출처: 외부 사용자 제보 → codex-ask 독립 검증 (본 세션)

## 문제 (Symptom)

**AMD-1** — `skills/auto-mode-on/SKILL.md` 가 자동모드 시 "stop-session-gate block 이 모두 silent
처리된다" 고 과장 서술. 실제로는 incident 관련 블록(반복 advisory / 미처리 incident / 3회 초과)에만
`is_auto_mode` 분기가 있고, inbox/index "작업 증거" 종료 차단(`stop-session-gate.sh:467-481`)에는
분기가 없다. 사용자가 자동모드를 켜고도 작업증거 게이트에 막혀 혼란. **버그가 아니라 문서 과장**으로
판정(codex 권고) — 코드는 `lib/auto-mode.sh:4-8` 주석대로 "incident 관련"으로 일관, 문서만 정정한다.

**MCU-1** — `post-edit-meta-check.sh:121-135` 가 `git ls-files --others --exclude-standard` 로
untracked 파일 전체를 D 집합에 합친다. 의도는 "새 Write 파일을 잡기 위함"이나, `.gitignore` 에 안 걸린
오래 방치된 untracked(예: screens_ref/, *.parquet, key_meaning.png)까지 D 에 들어가 "변경 파일 N개 vs
diff M개 불일치" false-positive 경고를 매 편집마다 유발.

## Root cause

- AMD-1: 코드의 자동모드 적용 범위는 처음부터 incident 한정인데, 스킬 문서가 작업증거 블록까지 포함하는
  것처럼 "모두"라고 서술. 코드-문서 불일치.
- MCU-1: untracked 포함 로직이 "이번 세션에 새로 생긴 파일"과 "예전부터 방치된 파일"을 구분하지 않음.

## 범위

- **AMD-1**: `skills/auto-mode-on/SKILL.md` 의 description(3행) + 본문(8행) + 주의(23행)에서 "모든
  stop-session-gate block" 표현을 "incident 관련 stop-session-gate block"으로 정정. `auto-mode-off`
  description 도 parity 로 동일 정정. **코드 무변경.** 작업증거 게이트는 자동모드여도 발동함을 명시.
- **MCU-1**: 메타체크가 untracked 파일을 D 에 합칠 때 "세션 시작 이후 수정된 파일만" 포함하도록
  mtime-window 필터 추가. 앵커 = `trail/incidents/.session-start-line` 스탬프 mtime(세션 시작 시
  1회 기록). 앵커 부재/판독불가 시 **fail-open**(현행대로 전체 untracked 포함 — 새 Write 탐지 보존).
  tracked-modified(`git diff HEAD`) 경로는 무변경.

## 변경 파일

- `plugins/rein-core/skills/auto-mode-on/SKILL.md` — 표현 정정 (AMD-1)
- `plugins/rein-core/skills/auto-mode-off/SKILL.md` — description parity 정정 (AMD-1)
- `plugins/rein-core/hooks/post-edit-meta-check.sh` — untracked mtime-window 필터 (MCU-1)
- `tests/hooks/test-post-edit-meta-check.sh` — 방치 untracked 제외 재현 테스트 (MCU-1)

## 검증 기준

- [ ] failing test 먼저(MCU-1): `test-post-edit-meta-check.sh` 에 "세션 시작 전 mtime 의 방치
      untracked 파일은 mismatch 카운트에 안 들어간다" 케이스 추가 → 현행 코드에서 false-positive 로 실패 확인
- [ ] mtime-window 필터 적용 후 위 케이스 GREEN + "세션 시작 후 새로 Write 된 untracked 는 여전히 잡힘"
      회귀 케이스도 GREEN
- [ ] 앵커 부재 시 fail-open(전체 포함) 동작 케이스 GREEN
- [ ] `bash tests/hooks/test-post-edit-meta-check.sh` 전체 GREEN (기존 + 신규)
- [ ] `bash tests/hooks/test-post-edit-meta-check-perf.sh` GREEN (perf 회귀 없음)
- [ ] AMD-1: `rg '모두 silent|모든 stop-session-gate' plugins/rein-core/skills/auto-mode-*/SKILL.md`
      0건, "incident 관련" 한정 표현으로 교체됨 확인
- [ ] codex review + security review 통과

## 라우팅 추천

```yaml
agent: feature-builder-fix
skills: []
mcps: []
rationale: >
  MCU-1 은 reproduction-first 버그수정(메타체크 hook + 그 test). AMD-1 은 동봉된 문서 표현 정정
  (코드 무변경, 동일 PR 범위). 단일 hook + 두 SKILL.md + 한 test 만 touch. codex/security review 와
  integration 은 상위 흐름이 처리하므로 skill/mcp 불필요.
approved_by_user: true
```

### 완료 기록

## 발단

외부 사용자 제보: "자동모드 마커를 켰는데도 작업 도중 세션 종료 게이트(trail 인덱스 미갱신)가 두 번 떴다." `/codex-ask` 독립 검증으로 제보의 코드 진단 4건이 전부 사실임을 확인 (codex + 직접 대조 일치).

## 판정

- **버그 아님 / 문서 과장**: 자동모드 silent 분기는 `stop-session-gate.sh` 의 incident 관련 블록(반복 advisory / 미처리 incident / 3회 초과)에만 있고, inbox/index 작업증거 종료 차단(467-481행)에는 원래 없다. `lib/auto-mode.sh:4-8` 주석이 적용 범위를 "incident-related"로 명시적으로 좁혀 정의하므로, 코드는 일관되고 스킬 문서만 "모두 silent"라고 과장. → codex 권고대로 **문서만 정정** (사용자 결정).
- **부수 결함(메타체크)**: `post-edit-meta-check.sh` 가 untracked 전체를 D 집합에 합쳐, 방치된 untracked(*.parquet 등)가 매 편집마다 false-positive 불일치 경고를 유발. → 사용자 결정 "같이 수정".

## 한 일

- **AMD-1 (문서)**: `auto-mode-on/SKILL.md`(description+본문+주의) + `auto-mode-off/SKILL.md`(description) 에서 "모든 stop-session-gate block silent" → "incident 관련 block 만 silent, 작업증거 종료 차단은 자동모드여도 발동"으로 정정. 코드 무변경.
- **MCU-1 (코드, reproduction-first)**: untracked 를 D 에 합치기 전 mtime-window 필터 추가. 앵커 = `trail/incidents/.session-start-line` mtime(세션 시작). 앵커보다 오래된 untracked = 방치로 제외, 이후 = 이번 세션 작업으로 포함. 앵커 부재/판독불가/개별 파일 판독실패 → fail-open(전체 포함, 새 Write 탐지 보존). tracked diff 경로 무변경. 재현 fixture F14/F15/F16 추가.

## 검증

- `test-post-edit-meta-check.sh` 16 fixture GREEN, `-perf` p95 173ms(≤180) GREEN, `bash -n` OK.
- codex 코드 리뷰 PASS (코드 결함 0, codex 가 테스트 직접 재실행 확인). 보안 리뷰 PASS (적대적 파일명/mtime 실측, injection·traversal 0건, 내용 충실 표식).

## 상태

dev working tree 에 적용 완료, 미커밋. 두 리뷰 표식 존재. 릴리스(버전 bump + main 머지)는 별도 승인 대기 — Rule A 상 hook 동작변경=user-facing 버그수정 patch + SKILL.md 문서 정정 동봉.

---
<!-- source: 2026-06-15-marker-audit-findings.md -->
## marker-audit-findings

### 완료 기록

> 출처: v1.5.4(자동모드 안내 정정 + 메타체크 stale-untracked 수정) 직후, "같은 클래스의 다른 마커 문제 없나" 사용자 요청 → codex-ask 독립 감사(high, 214K 토큰) + 직접 코드 확인. **수정 보류, 기록만** (사용자 결정). 다음 세션이 이 문서를 진입점으로 집어들 것.
>
> 위협모델 = "정직한 에이전트 규율"까지 (적대적 우회 하드닝은 범위 밖). 아래 분류의 "위협모델 내"는 정직한 에이전트한테도 안전장치가 조용히 꺼지는 것, "범위 밖"은 경쟁/적대 영역.

## 🔴 High — 위협모델 내 (직접 코드 확인됨)

### M1. `.skip-spec-gate` 가 1회 면제가 아니라 영구 면제로 동작 — ✅ 해결 (2026-06-16)
> 완료: `pre-edit-dod-gate.sh` 스펙 게이트 진입부를 1회 소비(consume-on-use)로 전환. 마커 존재+게이트 활성이면 `rm -f` 후 `[ ! -e ]` 로 제거 증명 → 성공 시에만 1회 skip, 실패(dir/fifo/socket/device 잔존)는 fail-closed. audit 로그는 결과 확정 후 정직 기록(consumed vs consume_failed, `+%FT%TZ` 로 기존 writer 와 포맷 통일). codex 코드리뷰 2R PASS + 보안 PASS, 테스트 39/39(+회귀 91/91). DoD `dod-2026-06-16-skip-spec-gate-consume.md`. codex advisory(뒤 게이트가 같은 편집을 막아도 1회권은 소비)는 "스펙 게이트 1회 우회" 정의상 의도대로 — 변경 없음.
- **증상**: 스펙 리뷰 게이트를 1회 우회하려고 만든 마커가 소비(삭제)되지 않아, 수동 삭제 전까지 설계→리뷰 강제가 계속 꺼진다.
- **근거**: `pre-edit-dod-gate.sh:493` 정의, `:496` `if [ ! -f "$SKIP_SPEC_GATE" ]` 로 스펙 게이트 전체 skip. 파일 내 `SKIP_SPEC_GATE` 참조는 이 2곳뿐 — **`rm -f` 없음**.
- **대조(의도된 패턴)**: 동일한 1회 우회 `.skip-stop-gate` 는 `stop-session-gate.sh:373` 에서 사용 즉시 `rm -f`. 스펙 게이트만 소비 누락.
- **권고**: 스펙 게이트 진입 시 bypass 를 audit log 에 남기고 즉시 `rm -f "$SKIP_SPEC_GATE"` 로 consume. 삭제 실패는 fail-closed. 테스트(`test-spec-review-gate.sh:233,255`)는 "존재하면 허용"만 검증하니 "소비 후 다음 편집은 다시 차단" 케이스 추가.

### M2/M3/M4 묶음 — ✅ 해결 (2026-06-16, 한 설계로 묶음)
> brainstorm→spec(codex 8R PASS)→plan(codex 3R PASS)→Wave 병렬구현(commit-gate 묶음 ∥ spec-gate 묶음, 파일 disjoint라 순차 fallback)→통합 codex 코드리뷰 2R PASS + 보안 PASS. DoD `dod-2026-06-16-review-stamp-freshness.md`, spec/plan `docs/{specs,plans}/2026-06-16-review-stamp-freshness.md`. 미release(dev 누적).
> - **M2**: 커밋게이트가 보안 표식을 코드 표식과 비교 — `security_reviewed >= codex_reviewed` + cycle 일치(non-empty) + `verdict=PASS`. 보안 표식 content-rich 표준화(`reviewed=`/`cycle=`/`verdict=`) + 보안리뷰어 지침 갱신. light-tier 면제 시 비교 skip.
> - **M3**: 코드 표식 통과판정 dual-read(`verdict: PASS` 우선, 없으면 legacy `resolution: passed`) + 시각 파싱. escalated_to_human 경고→차단 격상. 파싱불가/판정실패 fail-closed.
> - **M4**: 생성기 3개 fail-open 경로(비캐시 python/JSON 파싱/캐시경로 python) 보수 마커 + pre-edit 차단 + 1회성 바이패스(consume-on-use). auto-heal 은 **canonical spec/plan 재처리 시에만**(통합리뷰 R1 High: 광범 해소가 비-spec 편집으로 fail-open 재개방 → 좁힘).
> - **통합리뷰 발견·해소**: R1 High(M4 auto-heal 광범) + R1 Medium(헬퍼 단위테스트 false-green → 행위 기반 교체, 그 과정에 비-ISO/verdict누락 fail-closed가 JSON deny로 차단됨을 실측). collateral: M3 새 계약이 `test-bash-guard-split.sh`/`test-exit2-stderr-tone.sh`의 빈-touch fixture를 정당 차단 → content-rich 동기화. 전 묶음 147/147 GREEN.
> - **INFO 후속(비차단, 위협모델 밖 — 백로그)**: ① M4 바이패스를 M1식 삭제증명(`[ ! -e ]`)으로 일관화 ② `post-edit-spec-review-gate.sh`의 cause/reason stderr echo sanitize(routing/incident 패턴과 동형, 심층방어).

#### (원본 발견 — M2)
- **증상**: 코드 리뷰가 새로 돌면 코드 리뷰 표시는 새로 쓰이고 대기 표시는 지워지지만 보안 리뷰 표시는 그대로 남아, 이전 사이클의 낡은 보안 통과로 새 코드가 커밋된다.
- **근거**: 커밋 게이트 `pre-bash-test-commit-gate.sh:154` 가 `.security-reviewed` **존재만** 확인. codex review wrapper 는 `.codex-reviewed` 만 새로 쓰고 `.review-pending` 만 삭제(`rein-codex-review.sh` PASS 경로) — `.security-reviewed` 미터치. 보안 리뷰어 지침 `security-reviewer.md:90` 은 아직 내용 없는 `touch`(이번 세션 보안리뷰어는 content-rich 로 썼으나 지침 자체가 미갱신 = 비일관).
- **인덱스 추적 항목 ⓐ의 진짜 정체**: "도장 내용 없음"보다 "새 사이클에 무효화 안 됨 + 게이트가 존재만 검사"가 핵심.
- **권고**: (a) 새 소스 편집 시 `.security-reviewed` 도 invalidation(또는 `.review-pending` 생성 시 함께 stale 처리), (b) 커밋 게이트에서 `security_review_time >= codex_review_time` + stamp content/cycle 바인딩 검사, (c) `security-reviewer.md` 지침을 content-rich(cycle/files/verdict) 도장으로 갱신.

## 🟡 Medium — 위협모델 내

### M3. 코드 리뷰 통과 표시 내용 미검증
- **근거**: 커밋 게이트 freshness 는 `.review-pending` mtime vs `.codex-reviewed` mtime 비교뿐(`pre-bash-test-commit-gate.sh:170,179`), 대기 표시 없으면 `.codex-reviewed` 존재만 요구(`:195`). 표시 내 `verdict`/`diff_base`/`active_dod`/`reviewed_at` 미검증.
- **반박(안전 확인)**: `.spec-reviews/<hash>.reviewed` 는 content-bound — `rein-mark-spec-reviewed.sh:117,120` 가 `content_sha` 기록 + `pre-edit-dod-gate.sh:609,626` 가 현재 파일 SHA 와 비교. 이 패턴을 코드/보안 도장에도 확장하면 M2/M3 동시 해소.
- **권고**: 최소 `verdict: PASS` + parse 가능한 `reviewed_at` + 현재 active DoD/cycle 일치 + stamp 가 현재 변경 이후임을 검사.

### M4. 스펙 리뷰 표시 생성기 fail-open
- **근거**: `post-edit-spec-review-gate.sh:32,36`(Python resolver 실패), `:56,58`(JSON parse 실패) 에서 보수적 마커 없이 조용히 `exit 0`. 차단 게이트의 producer 가 fail-open.
- **대조(의도된 패턴)**: 라우팅 훅 `post-edit-dod-routing-check.sh:30,39` 는 Python 실패 시 `.routing-missing-unknown-python-runtime` 보수 마커 생성.
- **권고**: 동일하게 producer 실패 시 보수적 pending 마커를 남겨 다음 편집에서 차단.

## 🟢 Low — 위협모델 밖(보류) 또는 영향 작음

- **A1**: 자동모드에서 `stop-session-gate.sh:109-115` 의 pending draft / `auto-*` advisory 가 여전히 출력(우리가 v1.5.4 에서 고친 것의 잔여 한 끗). 문서를 "반복 advisory + incident block 만 silent"로 더 좁히거나 이 두 줄도 `_auto_silent` 안으로. 영향 작음.
- **D1**: `.codex-reviewed` 가 temp+rename 없이 직접 `cat >` (비원자적). spec-reviewed 는 `mktemp`+`mv`. 경쟁 영역.
- **D2**: `blocks.jsonl` 다중 writer `>>` append 에 writer-side flock 없음(`pre-edit-dod-gate.sh:79`, `bash-guard-infra.sh:58`). incident 집계 정확도 리스크, 직접 게이트 bypass 아님.
- **E2**: degraded 주석(`degraded-check.sh:8` "Bash gates pass through")과 실제 동작 불일치 — dispatcher 는 degraded 에서도 safety guard + test/commit gate 계속 실행(`pre-bash-dispatcher.sh:200,205,210`). 주석 한 줄 정정감.

## 다음 세션 권장 진입 순서
1. ~~**M1**(`.skip-spec-gate` 소비)~~ — ✅ 완료 (2026-06-16).
2. ~~**M2+M3+M4 묶음**~~ — ✅ 완료 (2026-06-16). M1~M4 모두 dev 누적, **미release**.
3. ~~**세션 후속**(검증기 표 형식 재발부채 + M4 INFO 2건)~~ — ✅ 완료 (2026-06-16, DoD `dod-2026-06-16-session-followups.md`, codex+보안 PASS). 검증기가 번호 heading+백틱 ID 수용(silent-skip 재발 봉합), M4 바이패스 삭제증명 fail-closed + cause/reason echo sanitize.
4. **다음 진입점 후보**: (a) **릴리스 판단**(M1~M4 + 후속 묶어 patch/minor — hook/validator 동작변경=user-facing), (b) A1/E2 문서 한 줄 정정, (c) ⓑ CI 자동 테스트 0건, (d) routing 블록 reason echo 도 동일 sanitize 적용(이번 비범위). D1/D2 는 위협모델 재확인 후.


---
## 2026-06-16
# Daily Summary: 2026-06-16

---
<!-- source: 2026-06-16-plan-writer-review-stamp-freshness.md -->
## plan-writer-review-stamp-freshness

### 완료 기록

Plan complete: docs/plans/2026-06-16-review-stamp-freshness.md
Validator: exit 0 (plan), schedule exit 0 (Wave1: commit-gate-bundle ∥ spec-gate-bundle / Wave2: tests-commit-gate ∥ tests-spec-gate)
Coverage: 11/11 Scope IDs implemented, 0 deferred. .coverage-mismatch 마커 없음.

Spec-side normalization (validator 차단 해소):
  - docs/specs/2026-06-16-review-stamp-freshness.md heading `## 3. Scope Items` → `## Scope Items` (validator 정확매칭 요구)
  - Scope ID 셀 backtick 제거 (`| `M2-...`` → `| M2-...`) — SCOPE_ROW_RE 가 bare 토큰만 파싱
  - 비-의미 정규화. §3 교차참조는 "Scope Items 가 3번째 절" 위치로 여전히 유효 (heading 본문에 주석 추가).

Spec review: NEEDS-FIX (codex gpt-5.5 high, automated — 실행 성공, fallback 아님)
Stamp: 생성 안 됨 (.spec-reviews/4198fce6b89b435e.pending 유지)
.codex-reviewed: 미오염 확인 (직전 M1 cycle stamp 그대로, spec review 가 안 건드림)

잔존 findings (author/메인 세션 핸드오프 — self-fix loop 없음):
  [High] `## 실행 전략` 이 task 본문과 불일치 — edit_only 번들로 선언하면서 (a) 구현 번들 task 본문에 failing test 작성(TDD)을 두고 (b) per-task `Commit:` 스텝 포함. edit_only worker 는 커밋 불가 + reproduction-first 면 test 가 번들 안에 있어야 함. 해소안: 각 번들 scope 에 source+test 파일 함께 포함 + 별도 다운스트림 test task 제거, OR 명시적 test-first task 를 구현이 depends_on 하도록 모델링. + edit_only worker 의 per-task commit 스텝 제거(커밋은 부모 소유).
  [Medium] Task 1.4 의 grep/check 지시문이 `agents/security-reviewer.md` 로 적힘 — 정식 경로는 `plugins/rein-core/agents/security-reviewer.md`. Files 블록은 정확. check 지시문만 canonical 경로로 교정.

Next (수동 개입 경로):
  (1) 위 2건 plan 에 반영 (실행 전략 ↔ task 본문 정합, Task 1.4 경로 교정)
  (2) validator 재실행
  (3) 수동 /codex-review 호출 또는 plan-writer 재실행

---
<!-- source: 2026-06-16-session.md -->
## session

### 완료 기록

- 날짜: 2026-06-16
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
(오늘 커밋 없음)

## 변경된 파일 (worktree + staged, 미커밋 전체)
plugins/rein-core/hooks/pre-edit-dod-gate.sh
tests/hooks/test-spec-review-gate.sh
trail/daily/2026-06-04.md
trail/daily/2026-06-05.md
trail/daily/2026-06-08.md
trail/dod/.active-dod
trail/dod/.codex-reviewed
trail/dod/.security-reviewed
trail/dod/dod-2026-06-11-delete-stale-orphan-tests.md
trail/dod/dod-2026-06-11-envelope-subject-consistency.md
trail/dod/dod-2026-06-11-persona-injection-truncation-fix.md
trail/dod/dod-2026-06-12-gate-misfire-fixes.md
trail/dod/dod-2026-06-15-automode-doc-fix-metacheck-untracked.md
trail/inbox/2026-06-11-delete-stale-orphan-tests.md
trail/inbox/2026-06-11-envelope-subject-consistency.md
trail/inbox/2026-06-11-persona-injection-truncation-fix.md
trail/inbox/2026-06-12-gate-misfire-fixes-plan.md
trail/inbox/2026-06-12-gate-misfire-fixes.md
trail/inbox/2026-06-15-automode-doc-fix-metacheck-untracked.md
trail/inbox/2026-06-15-marker-audit-findings.md
trail/incidents/.last-aggregate-state.json
trail/incidents/.last-processed-line
trail/incidents/.session-start-line
trail/incidents/active-dod-cleanup.log
trail/incidents/blocks.jsonl
trail/index.md
trail/weekly/2026-W23.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.


---
## 2026-06-17
# Daily Summary: 2026-06-17

---
<!-- source: 2026-06-17-commit-gate-security-surface-exempt.md -->
## commit-gate-security-surface-exempt

### DoD

- 날짜: 2026-06-16
- 유형: feature
- Scope ID: SX-command-form-failclosed, SX-staged-diff-acquire, SX-allowlist-docs-trail, SX-allowlist-version-line, SX-skip-p6-m2-when-all-exempt, SX-nonexempt-still-required, SX-coexist-light-tier, SX-audit-log, SX-regression-tests
- 출처: M2 릴리스 마찰 근본 해결 (brainstorm→spec→plan, codex 리뷰 PASS)

## 문제 (Symptom)

커밋 게이트의 보안 검토 요구(보안 표식 존재 + 신선도 비교)가 보안 surface 0 인 변경(릴리스=버전 문자열+문서)에도 보안 재검토를 강제한다(v1.5.5 릴리스 커밋이 실제 차단됨).

## 범위

`check_review_stamp()` 에 보안-surface 면제 분기 추가: 단독 `git commit`(staging 동반 없음 + 옵션 allowlist) + staged diff 전부 허용목록(문서/`trail`/버전 문자열-only)이면 보안 표식 존재(P6)+신선도(M2) 비교를 둘 다 skip. 코드 리뷰(P5)/내용검증(M3)/coverage 불변. light-tier 면제와 OR 병존. **명시 면제 외 전부 보안 관련 = fail-closed**(M2 구멍 재개방 금지). 위협모델 "정직한 에이전트 규율".

### 명시적 비범위
- content 바인딩(보안표식 baseline 기록), 적대적 우회 하드닝(버전 라인 로직숨김 등), 버전 파일 외 config 면제 확장. (spec §7)

## 변경 파일

- plugins/rein-core/hooks/pre-bash-test-commit-gate.sh
- tests/hooks/test-pre-bash-test-commit-gate.sh

## 검증 기준

- [ ] reproduction-first TDD: 각 면제/비면제/fail-closed 케이스 RED→GREEN
- [ ] 면제 성립: docs-only / 버전라인-only / docs+버전 혼합 / trail 표식 → 통과
- [ ] fail-closed: `git add && git commit` 한줄(TOCTOU) / `-a`/`--amend`/path / 여러단어 메시지 뒤 진짜 pathspec / `git diff --cached` 실패 / plugin.json 다른키·HEAD부재 / 버전파일 버전外 라인 / hook→docs rename → 보안 요구
- [ ] 불변: P5 코드표식 부재는 면제와 무관하게 차단. light-tier OR 병존.
- [ ] `bash tests/hooks/test-pre-bash-test-commit-gate.sh` + `test-security-tier-gate.sh` 전량 GREEN, `bash -n` 통과
- [ ] 통합 codex 리뷰 + 보안 리뷰 통과

## 범위 연결

plan ref: docs/plans/2026-06-16-commit-gate-security-surface-exempt.md
covers: [SX-command-form-failclosed, SX-staged-diff-acquire, SX-allowlist-docs-trail, SX-allowlist-version-line, SX-skip-p6-m2-when-all-exempt, SX-nonexempt-still-required, SX-coexist-light-tier, SX-audit-log, SX-regression-tests]

## 라우팅 추천

```yaml
agent: feature-builder
skills: []
mcps: []
rationale: >
  단일 파일 본체(pre-bash-test-commit-gate.sh) + 그 test 의 reproduction-first 신규 기능. 병렬 여지 없음(단일 파일).
  보안 게이트 변경이라 security_tier 표준(보안 리뷰 필수). 부모가 통합 codex/security 리뷰 + 커밋 소유.
approved_by_user: true
```

### 완료 기록

## 한 일

M2 릴리스 마찰 근본 해결: 커밋 게이트가 보안 surface 0 인 변경(릴리스=버전문자열+문서)에도
보안 재검토를 강제하던 문제를 게이트 자동 면제(허용목록)로 해결.

- **brainstorm → spec → plan** (codex 설계/플랜 리뷰 PASS + per-spec stamp)
- **구현**: `pre-bash-test-commit-gate.sh` `check_review_stamp()` 에 보안-surface 면제 분기.
  - 면제 대상: staged diff 가 전부 `*.md`/`docs/**`/`trail/**` 또는 버전문자열-only
    (`scripts/rein.sh` VERSION 라인 정규식 / `plugin.json` top-level version semantic JSON 비교).
  - 명령형태 = **단일-clause 종착 규칙**: 전경 단독 `git commit <allowlist-opts>` 한 clause 만 면제.
    multi-clause(cd/command/env/true/git add 등 무엇이든 동반)/wrapper/env-prefix/글로벌옵션/
    쉘평가·서브셸(`$(`·백틱·`${`·`<(`)/백그라운드 `&` → 전부 fail-closed.
  - 명시 허용목록 외 전부 보안 관련 = fail-closed (M2 구멍 재개방 금지). light-tier 면제와 OR 병존.
    면제 발동 시 `trail/incidents/security-surface-exempt.log` 에 audit 1줄(비차단).
- **통합 codex 코드 리뷰 4R**: R2 가 "비-git clause skip" 우회(`command git add; git commit` 등) 발견 →
  clause 를 세서 1개 초과면 거부로 봉합. R3 가 백그라운드 `&` fail-open 발견 → `&` 토큰 실격으로 봉합. R4 PASS.
- **보안 리뷰 PASS**: 카나리 테스트로 파서가 입력을 실행하지 않음 실증 + fail-closed/TOCTOU/M2-구멍 전수 확인.
- **테스트**: `test-pre-bash-test-commit-gate.sh` 104/104 + `test-security-tier-gate.sh` 15/15 GREEN.
- **커밋**: dev `d0b9a3e`(feat) + `bc6500c`(trail 회전). **릴리스 안 함**(사용자 미요청).

## 발견 (중요) — 활성 게이트는 워킹트리 훅 + 면제는 단독 커밋에만 (자가 오진 정정)

실제 `git commit` 으로 면제 dogfood 했더니 audit 미기록 → **1차 오진**: "활성=캐시 1.5.4(면제·M2 부재)".
**정정**: 이후 버전범프 커밋이 M2(`SECURITY_REVIEW_STALE`)로 차단됨 → M2 보유 사본은 워킹트리뿐 →
**활성 훅 = 워킹트리**(M2·면제 라이브). 실커밋이 면제 안 된 진짜 원인은 캐시가 아니라 **명령형태**:
dogfood 커밋을 늘 `cd …; git commit`(다중 clause)로 돌려 단일-clause 규칙이 정확히 거부 → M2 로 떨어짐.
`cd` 없이 cwd=루트에서 **단독 `git commit`** 로 돌리니 면제 정상 발동 — v1.5.6 릴리스 버전범프 커밋
`fac2000` 이 audit `staged_files=5 docs=3 version=2` 로 실증(보안 재검토 없이 통과 = 기능의 실제 용도 dogfood 성공).
교훈: 캐시 디렉토리 보인다고 활성이라 단정 말 것 — 게이트 reason-code(행위)로 확정.
[[reference_plugin_directory_marketplace_lifecycle]] 정정 반영함.

## 다음

- **v1.5.6 릴리스 진행 중**: dev 완료(`fac2000` 버전범프, 면제로 통과). main 선별 체크아웃 → tag → push(publish+mirror) → public strip/parity 검증 남음.
- 후속: audit 로그 churn(면제 커밋마다 `security-surface-exempt.log` append → tree dirty) gitignore 검토.
- 미해결 incident 전부 종결됨(검증 부산물 3건 declined: 리뷰후재수정/파이프쉘/보안리뷰-stale).

---
<!-- source: 2026-06-17-session.md -->
## session

### 완료 기록

- 날짜: 2026-06-17
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
bc6500c chore(trail): 세션 운영 기록 회전 — DoD 아카이브 + 일간/주간 + incident/meta-check
d0b9a3e feat(commit-gate): 보안 surface 없는 변경 자동 면제 — 단일-clause 종착 규칙 + 백그라운드 차단

## 변경된 파일 (worktree + staged, 미커밋 전체)
trail/inbox/2026-06-17-meta-check.jsonl
trail/index.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.


---
## 2026-06-18
# Daily Summary: 2026-06-18

---
<!-- source: 2026-06-18-skill-invocation-plugin-root.md -->
## skill-invocation-plugin-root

### DoD

- 날짜: 2026-06-18
- 유형: fix (docs/guidance — 사용자 ship 표면)
- Scope ID: SI-codex-review-skill, SI-code-reviewer-skill, SI-subagent-review-rule, SI-spec-writer-agent, SI-plan-writer-agent
- 출처: 본 세션 진단 — codex-review wrapper 호출이 bare `scripts/...` 상대경로라 repo 루트 복제본(메인테이너 dogfood 에만 존재)으로 떨어짐. 일반 사용자 repo 엔 루트 `scripts/` 부재라 글자 그대로면 깨짐. 같은 패턴이 5개 ship 문서에 분산.

## 문제 (Symptom)

ship 되는 스킬/규칙/에이전트 문서가 helper 스크립트를 `bash scripts/rein-*.sh` (맨앞 경로 없는 상대경로) 로 호출하라고 지시한다. 작업 디렉토리가 repo 루트일 때만 동작하며, 메인테이너 repo 의 루트 `scripts/` 복제본과 플러그인 정본(`${CLAUDE_PLUGIN_ROOT}/scripts/`) 사이에 드리프트가 생기면 낡은 wrapper 로 리뷰가 돌 수 있다. 사용자 repo 엔 루트 `scripts/` 가 없어 글자 그대로 실행 시 실패.

## 범위

5개 문서의 모든 `bash scripts/rein-*.sh` 호출형을 `bash "${CLAUDE_PLUGIN_ROOT:-$PWD}/scripts/rein-*.sh"` 로 정정한다. 플러그인 경로 우선, 변수 미설정 시 `$PWD` 폴백 — 문서화된 resolver 우선순위(플러그인 우선, repo 폴백)와 일치하며 메인테이너 dogfood 도 안 깨진다.

대상 호출 스크립트: `rein-codex-review.sh`, `rein-mark-spec-reviewed.sh`.

### 명시적 비범위
- repo 루트 `scripts/` 복제본 자체의 제거/동기화 장치 (별도 결정 — 본 작업은 호출 표기만 고정).
- 호출이 아닌 순수 서술 프롬 언급(`bash` 접두 없는 경로 멘션)은 손대지 않는다.
- 훅 코드의 resolver(`plugin-script-path.sh`) 변경 없음 — 이미 올바른 우선순위.

## 변경 파일

- plugins/rein-core/skills/codex-review/SKILL.md
- plugins/rein-core/skills/code-reviewer/SKILL.md
- plugins/rein-core/rules/subagent-review.md
- plugins/rein-core/agents/spec-writer.md
- plugins/rein-core/agents/plan-writer.md

## 검증 기준

- [ ] 위 5개 파일에서 `bash scripts/rein-` (bare) 잔존 0건
- [ ] 모든 정정 호출이 `bash "${CLAUDE_PLUGIN_ROOT:-$PWD}/scripts/rein-...` 형태
- [ ] `bash` 접두 없는 순수 서술 멘션은 불변
- [ ] 다른 ship 문서에 같은 bare 패턴 신규 유입 0건 (전수 grep)
- [ ] codex 리뷰 통과 (문서 변경이지만 호출 동작 가이던스라 게이트 경로 준수)

## 라우팅 추천

```yaml
agent: docs-writer
skills: []
mcps: []
rationale: >
  ship 문서 5개의 호출 경로 문자열 surgical 정정 (코드 로직 불변). 파일 disjoint 라 병렬 여지 있으나
  변경이 동일 치환 패턴이고 전체 부피가 작아 부모(메인 세션)가 직접 일괄 편집 + 전수 검증이 더 빠르고 안전.
  보안 surface 없음(경로 문자열 가이던스). 부모가 codex 리뷰 + 기록 소유.
approved_by_user: true
```

### 완료 기록

## 배경 (진단)

codex 코드 리뷰 시 wrapper(`rein-codex-review.sh`) 호출이 맨앞 경로 없는 상대경로 `bash scripts/rein-codex-review.sh` 라, 작업 디렉토리(repo 루트) 기준으로 **repo 루트 복제본**에 떨어졌다. repo 루트 `scripts/` 는 메인테이너 dogfood 에만 존재하는 fallback 복제본이고 플러그인 정본은 `${CLAUDE_PLUGIN_ROOT}/scripts/`(설치 캐시 = 활성 버전). 둘이 드리프트하면 낡은 wrapper 로 리뷰가 돌 위험 + 일반 사용자 repo 엔 루트 `scripts/` 부재라 글자 그대로 실행 시 실패. 같은 bare 패턴이 ship 문서 5개에 분산돼 있어 버그 클래스로 판정.

## 한 일

- **DoD**: `dod-2026-06-18-skill-invocation-plugin-root.md` (유형 fix, 사용자 승인).
- **수정**: ship 문서 5개의 모든 `bash scripts/rein-*.sh` 호출형(12곳)을 `bash "${CLAUDE_PLUGIN_ROOT:-$PWD}/scripts/rein-*.sh"` 로 정정. 플러그인 경로 우선 + 변수 미설정 시 `$PWD` 폴백 → 문서화된 resolver 우선순위(플러그인 우선, repo 폴백)와 일치, 메인테이너 dogfood 도 안 깨짐.
  - `skills/codex-review/SKILL.md` (5), `skills/code-reviewer/SKILL.md` (2), `rules/subagent-review.md` (2), `agents/spec-writer.md` (1), `agents/plan-writer.md` (2).
  - 대상 호출 스크립트: `rein-codex-review.sh`, `rein-mark-spec-reviewed.sh`.
- **검증(grep)**: 5개 파일 bare 잔존 0건, robust 형태 12건 일치, `bash` 접두 없는 순수 서술 멘션은 불변.

## 결정 (사용자)

- 범위 = 같은 패턴 5개 파일 전부 (codex-review 단독 아님).
- 경로 형태 = 안전형 폴백 `${CLAUDE_PLUGIN_ROOT:-$PWD}`.
- helper 스크립트 2개 자체 `# Usage:` 주석(6줄, bare 잔존)은 **그대로 둠** — 에이전트 실행 지시문이 아닌 내부 설명 주석이고 .sh 편집은 코드+보안 리뷰 게이트를 동작 변화 없이 유발하므로 비범위.

## 상태 / 다음

- **dev 워킹트리에서 편집·검증 완료, 미커밋**. `.review-pending` 미생성(.md 변경이라 게이트 자동 플래그 안 함). 기존 `.codex-reviewed` 는 직전 작업(6/17)분으로 stale.
- 커밋 전 다음 단계: codex 코드 리뷰(ship 변경) → dev 커밋. 릴리스 타이밍·배치 여부는 보스 결정 대기(dev 누적 원칙 + 하루 1머지 Rule B).
- 비범위 후속: repo 루트 `scripts/` 복제본 자체의 동기화 강제 장치(드리프트 근본 차단)는 별도 결정.


