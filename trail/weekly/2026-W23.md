# Weekly Summary: 2026-W23

---
## 2026-06-01
# Daily Summary: 2026-06-01

---
<!-- source: 2026-06-01-claude-overlay-cleanup.md -->
## claude-overlay-cleanup

### DoD

- 날짜: 2026-06-01
- 유형: refactor (메인테이너 dev 오버레이 슬림화 + stale 참조 정리, 기능 변경 없음)
- plan ref: N/A (정비 작업 — 어떤 훅도 plan ref 강제 안 함). 근거: 2026-06-01 codex-ask second opinion (`.claude/` 오버레이 삭제/슬림 판단, gpt-5.5/medium, read-only)

## 목표 (Why)

Option C Phase 3(2026-05-13)에서 plugin-first 로 전환된 뒤, `.claude/` 오버레이의 일부 파일이 plugin SSOT(`plugins/rein-core/rules/operating-sequence.md`, `routing-procedure.md`, `routing-map.md`)와 **내용이 중복**되거나 **폐기된 경로(`.claude/agents/`, `.claude/hooks/`)를 참조**하는 stale 상태로 남았다. `.claude/CLAUDE.md`는 매 세션 자동 로드되며 `@.claude/orchestrator.md`를 import 해 11단계 시퀀스·라우팅을 **이중 로드** → 에이전트가 "어느 게 권위본인가" 혼란. 혼란의 실제 원인은 파일 존재가 아니라 **자동 로드/import 되는 중복 본문 + stale 내용**(codex 진단).

목표: 메인테이너 전용 규칙(branch/versioning/readme/legacy)의 세션 자동 로드는 보존하면서, 중복·stale 오버레이를 제거하고 거버넌스 표면(AGENTS.md, govcheck)의 끊긴 참조를 정리한다.

## 성공 기준 (Acceptance)

### 1. `.claude/CLAUDE.md` 슬림화 (slim, not delete)
- 11단계 강제 시퀀스 본문 + trail 운영 규칙 중복 서술 + `@.claude/orchestrator.md` import 제거.
- "운영 시퀀스/라우팅의 권위본은 plugin 규칙(operating-sequence / routing-procedure / routing-map)이며 본 파일은 메인테이너 전용 규칙만 추가 로드한다"는 취지 명시.
- 메인테이너 전용 규칙 import 보존: branch-strategy / readme-style / versioning (현행 3종 유지 — 동작 불변. legacy-shipped-pending 은 현재도 미import 였으므로 그대로 유지).

### 2. 중복·stale 오버레이 삭제 (git rm — git history 가 아카이브)
- `.claude/orchestrator.md` (라우팅 = plugin routing-procedure/map 가 SSOT, 폐기된 `.claude/agents/...draft` 참조 205줄).
- `.claude/workflows/*.md` 5종 (add-feature/fix-bug/build-from-scratch/research-task/design-to-plan — 핵심은 plugin agents/rules/skills + design-plan-coverage 로 이관됨).
- `.claude/registry/agents.yml` (폐기된 `.claude/agents/*` 가리킴, 런타임 reader 없음 — repo-audit 체크리스트 한 줄은 advisory).
- `.claude/plans/*.md` 2종 (hook-enforcement / -revisions — 과거 설계 로그, 자동 로드 안 됨, git history 보존).

### 3. 거버넌스 표면 끊긴 참조 정리 (main 포함 파일 — 내부 문서/툴링 수정)
- `AGENTS.md:81` — `.claude/workflows/design-to-plan.md` 참조 → plugin 의 design-plan-coverage 규칙 + writing-plans 스킬로 재지정.
- `AGENTS.md:232-235` — `.claude/agents/*.md` (이미 부재, dangling) → `plugins/rein-core/agents/*.md` 로 경로 수정.
- `scripts/rein-govcheck.py` — `ROOT_DOCS` (48줄) + docstring(11줄)에서 `.claude/orchestrator.md` 제거 (삭제 파일이므로). `if p.is_file()` 가드 덕에 런타임 미차단이나 죽은 config 제거.

### 4. 검증
- `python3 scripts/rein-govcheck.py` → exit 0 유지(baseline 0).
- 삭제 후 활성 거버넌스 표면(AGENTS.md, .claude/CLAUDE.md)에 끊긴 `.claude/orchestrator|workflows|registry|plans|agents` 참조 0건.
- codex 리뷰 PASS + 보안 리뷰 PASS (commit gate).

## 변경 파일

- `.claude/CLAUDE.md` (슬림화)
- `.claude/rules/branch-strategy.md` (제외 표 orchestrator/workflows/registry 행 갱신 + 머지 예제 `.claude/hooks/` → plugin — codex round 1·3 반영. CLAUDE.md import 하는 활성 surface)
- `.claude/rules/versioning.md` (internal hook helper 예시 경로 `.claude/hooks/lib/**` → `plugins/rein-core/hooks/lib/**`)
- `.claude/rules/legacy-shipped-pending.md` (관련 파일 절 healer + 2 hook 경로 → plugin)
- `AGENTS.md` (에이전트 표 경로 + design-to-plan 참조 + code-reviewer skill 참조 → plugin/skill name. main 포함)
- `scripts/rein-govcheck.py` (ROOT_DOCS + docstring orchestrator 제거 + HOOK_GLOB → plugin 훅 스캔 + plugin 상대경로 dual-path 해석. main 포함)
- `plugins/rein-core/rules/design-plan-coverage.md` (`.claude/hooks/post-edit-plan-coverage.sh` → bare hook name. **사용자 ship plugin 규칙** — codex round 3 High 반영, 사용자 범위확장 승인)
- `plugins/rein-core/rules/answer-only-mode.md` (`.claude/skills/...` → `${CLAUDE_PLUGIN_ROOT}/skills/...`. **사용자 ship plugin 규칙**)
- 삭제: `.claude/orchestrator.md`, `.claude/workflows/{add-feature,fix-bug,build-from-scratch,research-task,design-to-plan}.md`, `.claude/registry/agents.yml`, `.claude/plans/{hook-enforcement,hook-enforcement-revisions}.md`

## 제외 (Out of scope)

- ~~`scripts/rein-govcheck.py` 의 `HOOK_GLOB` 미변경~~ → **범위 포함으로 전환** (사용자 승인 2026-06-01 "전부 이번에 정리"). HOOK_GLOB 을 plugin 훅(`plugins/rein-core/hooks/*.sh`)으로 재지정하고, false-fail 우려는 `validate_ref` 에 repo-root → `plugins/rein-core/` dual-path 해석을 추가해 해소. govcheck 가 이제 plugin 훅 30개를 실제로 스캔(top-level 8 훅이 script 참조) + exit 0.
- 루트 임시 노트(`need-to-confirm.md` 등)의 본 정비 언급 줄 갱신 — 별 작업(앞 codex-ask 에서 별도 다룸).
- trail/CHANGELOG 등 과거 기록의 `.claude/orchestrator|agents` 언급 — 히스토리이므로 불변.
- main 병합 — 진행 중인 병렬 실행 시리즈와 함께 별도 판정(본 정비는 내부 doc/tooling = no bump).

## 리스크

- (R1) CLAUDE.md 슬림화로 메인테이너 규칙 자동 로드가 끊길 위험 → mitigation: branch/readme/versioning import 3줄을 명시 보존(현행 동작과 동일). 슬림 후에도 세션 시작 시 3규칙 로드 확인.
- (R2) orchestrator/workflows 삭제로 govcheck/AGENTS 참조 dangling → mitigation: 본 DoD 3번에서 동시 정리. 삭제 후 govcheck exit 0 + grep 0건 검증.
- (R3) `.claude/CLAUDE.md`·govcheck·AGENTS 는 source 로 분류되어 DoD 게이트 대상 → 본 DoD 가 충족. 삭제는 `git rm`(Edit/Write 아님)이나 정합 위해 동일 DoD 범위.

## 라우팅 추천

```yaml
agent: rein:feature-builder-refactor   # 기능 변경 없는 구조 개선(오버레이 슬림 + 참조 정리) — refactor 전담
skills:
  - rein:codex-review                  # commit gate 필수 (Mode A — stamp)
mcps: []
rationale: >
  DoD 키워드 = refactor / 오버레이 정리 / stale 참조 → feature-builder-refactor 매치.
  본 세션이 이미 codex-ask 로 대상·리스크·참조처를 전수 검증 완료(govcheck if-guard,
  HOOK_GLOB false-fail, AGENTS dangling, 삭제 4종 main 미포함)했으므로 메인 세션 인라인
  실행이 효율적 — 단일 coherent 정비(슬림 1 + 삭제 4 + 참조수정 2). codex-review 는
  commit 게이트라 필수. security_tier=light — 마크다운/문서 + govcheck config 1줄 제거,
  실행 경로·권한·입력 경계 변화 없음.
security_tier: light
approved_by_user: true   # 사용자 "오토모드 키고 바로 정리 들어가" (2026-06-01) 로 승인
```

### 완료 기록

- 날짜: 2026-06-01
- 유형: refactor
- 변경 파일:
  - 슬림: `.claude/CLAUDE.md` (11단계 시퀀스·orchestrator import 제거, "권위본=plugin 규칙" + 메인테이너 규칙 3종 import만 보존)
  - 삭제(git rm, dev 전용·git history 보존): `.claude/orchestrator.md`, `.claude/workflows/*.md` (5), `.claude/registry/agents.yml`, `.claude/plans/*.md` (2)
  - 참조 정리: `.claude/rules/{branch-strategy,versioning,legacy-shipped-pending}.md`, `AGENTS.md`(에이전트 표 + skill/workflow 참조 → plugin), `scripts/rein-govcheck.py`
  - plugin 규칙(사용자 ship, 사용자 범위확장 승인): `plugins/rein-core/rules/{design-plan-coverage,answer-only-mode}.md`
- 요약: codex-ask second opinion(gpt-5.5/medium)으로 판단 → dev 오버레이가 plugin SSOT와 중복·stale 참조로 세션 시작 시 혼란 유발. CLAUDE.md 슬림화 + 중복/stale 9파일 삭제 + 활성·규칙·plugin 표면의 폐기 경로(`.claude/hooks|skills|agents|orchestrator|workflows|registry`) 참조 전수 정리. govcheck는 ROOT_DOCS에서 orchestrator 제거 + HOOK_GLOB을 plugin 훅으로 재지정(plugin 상대경로 dual-path 해석 추가, 훅 30개 실제 스캔, exit 0).
- 리뷰: codex Round 4 PASS(R1·R3 NEEDS-FIX를 통해 branch-strategy 제외표·AGENTS skill 참조·plugin 규칙 2건·govcheck HOOK_GLOB까지 점진 발견·해소). 보안 light-tier PASS(govcheck dual-path 적대적 입력 8종 no-match로 traversal 불가 실증, 삭제 파일 시크릿 없음, 게이트 약화 없음).
- 검증: 전 표면 live stale 오버레이 참조 0건, govcheck exit 0, CLAUDE.md 메인테이너 규칙 3종 import 보존.
- 미진행: 로컬 커밋만(자동모드). push·main 병합은 진행 중 병렬 실행 시리즈와 함께 별도 승인 대기. 범위/등급: dev 오버레이=릴리스 영향 0, AGENTS.md·govcheck=내부 doc/tooling, plugin 규칙 2건=경로참조 정정(patch급) — 시리즈 종결 시 일괄 bump 판정.

---
<!-- source: 2026-06-01-phase2-parallel-execute-skill.md -->
## phase2-parallel-execute-skill

### DoD

- 날짜: 2026-06-01
- 유형: feat (병렬 실행 재설계 Phase 2 — 같은-트리 웨이브 병렬 실행 스킬)
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 2 (Task 2.1/2.2/2.3)

## 목표 (Why)

Phase 1 에서 v2 스키마(`tasks[]` + `depends_on` + `mode` + `scope`)와 검증기(위상정렬·사이클·동시쌍 disjoint fail-closed + `schedule` emitter)를 확정했다. Phase 2 는 그 위에 **실제 실행 계층**을 올린다 — 활성 plan 의 `## 실행 전략` 을 읽어 의존 위상정렬로 웨이브를 유도하고, 각 웨이브의 독립 `edit_only` 태스크를 **같은 작업 트리에서** 서브에이전트로 병렬 실행, `mutating`·의존 태스크는 순차 실행하며, 부모(메인 세션)가 웨이브 단위로 검증·테스트·커밋하는 흐름을 Rein 자체 스킬(`parallel-execute`)로 제공한다.

격리는 강제된 sandbox 가 아니라 **(1) 워커 프롬프트의 best-effort 규율 + (2) 부모의 사후 델타 검증** 두 겹이다. 안전성의 1차 보증은 write-set 분리(검증기가 동시쌍 disjoint 보장), 2차는 부모의 baseline-delta 검증이다.

## 성공 기준 (Acceptance)

### Task 2.1 — 스킬 골격 + 웨이브 유도 + 결정적 스케줄러
covers: EXEC-SKILL-WAVE-DETERMINISTIC-SCHEDULER-MUTATING-SOLO

1. **실패 테스트 먼저 (TDD RED)** — `tests/skills/test-parallel-execute-skill.sh` 신설: 파일 존재, frontmatter `name: parallel-execute` + `description:`, 본문 키워드 grep("위상정렬"/"ready"/"mutating"/"단독"/"병렬 dispatch"), 본문 ≤ 6144 byte(`wc -c`). RED 확인.
2. `plugins/rein-core/skills/parallel-execute/SKILL.md` 신설 — frontmatter + `## 목적` + `## 사용 시점` + `## Preflight`(활성 plan `## 실행 전략` 파싱, 검증기가 이미 fail-closed 검증함을 전제, 섹션 부재 → 순차 안내) + `## 웨이브 유도 + 결정적 스케줄러` + `## 호스트 능력 fallback`.
3. **canonical 순서 SSOT** — 스케줄러 섹션은 `python3 scripts/rein-validate-coverage-matrix.py schedule <plan>`(Phase 1 emitter, 출력 `step <n>: <id>...`)를 결정적 웨이브 순서의 SSOT 로 소비하도록 명시. helper 미가용 시 같은 규칙(ready→mutating-solo-then-edit_only) 복제.
4. GREEN — `bash tests/skills/test-parallel-execute-skill.sh` 통과(Task 2.1 키워드 한정).

### Task 2.2 — 워커 dispatch 계약 (edit_only + mutating) + 구조화 결과 스키마
covers: EXEC-SKILL-WORKER-EDIT-ONLY-CONTRACT-RESULT-SCHEMA, EXEC-SKILL-WAVE-DETERMINISTIC-SCHEDULER-MUTATING-SOLO

5. **실패 단언 추가 (RED)** — 공통 결과 스키마 키("task_id"/"status"/"changed_files"/"blocked_reason"/"recommendation") + edit_only("커밋 금지"/"stamp 금지") + mutating("변경성 명령 허용"/"단독 웨이브"/"예상 부작용 경로") grep 추가. RED 확인.
6. SKILL.md `## 워커 dispatch 계약` — 공통 결과 스키마(두 변형 동일, 최종 메시지 반환 기반, 워크트리 마커/결과 파일 없음): `task_id`(필수)/`status: completed|blocked`(필수)/`changed_files`(필수, advisory)/`blocked_reason`(blocked 시)/`recommendation: parent_fallback|split|scope_expand`(blocked 시)/`summary`. 부모는 `status=blocked` 또는 결과 누락(timeout/truncation)을 미완 처리 → 의존 후속 다음 웨이브 진입 불가.
7. **edit_only 변형** — 선언 `scope` 파일만 편집(best-effort). 금지: 커밋·스테이징·리뷰/보안 stamp·trail·전체 포매터·변경성 테스트/코드젠. 같은 웨이브 여러 edit_only 한 메시지 병렬 dispatch.
8. **mutating 변형** — 자기 자신만의 단독 웨이브. edit_only 금지목록 중 변경성 명령만 허용(코드젠·변경성 테스트·설치). 커밋·스테이징·stamp·trail 은 여전히 부모 소유 금지. 동일 결과 스키마 반환. 검증 기준 `scope` = 선언 + 예상 부작용 경로.
9. 본문 ≤ 6144 byte 재확인. 초과 시 schema 상세를 `exec-strategy-schema.md` 로 위임. GREEN.

### Task 2.3 — 부모 통합 (클린 시작 + 델타 검증 + 웨이브당 1커밋)
covers: EXEC-SKILL-PARENT-CLEAN-START-DELTA-VALIDATION-WAVE-COMMIT

10. **실패 단언 추가 (RED)** — "클린"/"porcelain"/"델타"/"부분집합"/"reject"/"웨이브당 1커밋" + `git status --porcelain` + `git ls-files --others --exclude-standard` grep 추가. RED 확인.
11. SKILL.md `## 부모 통합 (barrier)` — 각 웨이브 클린 트리 시작(직전 웨이브 커밋 보장, 첫 웨이브는 세션 현재 커밋 상태). 웨이브 경계 dirty → 중단·보고. 웨이브 완료 후 시작 이후 델타 산출(`git status --porcelain=v1 -z -uall --ignored=no` 또는 `git diff --name-only HEAD` + `git ls-files --others --exclude-standard`) → repo-relative literal 파일 경로 정규화. 델타 ⊆ 웨이브 `scope`(mutating 은 +부작용) 합집합 검증. 위반 → reject + 보고(커밋 안 함). 통과 → 웨이브 단위 1회 포맷/린트/테스트/리뷰 → 그 델타만 1커밋 → 다음(다시 클린 시작).
12. **(보안 L1 이월, 필수)** — 부모가 `scope` 경로를 델타 검증에 소비할 때 `..` 정규화(path traversal 차단) + 프로젝트 루트 containment 검증(루트 밖 경로는 reject). 이를 부모 통합 섹션에 명시.
13. 사용자 보고 방식 섹션(내부 식별자 평문 번역) 추가. 본문 ≤ 6144 byte 최종 확인. GREEN.

### 공통

14. 회귀 무영향 — 기존 skills 테스트 스위트(`tests/skills/run-all.sh`) 무회귀 + 신규 테스트를 run-all 에 등록.
15. codex 리뷰 PASS + 리뷰 stamp, security 리뷰 PASS + security stamp (commit gate).

## 변경 파일

- `plugins/rein-core/skills/parallel-execute/SKILL.md` (신설)
- `tests/skills/test-parallel-execute-skill.sh` (신설)
- `tests/skills/run-all.sh` (신규 테스트 등록)

## 제외 (Out of scope)

- Phase 3 (plan-writer v2 / PLN-1 게이트 제거 / 워크트리 기계 폐기) — 별 Task. 본 Phase 는 스킬 신설만.
- Phase 4 회귀 스위트 통합 (validator+스케줄러+부모 end-to-end) — Phase 4.
- 검증기/스케줄러 emitter 자체 변경 — Phase 1 에서 확정. 본 Phase 는 그 출력을 **소비**만.
- `feature-builder-worker` 에이전트 재작성 — Phase 3 Task 3.3. 본 Phase 의 워커 dispatch 는 인라인 프롬프트 또는 Task 3.3 후 재작성된 에이전트 지정(둘 다 계약 키워드 동일)으로 기술.
- 워크트리 격리 / mutating 병렬 / glob scope / cross-plan / 워커별 독립 커밋·stamp / 자동 롤백 (spec Non-goals 전부).

## 리스크

- (R1) 스킬 본문 6 KB NFR 초과 — 3 계약(스케줄러·워커·부모)을 한 파일에 채우면 예산 압박. mitigation: schema 상세는 `exec-strategy-schema.md` 참조 위임, 본문은 절차·키워드 중심 압축. 매 Task 끝에 `wc -c` 확인.
- (R2) 스킬은 마크다운 지시문이라 런타임 강제 불가 — 격리는 best-effort. mitigation: 안전성 1차는 검증기의 동시쌍 disjoint(Phase 1 기계 보증), 2차는 부모 델타 검증(기계가독 git porcelain). 스킬은 이 두 겹을 명문화.
- (R3) 델타 검증 경로 정규화 누락 시 path traversal — `..` 또는 루트 밖 scope 가 검증을 우회. mitigation: Acceptance 12 (L1 이월) 로 `..` normalize + containment 명시.
- (R4) 테스트가 키워드 grep 위주라 실제 동작 미검증 — 스킬은 지시문이라 단위 실행 불가. mitigation: grep 단언은 계약 presence 보증, 실제 동작 검증은 Phase 4 end-to-end + validator/스케줄러 단위(Phase 1)가 담당. 본 Phase 테스트는 구조·계약 presence 로 한정(정직).

## 라우팅 추천

```yaml
agent: rein:feature-builder        # Phase 2 = 신규 스킬 파일 + 테스트 신설 (add-feature). 버그·리팩토링 아님
skills:
  - rein:codex-review              # commit gate 필수 (리뷰 stamp)
mcps: []
rationale: >
  parallel-execute 스킬 신설은 신규 기능 추가(SKILL.md + 테스트 파일 생성)이므로
  feature-builder 가 적합. TDD red-green (각 Task 실패 테스트 먼저 → GREEN). 산출물이
  마크다운 지시문 + bash grep 테스트라 외부 의존 없음(MCP 불필요). Phase 1 의 schedule
  emitter 출력을 소비하는 계약을 정확히 기술하는 것이 핵심. security_tier=normal —
  스킬이 git delta·scope 경로를 다루므로 path traversal(L1 이월)을 acceptance 에 포함.
security_tier: normal
approved_by_user: true             # 사용자 "이대로 진행" — feature-builder + TDD + codex/security 리뷰 승인 (2026-06-01)
```

## Self-review 예정 항목 (AGENTS.md §6)

- 스케줄러 섹션이 Phase 1 `schedule` emitter 출력(`step <n>: <id>...`)을 SSOT 로 정확히 소비하는가 (규칙 재정의 아닌 위임)
- mutating 단독 웨이브 / edit_only 병렬 dispatch 의 결정성이 spec 과 일치하는가 (plan 순서 가장 앞선 mutating 1개)
- 공통 결과 스키마 6 키가 spec EXEC-SKILL-WORKER 행과 정확히 일치하는가
- 부모 델타 검증이 기계가독 git porcelain + repo-relative literal 정규화 + `..`/containment(L1)를 모두 명문화하는가
- 본문 ≤ 6144 byte NFR 충족 (각 Task 후 측정)
- 신규 테스트가 RED→GREEN 증명 + run-all 등록 + 기존 skills 스위트 무회귀
- 사용자 보고 섹션이 내부 식별자를 평문 번역하는가

### 완료 기록

- 날짜: 2026-06-01
- 유형: feat
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 2
- 변경 파일:
  - `plugins/rein-core/skills/parallel-execute/SKILL.md` (신설, 6093 byte ≤ 6144 NFR)
  - `tests/skills/test-parallel-execute-skill.sh` (신설, 33 단언)
  - `tests/skills/run-all.sh` (신규 테스트 등록)
  - `trail/dod/dod-2026-06-01-phase2-parallel-execute-skill.md` (DoD)
- 요약:
  병렬 실행 재설계 Phase 2 완료. 활성 plan 의 `## 실행 전략` v2 를 읽어 위상정렬 웨이브로
  나누고, 독립 edit_only 태스크를 같은 트리에서 병렬 서브에이전트로, mutating·의존 태스크는
  순차 실행하는 `parallel-execute` 스킬을 신설. Phase 1 의 schedule emitter
  (`rein-validate-coverage-matrix.py schedule`) 를 canonical 순서 SSOT 로 소비. 워커는 공통
  결과 스키마 6키(task_id/status/changed_files/blocked_reason/recommendation/summary)를 최종
  메시지로 반환, 부모는 클린 시작 → 웨이브 델타 ⊆ scope union 검증 → 위반 reject → 웨이브당 1커밋.

- 리뷰:
  - codex (medium 1): 테스트가 결과 스키마 `summary` 키 미검증 → 단언 추가로 해소.
  - security standard (medium 1): 경로 안전화가 `..` 만 명시, 표준 #7 의 절대경로·NUL·드라이브
    문자·symlink(realpath) 누락 + 검증기가 이 토큰들을 안 걸러 step4 가 유일 방어선 → reject
    집합 전체 + realpath containment + scope·델타 동일 정규형으로 확장, 재리뷰 PASS.
  - 6144 byte 예산: 보안 확장으로 6258 초과 → 목적/preflight/host-fallback/보고 섹션 무손실
    압축으로 6093 복귀 (모든 테스트 키워드 보존).

- 다음:
  - **Phase 3**: plan-writer v2 (depends_on + edit_only/mutating 판단) + pre-edit-dod-gate 의
    PLN-1 parallelizable 블록 제거 + 워크트리 기계 폐기 (worktree-cleanup.md + feature-builder-worker 재작성).
  - **Phase 4**: 회귀 테스트 스위트 통합 (validator + 스케줄러 + 부모 end-to-end).
  - main 병합 시 patch bump (새 user-exposed 스킬 추가 — minor 후보이나 시리즈 종결 후 일괄 판정).

---
<!-- source: 2026-06-01-phase3-planwriter-gate-worktree.md -->
## phase3-planwriter-gate-worktree

### DoD

- 날짜: 2026-06-01
- 유형: refactor (병렬 실행 재설계 Phase 3 — 구 워크트리/통짜-불리언 surface 정리 + plan-writer v2 전환)
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 3 (Task 3.1/3.2/3.3)

## 목표 (Why)

Phase 1(스키마/검증기 v2)·Phase 2(parallel-execute 스킬)가 새 모델을 확립했다. Phase 3 은 그 모델과 충돌하는 **구 surface 를 정리**한다: (1) plan-writer 가 v2 `## 실행 전략`(depends_on + edit_only/mutating)을 생성하도록 전환, (2) 더 이상 의미 없는 PLN-1 parallelizable 차단 블록을 게이트에서 제거, (3) 폐기된 워크트리 기계(cleanup doc + worker frontmatter + marker 계약) 제거. 세 작업은 서로 다른 파일을 만지므로 독립 — 병렬 실행 가능.

## 성공 기준 (Acceptance)

### Task 3.1 — plan-writer.md v2 (depends_on + edit_only/mutating 판단)
covers: PLANWRITER-V2-DEPENDS-ON-EDIT-ONLY-MUTATING-JUDGMENT

1. **실패 테스트 먼저 (RED)** — `tests/agents/test-plan-writer-exec-strategy-v2.sh` 신설: plan-writer.md 에 "depends_on"/"edit_only"/"mutating"/"동시 실행"/"disjoint" presence, **부재**: "parallelizable"/"3 axis"/"worktree-cleanup". RED 확인.
2. `plugins/rein-core/agents/plan-writer.md` `## 실행 전략 결정` 섹션 v2 재작성: v2 `## 실행 전략` 자동 첨부(tasks[]+depends_on+mode+scope) + 태스크별 edit_only/mutating 판단(mutating = 커밋/코드젠/전체포매터/패키지설치/스냅샷/변경성 테스트/scope 밖 쓰기 중 하나라도) + scope = 예상 실제 write set(mutating 은 부작용 경로 포함) + **동시 실행 가능한 edit_only 끼리만 pairwise disjoint**(depends_on 으로 순서 강제된 쌍은 겹쳐도 무방) + 불명확 시 순차 기본. 구 3-axis worker-split·workers[].scope·worktree-cleanup 참조·manual dispatch 제거. v2 예시(2 edit_only + 1 mutating) 추가, v1 예시 제거.
3. GREEN.

### Task 3.2 — pre-edit-dod-gate.sh PLN-1 블록 제거 (다른 분기 무영향)
covers: GATE-REMOVAL-PARALLELIZABLE-ENFORCEMENT-OTHERS-INTACT

4. **실패 테스트 먼저 (RED)** — `tests/hooks/test-pre-edit-dod-gate-pln1-enforce.sh` 재작성: (a) `grep 'PLN-1: parallelizable enforcement'` 매치 **없어야**, (b) `grep 'parallelizable plan without AG-2 worker'` 없어야, (c) `bash -n` 구문 OK, (d) 다른 분기 intact (DoD/라우팅/spec-review 분기 grep presence 유지). RED(아직 블록 존재).
5. `plugins/rein-core/hooks/pre-edit-dod-gate.sh` 에서 `# === PLN-1: parallelizable enforcement ...` 주석부터 그 `if [ -d "$DOD_DIR" ]; then ... done; fi` 블록까지 통째 삭제. 직후 `if [ "$DOD_FOUND" = true ]` 블록 보존.
6. `bash -n` 구문 검증 + 다른 게이트 회귀 확인(`bash tests/hooks/test-dod-gate.sh`). GREEN.

### Task 3.3 — 워크트리 기계 폐기
covers: WORKTREE-MACHINERY-DISCARD

7. **실패 테스트 먼저 (RED)** — `tests/agents/test-ag2-worktree-frontmatter.sh` 재작성: (a) feature-builder-worker.md frontmatter 에 `isolation: worktree` **없어야**, (b) 본문에 "worker-marker.json"/"worker-result.json"/"git worktree"/"cleanup" **없어야**, (c) "edit_only"/"선언 scope"/"커밋 금지"/"구조화 결과 반환" presence, (d) `worktree-cleanup.md` 파일 부재. RED.
8. `git rm plugins/rein-core/docs/worktree-cleanup.md`. `feature-builder-worker.md` 재작성: frontmatter `isolation: worktree` 제거 + description 을 "같은-트리 edit-only 병렬 워커 — parallel-execute 스킬이 선언 scope 로 dispatch" 로 교체. 본문 = 선언 scope 만 편집 / 금지목록(커밋·스테이징·stamp·trail·전체 포매터·변경성 명령) / 구조화 결과 반환(task_id/status/changed_files/blocked_reason/recommendation/summary — Phase 2 일치). 워크트리 생성·marker·result.json·cleanup·cherry-pick·stamp 소유권 섹션 전부 제거. 본문 ≤ 4 KB(NFR).
9. 잔존 참조 정리: `grep -rn "worktree-cleanup\|worker-marker.json\|worker-result.json\|isolation: worktree" plugins/rein-core/ scripts/` → dangling 참조 함께 정리. **확인된 dangling: `plugins/rein-core/rules/operating-sequence.md:26`** 의 `worker-result.json` blocked 신호 언급 → "worker 는 최종 메시지의 구조화 결과(status=blocked)로 보고" 로 교체. GREEN.

### 공통
10. scripts/ ↔ plugins/rein-core/scripts/ 미러 동기 필요 시 byte-identical 유지(해당 시).
11. codex 리뷰 PASS + 보안 리뷰 PASS (commit gate). 두 stamp 후 커밋.

## 변경 파일

- `plugins/rein-core/agents/plan-writer.md` (3.1)
- `tests/agents/test-plan-writer-exec-strategy-v2.sh` (3.1 신설)
- `plugins/rein-core/hooks/pre-edit-dod-gate.sh` (3.2)
- `tests/hooks/test-pre-edit-dod-gate-pln1-enforce.sh` (3.2 재작성)
- `plugins/rein-core/agents/feature-builder-worker.md` (3.3 재작성)
- `plugins/rein-core/docs/worktree-cleanup.md` (3.3 삭제)
- `plugins/rein-core/rules/operating-sequence.md` (3.3 dangling worker-result.json 참조 정리)
- `tests/agents/test-ag2-worktree-frontmatter.sh` (3.3 재작성)

## 제외 (Out of scope)

- Phase 4 회귀 스위트 통합 — 별 DoD.
- 검증기/스케줄러/스킬 자체 변경 — Phase 1/2 확정.
- pre-edit-dod-gate 의 DoD/라우팅/spec-review 분기 변경 — PLN-1 블록만 제거, 나머지 무영향.

## 리스크

- (R1) 3.2 가 라이브 게이트 파일 편집 — 그러나 실행 중 훅은 설치된 plugin 사본이라 저장소 편집이 현재 세션 게이트 동작에 영향 없음. 안전. 그래도 `bash -n` + test-dod-gate.sh 로 회귀 확인.
- (R2) feature-builder-worker 재작성 시 다른 agent/rule 의 dangling 참조 — `grep -rn` 으로 전수 확인 후 동시 정리.
- (R3) 병렬 3 subagent 가 같은 트리 공유 — 파일 disjoint(plan-writer / pre-edit-dod-gate / feature-builder-worker+worktree-cleanup)라 write-set 충돌 없음. 부모가 사후 통합 diff 검증.

## 라우팅 추천

```yaml
agent: rein:feature-builder-refactor   # 구 surface 정리·구조 전환 (기능 변경 없이 모델 정합) — refactor 적합
skills:
  - rein:codex-review                  # commit gate 필수
mcps: []
rationale: >
  Phase 3 = 구 워크트리/통짜-불리언 surface 를 새 모델에 맞춰 정리하는 refactor.
  3.1 plan-writer 지시 전환, 3.2 게이트 블록 제거, 3.3 워크트리 기계 폐기 — 세 작업
  file-disjoint·무의존이라 병렬 dispatch. 각 TDD(부재 검증 포함). security_tier=normal
  — 3.2 가 게이트 코드를 만지나 라이브는 설치 사본이라 자기 영향 0, DoD/라우팅/spec
  분기 보존을 테스트로 강제.
security_tier: normal
approved_by_user: true                 # 사용자 "Phase 3,4 오토모드 자동 진행" 위임 (2026-06-01)
```

## Self-review 예정 항목 (AGENTS.md §6)

- plan-writer v2 가 구 3-axis/parallelizable/worktree 참조를 완전히 제거하고 v2 만 남기는가
- PLN-1 블록 제거 후 DoD/라우팅/spec-review 게이트 분기가 전부 intact 인가 (test-dod-gate.sh 무회귀)
- feature-builder-worker 재작성이 Phase 2 워커 결과 스키마 6키와 일치하는가
- worktree-cleanup.md 삭제 후 dangling 참조 0 (grep 전수)
- 병렬 3 subagent 사후 통합 diff 가 선언 파일 집합 내인가

### 완료 기록

- 날짜: 2026-06-01
- 유형: refactor
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 3
- 변경 파일:
  - `plugins/rein-core/agents/plan-writer.md` (3.1 — `## 실행 전략 결정` v1→v2 재작성)
  - `tests/agents/test-plan-writer-exec-strategy-v2.sh` (3.1 신설, 11 단언)
  - `plugins/rein-core/hooks/pre-edit-dod-gate.sh` (3.2 — PLN-1 parallelizable 블록 제거)
  - `tests/hooks/test-pre-edit-dod-gate-pln1-enforce.sh` (3.2 재작성, 7 단언 — 블록 부재 + 타 분기 intact)
  - `plugins/rein-core/agents/feature-builder-worker.md` (3.3 재작성, 4074 byte)
  - `plugins/rein-core/docs/worktree-cleanup.md` (3.3 삭제)
  - `plugins/rein-core/rules/operating-sequence.md` (3.3 dangling worker-result.json 참조 정리)
  - `tests/agents/test-ag2-worktree-frontmatter.sh` (3.3 재작성, 22 단언)
- 요약:
  병렬 실행 재설계 Phase 3 완료 — 구 v1 surface 정리. (3.1) plan-writer 가 v2 `## 실행 전략`
  (depends_on + edit_only/mutating 판단 + 예상 write-set scope + 동시쌍 disjoint) 을 생성하도록
  전환, 구 3-axis/parallelizable/workers[]/worktree-cleanup 참조 제거. (3.2) pre-edit-dod-gate 의
  PLN-1 parallelizable 차단 블록(워크트리 worker-marker 우회 포함) 통째 제거 — DoD/라우팅/spec-review
  /incident 게이트 분기는 전부 보존. (3.3) feature-builder-worker 를 같은-트리 edit-only 워커로
  재작성(Phase 2 결과 스키마 6키 일치, isolation:worktree·marker·result.json·cleanup·cherry-pick
  제거), worktree-cleanup.md 삭제, operating-sequence.md 의 worker-result.json 언급 정리.
- 실행 방식: 세 작업 file-disjoint → 병렬 subagent(feature-builder-refactor) 3개 동시 dispatch,
  부모가 사후 통합 검증 + 단일 커밋 (parallel-execute 모델 dogfood).
- 리뷰:
  - codex PASS (차단 결함 0, 세 Scope 전부 MATCH, 게이트 분기 intact 확인, dangling 참조 0).
    테스트 grep 기반이라 일부 PARTIAL — codex "grep 계약 테스트로 수용 가능" 판정.
  - security standard PASS — 게이트 제거가 workflow-only 블록 제거이며 보안 regression·우회 없음,
    워커 금지 계약(커밋·표시·trail 자체 생성 불가) 유지, 주입·시크릿 없음.
  - 회귀: test-dod-gate 8/8, 3 신규/재작성 테스트 GREEN, dangling 참조 0, 게이트 구문 OK.
- 다음: Phase 4 (결정적 스케줄러 + 부모 델타 검증 동작 테스트 + run-all 등록 + 전체 그린).

---
<!-- source: 2026-06-01-phase4-regression-suite.md -->
## phase4-regression-suite

### DoD

- 날짜: 2026-06-01
- 유형: test (병렬 실행 재설계 Phase 4 — 스케줄러/부모-델타 동작 테스트 + 러너 등록)
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 4 (Task 4.1/4.2)

## 목표 (Why)

Phase 1~3 의 산출물(검증기 v2 + schedule emitter, parallel-execute 스킬, plan-writer v2, 게이트 제거, 워크트리 폐기)에 대한 회귀 그물을 완성한다. 검증기 단위 테스트(Phase 1)·스킬/plan-writer/게이트/워커 grep 테스트(Phase 2~3)는 이미 존재 — 본 Phase 는 (1) **결정적 스케줄러 + 부모 델타 검증의 동작(product 표면)** 테스트를 신규 작성하고, (2) 신규 테스트를 러너에 등록해 전체 그린을 확인한다.

## 성공 기준 (Acceptance)

### Task 4.1 — 결정적 스케줄러 + 부모 웨이브-union 검증 동작 테스트
covers: REGRESSION-TESTS-V2

1. `tests/scripts/test-wave-scheduler-and-parent-delta.sh` 신설.
2. **스케줄러 동작 (product 표면 직접 검증)** — fixture plan(혼합 ready 집합: edit_only 2 disjoint + mutating 1, depends_on 구성)에 `python3 scripts/rein-validate-coverage-matrix.py schedule <fixture>` 실행 → 출력 step 순서가 **mutating-solo-then-edit_only + plan 순서 tiebreak** 으로 결정적인지 단언(mutating 단독 step + edit_only 동시 step). "테스트가 테스트를 검증" 회피 — emitter 가 testable product.
3. **부모 델타 검증 로직** — 임시 git repo fixture 에서 (a) 델타 ⊆ scope union → 통과, (b) 선언 밖 파일(untracked 디렉토리 포함) 변경 → reject 단언. 델타 산출은 `git status --porcelain=v1 -z -uall` 정규화 경로 비교를 셸 헬퍼로 재현(스킬 지시 명령과 동일 형태).
4. Run → GREEN.

### Task 4.2 — 러너 등록 + 전체 회귀 그린
covers: REGRESSION-TESTS-V2

5. `tests/scripts/test-wave-scheduler-and-parent-delta.sh` 를 `tests/scripts/run-all.sh` 에 등록(CI 가 invoke 하는 러너 — 명시 리스트 패턴 동일).
6. `tests/agents/` 가 유일하게 run-all.sh 부재 → 다른 디렉토리(hooks/rules/scripts/skills)와 동일 패턴으로 `tests/agents/run-all.sh` 신설, agents 테스트(test-ag2-worktree-frontmatter / test-plan-writer-exec-strategy-v2 / test-dod-changed-files-section) 등록. (test-parallel-execute-skill 은 이미 tests/skills/run-all.sh 등록됨 — 확인만.)
7. 폐기/재작성 테스트 정합: 재작성된 test-pln1-enforce / test-ag2-worktree-frontmatter / 검증기 v2 테스트가 러너에서 호출되며 통과하는지 확인(이름 유지).
8. Run: 전 디렉토리 러너(`tests/hooks/run-all.sh`, `tests/scripts/run-all.sh`, `tests/rules/run-all.sh`, `tests/skills/run-all.sh`, `tests/agents/run-all.sh`) → 전부 GREEN. legacy v1 plan(`docs/plans/2026-05-28-ag2-dogfood-4-worker.md`)은 재검증 안 됨(히스토리) 확인.
9. codex 리뷰 PASS + 보안 리뷰 PASS (commit gate).

## 변경 파일

- `tests/scripts/test-wave-scheduler-and-parent-delta.sh` (신설)
- `tests/scripts/run-all.sh` (신규 테스트 등록)
- `tests/agents/run-all.sh` (신설 — 다른 디렉토리 패턴 따라 agents 테스트 등록)

## 제외 (Out of scope)

- `.github/workflows/tests.yml` 에 tests/agents·tests/skills 러너 추가 — CI 표면 확장은 별 결정(test-ci-matrix.sh 가 현재 hooks+scripts 만 강제). 본 Phase 는 로컬 전체 그린 + scripts 러너(CI 도달) 등록까지. **CI 가 agents/skills 러너를 안 도는 구조적 사실은 부모에게 surface**.
- 검증기/스케줄러 emitter 로직 자체 변경 — Phase 1 확정, 본 Phase 는 소비/검증만.
- 스킬/plan-writer/게이트/워커 grep 테스트 — Phase 2~3 에서 이미 생성.

## 리스크

- (R1) 스케줄러 동작 테스트가 emitter 출력 포맷(`step <n>: <id>...`)에 의존 — 포맷 변경 시 깨짐. mitigation: Phase 1 이 SSOT 로 고정, 본 테스트가 그 계약을 회귀 보호(의도).
- (R2) 부모 델타 검증을 셸 헬퍼로 재현 — 스킬의 마크다운 지시와 drift 가능. mitigation: 스킬이 지시하는 정확한 명령(`git status --porcelain=v1 -z -uall` + repo-relative 정규화)을 그대로 사용, 동일성 주석 명시.
- (R3) 임시 git repo fixture — CI 환경 git 식별자/clean state 의존. mitigation: fixture 내 `git init` + 로컬 config, 절대경로 격리.

## 라우팅 추천

```yaml
agent: rein:feature-builder          # 신규 테스트 파일 + 러너 신설 (add-test) — feature-builder 적합
skills:
  - rein:codex-review                # commit gate 필수
mcps: []
rationale: >
  Phase 4 = 회귀 테스트 신설 + 러너 등록. product 표면(schedule emitter + 델타 로직)을
  직접 검증하는 동작 테스트가 핵심. 단일 coherent 작업(4.1 작성 → 4.2 등록)이라 단일
  subagent 순차. security_tier=normal — 테스트가 임시 git repo·셸 헬퍼를 다루므로 fixture
  격리·주입 표면 확인.
security_tier: normal
approved_by_user: true               # 사용자 "Phase 3,4 오토모드 자동 진행" 위임 (2026-06-01)
```

## Self-review 예정 항목 (AGENTS.md §6)

- 스케줄러 테스트가 emitter product 표면을 직접 호출하는가(테스트가 테스트 검증 회피)
- 부모 델타 테스트가 스킬 지시 명령과 동일 형태(`--porcelain=v1 -z -uall` + 정규화)인가
- 신규 테스트가 CI 도달 러너(tests/scripts/run-all.sh)에 등록됐는가
- tests/agents/run-all.sh 가 다른 디렉토리 패턴과 일치하는가
- 전 디렉토리 러너 전부 GREEN + 임시 fixture 격리(절대경로·로컬 config)
- CI 가 agents/skills 러너 미invoke 하는 구조적 사실을 surface 했는가

### 완료 기록

- 날짜: 2026-06-01
- 유형: test
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md §Phase 4
- 변경 파일:
  - `tests/scripts/test-wave-scheduler-and-parent-delta.sh` (신설, 8 단언)
  - `tests/agents/run-all.sh` (신설 — 다른 디렉토리 패턴 따라 agents 러너)
  - `tests/scripts/run-all.sh` (신규 테스트 + 미등록이던 test-pln1-execution-strategy 등록)
- 요약:
  병렬 실행 재설계 Phase 4 완료 — 회귀 그물 완성. (4.1) 결정적 스케줄러와 부모 델타 검증의
  **동작(product 표면)** 테스트 신설: schedule emitter 를 직접 호출해 혼합 ready 집합의 웨이브
  순서(edit_only 동시 → mutating 단독)·absent-section exit 0 을 단언, 임시 git repo 에서 스킬과
  동일한 `git status --porcelain=v1 -z -uall` 명령으로 델타 ⊆ scope union 검증(in-scope 수용 /
  out-of-scope 거부 / untracked 디렉토리 미collapse). (4.2) tests/agents/run-all.sh 신설(유일하게
  러너 부재였음), 신규 테스트를 CI 도달 러너 tests/scripts/run-all.sh 에 등록. 5개 디렉토리 러너 전부 GREEN.
- 실행 방식: 단일 feature-builder subagent 순차(4.1→4.2).
- 리뷰:
  - codex R1 HIGH: 델타 부분집합 검사가 공백 구분 문자열 + 부분문자열 매칭이라 공백 포함
    경로에서 오탐 가능 → 배열 + 정확비교로 수정 + 공백경로 회귀 케이스(c1 거부/c2 수용) 추가 →
    codex R2 PASS. (테스트 본연의 경로 처리 soundness 버그를 테스트 자체에서 잡은 셈.)
  - security standard PASS — test/runner bash 만, 임시 repo 격리(mktemp+local identity+trap),
    파괴적 git clean 도 서브셸 내 임시 repo 한정, NUL-safe 파싱, 주입·시크릿 없음.
  - subagent 가 TDD 중 NUL 스트림이 명령치환에서 소실되던 1차 버그도 자체 발견·수정.
- surface (구조적 관찰, 미조치): CI(tests.yml)는 hooks·scripts 러너만 invoke — agents/skills/rules
  러너는 로컬 전용. 신규 스케줄러/델타 테스트는 scripts 러너에 있어 CI 도달. CI 표면을 agents/skills
  까지 확장할지는 별 결정(test-ci-matrix 가 현재 hooks+scripts 만 강제).
- 다음: Phase 1~4 전부 완료 → 병렬 실행 재설계 종결. 잔여 = main 병합(버전 bump 판정) + index 갱신 + 자동모드 해제.

---
<!-- source: 2026-06-01-session.md -->
## session

### 완료 기록

- 날짜: 2026-06-01
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
(오늘 커밋 없음)

## 변경된 파일 (worktree + staged, 미커밋 전체)
trail/daily/2026-05-24.md
trail/dod/dod-2026-05-31-gate-bypass-guardrail-mtime-fp.md
trail/dod/dod-2026-05-31-wave-parallel-phase1-schema-validator.md
trail/inbox/2026-05-31-gate-bypass-guardrail-mtime-fp.md
trail/inbox/2026-05-31-parallel-execution-redesign-design.md
trail/inbox/2026-05-31-plan-driven-wave-parallel-execution-plan.md
trail/inbox/2026-05-31-session.md
trail/inbox/2026-05-31-wave-parallel-phase1-schema-validator.md
trail/index.md
trail/weekly/2026-W21.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.

---
<!-- source: 2026-06-01-v1-4-2-release.md -->
## v1-4-2-release

### DoD

- 날짜: 2026-06-01
- 유형: release (chore)
- plan ref: N/A (릴리스 작업). 근거: 사용자 지시 "나머지 모두 푸시하고 main 에 머지해" (2026-06-01) + 버전 결정 v1.4.2 (patch, 사용자가 advisory minor 를 patch 로 오버라이드).

## 목표 (Why)

v1.4.1 이후 dev 에 누적된 3 묶음을 v1.4.2 로 릴리스한다:
1. 병렬 실행 재설계 시리즈 Phase 1~4 (새 사용자 노출 스킬 `parallel-execute` + plan `depends_on` 웨이브 스케줄러 + plan-writer v2 + 회귀 테스트). 커밋 `20f3bb9`·`9ec1b90`·`d40234b`·`17c8898`·`91349f1`·`19227d0`.
2. 게이트우회 가드레일 (`b3986ef`·`bdf6433`).
3. `.claude/` 오버레이 정리 (`83d187c`).

버전 등급: advisory 규칙은 minor(새 사용자 노출 스킬) 이나 **사용자가 patch (v1.4.2) 로 명시 결정** — 이를 따른다.

## 성공 기준 (Acceptance)

### 1. 버전 표면 동기화 (2곳)
- `plugins/rein-core/.claude-plugin/plugin.json` version → `1.4.2`
- `scripts/rein.sh` VERSION → `1.4.2`
- 두 값 일치 확인.

### 2. CHANGELOG.md — user-facing 항목만 (Rule C)
- v1.4.2 엔트리 추가: 새 `parallel-execute` 스킬(plan 의존 웨이브 병렬 실행) + 게이트우회 가드레일(자율 진행 시 리뷰 게이트 우회 차단). 오버레이 정리는 메인테이너 내부라 CHANGELOG 제외(내부 경로 참조 정정).

### 3. README 버전 히스토리 (최신 1개 + CHANGELOG 링크)
- `README.md` + `README.ko.md` 에 v1.4.2 간략 1~2줄 엔트리. KR/EN parity 유지. 상세는 CHANGELOG 링크.

### 4. 리뷰 (commit 게이트)
- 버전 표면 편집이 `.review-pending` 생성 → codex 리뷰 PASS + 보안 리뷰 PASS.

### 5. dev 푸시 + main 선별 체크아웃
- dev → origin/dev 푸시.
- main clean tree 확인 후 **선별 체크아웃** (`git checkout dev -- <included>`): `plugins/rein-core/**`, `tests/**`, `scripts/rein*`(govcheck/heal-legacy/validate-coverage 포함), `AGENTS.md`, `CHANGELOG.md`, `README.md`/`README.ko.md`, `.claude-plugin/marketplace.json`(변경 시), `docs/{changelog-archive,troubleshooting,agents-md-examples}`(변경 시), `trail/**`, `.rein/project.json`(변경 시).
- **제외 확인**: `.claude/**` 오버레이(CLAUDE.md/rules/orchestrator/workflows/registry/plans/settings), `docs/{specs,plans,brainstorms,reports}/**`, 루트 임시 노트는 main 체크아웃 대상 아님.
- **main 잔존 overlay 점검**: 과거 머지 누락으로 main 에 `.claude/**` 잔재가 있으면 같은 머지에서 정리 여부 판단 (별 이슈면 분리).
- 훅 `bash -n` 통과.

### 6. 태그 + 푸시
- main 커밋 후 `git tag v1.4.2`.
- `git push origin main` + `git push origin v1.4.2`.
- publish-plugin + mirror-to-public GH Actions 트리거 확인(사용자 보고).

## 변경 파일 (release-prep)

- `plugins/rein-core/.claude-plugin/plugin.json` (version)
- `scripts/rein.sh` (VERSION)
- `CHANGELOG.md` (v1.4.2 엔트리)
- `README.md`, `README.ko.md` (버전 히스토리 엔트리)

## 제외 (Out of scope)

- 선택적 후속: CI(tests.yml) 가 agents/skills 러너 미invoke 확장 / G3 성능 / public strip spot-check — 별 결정.
- main 의 과거 overlay 잔재 전수 정리 — 발견 시 규모 보고 후 분리 판단(이번 릴리스 차단 아님).

## 리스크

- (R1) 선별 체크아웃에서 제외 경로(.claude/** 등)가 섞여 들어갈 위험 → mitigation: `git checkout dev -- <명시 경로>` 만, 이후 `git status` 로 예상 외 파일 점검.
- (R2) KR/EN README drift → mitigation: 양쪽 동시 편집 + parity 확인.
- (R3) 버전 patch 가 advisory minor 와 불일치 → 사용자 명시 결정이므로 수용, DoD 에 근거 기록.
- (R4) publish/mirror Actions 가 비정상 → push 후 `gh run` 으로 상태 확인, 실패 시 보고.

## 라우팅 추천

```yaml
agent: rein:docs-writer            # CHANGELOG + README 버전 엔트리 작성이 핵심 산출물
skills:
  - rein:codex-review              # 버전 표면 편집 → .review-pending → commit 게이트 필수
  - rein:changelog-writer          # CHANGELOG 엔트리 작성 보조
mcps: []
rationale: >
  릴리스 = 버전 동기화 + CHANGELOG/README user-facing 엔트리 + 선별 체크아웃 + 태그.
  문서 산출물 중심이라 docs-writer. 버전 표면 편집이 리뷰 게이트를 재유발하므로 codex-review
  필수. 본 세션이 누적 변경·파일 집합·버전 결정을 이미 확인했으므로 메인 세션 인라인 실행.
  security_tier=light — 버전 문자열 + 마크다운, 실행 경로 변화 없음.
security_tier: light
approved_by_user: true   # "나머지 모두 푸시하고 main 에 머지해" + 버전 v1.4.2 명시 결정 (2026-06-01)
```

### 완료 기록

- 날짜: 2026-06-01
- 유형: release (chore)
- 변경 파일(release-prep): `plugins/rein-core/.claude-plugin/plugin.json`(version 1.4.1→1.4.2), `scripts/rein.sh`(VERSION), `CHANGELOG.md`(v1.4.2 엔트리), `README.md`/`README.ko.md`(최신 릴리즈 1줄, EN/KO parity)
- 요약: v1.4.1 이후 dev 누적 3덩어리를 v1.4.2 로 릴리스 — (1) 병렬 실행 재설계 시리즈 Phase 1~4(parallel-execute 스킬 + plan depends_on 웨이브 스케줄러 + plan-writer v2 + 회귀 테스트), (2) 게이트우회 가드레일, (3) `.claude/` 오버레이 정리(plugin SSOT 정합 + govcheck plugin 훅 재지정). 버전 등급: advisory=minor(새 사용자 노출 스킬)이나 **사용자가 patch(v1.4.2) 명시 결정** — 따름.
- 절차: 릴리스 준비 편집 → codex 리뷰 PASS + 보안 통합 리뷰 PASS → dev 커밋 `83e74b1` 푸시 → main 선별 체크아웃(plugins/** + tests/** + scripts/rein* + AGENTS.md + CHANGELOG + README*2 + trail/**, 제외경로 0 검증) → main 커밋 `e76763d` + tag `v1.4.2` 푸시.
- 전파: publish-plugin success(마켓플레이스), mirror-to-public main+tag 둘 다 success. public 태그 `1270235b`(trail/.rein/AGENTS strip된 클린 트리 — 로컬 sha와 다른 것이 정상).
- 미해결/주의: `need-to-confirm.md` 가 이번 세션 중 **내가 작성하지 않은** 내용(plan 작성 경로 일관성, SPEC-WRITER-1 번들)으로 수정돼 dev 에 미커밋 상태로 남음 — 서브에이전트가 백로그를 건드린 것으로 추정. main 제외 대상이라 릴리스엔 영향 없음. 사용자에게 surface 함. 처리(커밋/되돌림) 사용자 결정 대기.
- 다음: 선택적 후속(CI 확장 / plan 경로 일관성 / G3-perf / SPEC-WRITER-1 설계).


---
## 2026-06-02
# Daily Summary: 2026-06-02

---
<!-- source: 2026-06-02-hook-hotpath-perf-design.md -->
## hook-hotpath-perf-design

### 완료 기록

## 무엇

사용자 요청 "G3 성능 개선 + 속도 검증 + 다른 병목 전수 점검" → 측정 후 설계-first 진행.

## 측정 결과 (macOS Darwin / Python 3.9.6, 훅당 20-run p95)

- **G3(meta-check) 단일 훅 목표 충족**: 빠른 경로 172ms ≤180ms, 대용량 195ms ≤200ms. 더 짤 것 없음(OS-level cold-start floor).
- **진짜 병목은 이벤트당 누적 체인**:
  - 편집 1회 = PreToolUse 3훅(~334) + PostToolUse 11훅(격리 합산 ~1556) ≈ **1.9s** ⚠️ 격리 합산 — CC 실제 dispatch 순차/병렬 미확인(SPIKE 도 미확인). 병렬이면 실질은 합 아님 → felt-value 불확실.
  - Bash 1회 = pre-bash-dispatcher 396(일반)~616(commit) + post-bash 129.
- **근본원인 3종**: (1) `resolve_python` 이 매 훅 health_check 로 python launch(~24ms 낭비) (2) post-edit-dispatcher 폐기로 일부 훅 중복 spawn (3) pre-bash 가 bootstrap/safety 별도 fork.

## 설계 결정 (사용자)

- 범위 = **A + C + B축소** (전체 재통합 아님).
- Track C ship/defer = **A 적용 후 측정값 보고 → 사용자 결정** (고정 임계 없음).

## 검증으로 좁혀진 그림 (중요)

- **B 전체 재통합은 닫힌 결정** (8→1 디스패처는 SPIKE-1 근거로 일부러 11 엔트리 재분할). 재개방 금지. 진짜 레버는 "프로세스 수↓"가 아니라 spawn↓.
- **A 무조건 launch-skip 은 위험** → 좁힌 형태(PATH bare python3/python 만, REIN_PYTHON·venv 는 health_check 유지). 단일 의도된 deviation: 깨진 bare python3 exit 12→0.
- A 와 B축소는 **disjoint 훅 집합** (A=resolve_python 8훅, B=bare-python3 2 rule 훅). 결합 없음.
- B축소 = 2 rule 훅의 file_path(3단 fallback 보존!)+tool_use_id spawn 병합 3→2. meta-check 는 이미 최적.

## 산출물 (모두 degraded 서브에이전트 검토 — codex usage-limit)

- brainstorm: `docs/brainstorms/2026-06-02-hook-hotpath-perf.md`
- spec: `docs/specs/2026-06-02-hook-hotpath-perf.md` (R1 NEEDS-FIX→R2 PASS+nit→R3 PASS, 표식 생성)
- plan: `docs/plans/2026-06-02-hook-hotpath-perf-implementation.md` (R1 NEEDS-FIX[B1 fallback 누락/B2 부재 fixture]→R2 PASS, validator exit 0, 표식 생성)
- reviewer id: `general-purpose-subagent-degraded-codex-unavailable` (codex 장애 정직 기재)

## 재개 지점

- **구현 미착수** — codex 복구 후 통합 코드+보안 리뷰 가능 시점에. plan 의 실행 전략(Wave 1 병렬 A+B+doc, 부모 게이트 Task 3.0, Wave 2 C, Wave 3 bench)대로 parallel-execute 가능.
- DoD 미작성 (구현 진입 시 작성).
- 릴리스 묶음: PLAN-PATH-1 + spec-writer + (본 perf 구현 후) 일괄 — 사용자 결정 대기.

---
<!-- source: 2026-06-02-plan-path-consistency.md -->
## plan-path-consistency

### DoD

- 날짜: 2026-06-02
- 작업 유형: 리팩토링 (구조 개선 — 스킬 제거 + 내부 문서화 + 라우팅 정리, 사용자 진입점 통일)
- plan ref: docs/plans/2026-06-02-plan-path-consistency-implementation.md

## 요약

plan 작성의 사용자 진입점을 `plan-writer` 에이전트 하나로 통일한다. `writing-plans` 스킬 절차를 `plugins/rein-core/docs/writing-plans-procedure.md` 내부 문서로 옮기고 스킬 디렉토리를 삭제해 `/rein:writing-plans` 직접 호출(자동 리뷰 누락) 경로를 코드 레벨에서 제거하며, plan-writer 가 내부 문서를 참조한다. 라우팅 2곳을 spec 작성 행과 정합시키고 회귀 테스트로 고정한다.

## 범위 (IN)

- PP-1: 작성 절차 내부 문서 생성 (`plugins/rein-core/docs/writing-plans-procedure.md`)
- PP-2: `plugins/rein-core/skills/writing-plans/` 삭제 (즉시 제거, shim 없음)
- PP-3: plan-writer 내부 호출 참조 갱신 (옛 `.claude/skills/` 경로 제거 + 새 문서 참조)
- PP-4: routing-map.md plan 작성 행 skill 컬럼 → `—`
- PP-5: routing-procedure.md §5 표 line 64/67 writing-plans 제거
- PP-6: 회귀 테스트 `tests/agents/test-plan-path-consistency.sh` 신규 + run-all 등재
- PP-7: 내부 문서 안내를 plan-writer 자동 리뷰 경로와 정합 재서술

## 범위 (OUT)

- spec 작성 경로 (이미 spec-writer 로 해소)
- codex-review 스킬 / spec-review·DoD 게이트 / coverage validator 변경 (무변경)
- deprecation shim / redirect (사용자 결정: 즉시 제거)
- agents 테스트 CI invoke 확장 (별도 추적)

## 변경 파일

- plugins/rein-core/docs/writing-plans-procedure.md
- plugins/rein-core/skills/writing-plans/SKILL.md
- plugins/rein-core/agents/plan-writer.md
- plugins/rein-core/rules/routing-map.md
- plugins/rein-core/rules/routing-procedure.md
- tests/agents/test-plan-path-consistency.sh
- tests/agents/run-all.sh

## 완료 기준 (검증)

1. `plugins/rein-core/skills/writing-plans/` 부재 — `/rein:writing-plans` 미노출
2. 내부 문서 존재(frontmatter 없음) + plan-writer 가 참조 + 옛 `.claude/skills/writing-plans` 잔재 0건
3. routing-map.md / routing-procedure.md §5 에 `writing-plans` 미등장 + routing-map ≤800B
4. `test-plan-path-consistency.sh` 5/5 PASS + `run-all.sh` 전체 PASS
5. `test-routing-map-emit.sh` / `test-slash-command-namespace.sh` 통과
6. `rein-check-plugin-drift.py` 정합성 통과
7. 내부 문서 안내가 plan-writer 자동 리뷰 경로와 모순 없음
8. codex 코드리뷰 + security 리뷰 통과

## 라우팅 추천

agent: rein:feature-builder-refactor
skills:
  - rein:parallel-execute
  - rein:codex-review
mcps: []
security_tier: light
rationale:
  - 작업 유형 = 리팩토링(스킬 제거 + 내부 문서화 + 라우팅 정리, 동작 표면은 진입점 통일). 키워드 "정리/구조 개선/refactor" → feature-builder-refactor.
  - 변경이 markdown 규칙·에이전트·docs·테스트에 한정, 실행 코드/보안 표면 없음 → security_tier light.
  - 5개 edit_only 태스크가 disjoint scope → plan 실행 전략의 웨이브 병렬을 parallel-execute 로 실행(사용자 선택). delete-skill 은 mutating 단독 웨이브.
approved_by_user: true

### 완료 기록

- 날짜: 2026-06-02
- 커밋: dev `5545967`(설계) + `57ed31d`(구현) — **미push / 미main머지** (릴리스는 나중에 일괄, 사용자 결정)
- DoD: trail/dod/dod-2026-06-02-plan-path-consistency.md

## 문제

plan 작성 진입점이 둘(`plan-writer` 에이전트=자동 리뷰 / `writing-plans` 스킬=수동 안내)이라 사용자가 스킬을 직접 호출하면 codex 자동 리뷰가 누락됐다. spec-writer 가 "에이전트 단독"으로 해소한 것과 같은 진입점 일관성 테마의 plan 쪽 잔존 비대칭.

## 결정 (설계)

- brainstorm: codex-ask(gpt-5.5/high)가 4번째 옵션 제안 — 작성 절차를 auto-discover 되는 `skills/` 밖 내부 문서로 옮기고 plan-writer 가 참조. 사용자 선택 = **Option D + 즉시 제거**(deprecation shim 없음).
- codex Q2 교정: "라우팅에서만 빼면 게이트가 막아준다"는 불완전(post-edit 훅 fail-open + pre-edit 게이트의 tests/ 면제) → 직접 호출 경로 자체 제거가 견고.

## 구현 (PP-1~PP-7)

- PP-1/PP-7: `writing-plans/SKILL.md` 절차를 `plugins/rein-core/docs/writing-plans-procedure.md` 로 rename(80% 유사도) — frontmatter 제거, H1 교체, 수동 `/codex-review` 안내를 plan-writer 자동 경로로 정합.
- PP-2: `skills/writing-plans/` 삭제 → `/rein:writing-plans` 직접 호출 경로 소멸.
- PP-3: `plan-writer.md` `## 내부 호출` → `## plan 작성 절차`, 내부 문서 참조 + 옛 `.claude/skills/writing-plans` 경로 제거.
- PP-4/PP-5: `routing-map.md`(782B) plan 행 skill → `—`, `routing-procedure.md` §5 두 행에서 writing-plans 제거.
- PP-6: `tests/agents/test-plan-path-consistency.sh` 5종 신규 + run-all 등재.

## 검증

- 신규 회귀 5/5, agents run-all ALL PASS, test-routing-map-emit OK, test-slash-command-namespace OK(13 skills), rein-check-plugin-drift OK.
- 설계 3단계 codex: spec review PASS(R1), plan review PASS(R2 — R1 High[worker commit step]+Medium[drift 검증 누락] 수정 후).
- 구현: codex 코드리뷰 PASS + 보안 리뷰 PASS(보안 표면 없음, light tier).

## 실행 방식 (dogfood)

parallel-execute 스킬로 plan 실행 전략을 dogfood — 웨이브 1(edit_only 5개 disjoint 병렬 worker) + 웨이브 2(delete-skill, 부모 직접 git rm 순차 fallback). 부모가 델타 부분집합 검증 후 통합 검증·리뷰·커밋.

## 회고

- spec-writer(인라인)와 plan(내부 문서 참조)은 **구현 형태는 비대칭이나 결과(사용자 노출=에이전트 단독)는 대칭**. plan 절차가 길어 인라인 대신 문서 분리 선택.
- plan-writer 자동 리뷰 절차를 메인 세션이 대행 — 서브에이전트 dispatch 대신(cache 버전·내부 Skill/codex 호출 불확실성 회피). plan R1 NEEDS-FIX 의 명백한 결함(worker commit step)은 작성자로서 직접 수정(에이전트 self-fix 금지와 구분).

## 잔여 (백로그)

- 릴리스: spec-writer + PLAN-PATH-1 묶어서 나중에 일괄 (사용자 결정).
- G3-perf-NFR (perf follow-up), agents 테스트 CI invoke 확장 (별도 추적).

---
<!-- source: 2026-06-02-session.md -->
## session

### 완료 기록

- 날짜: 2026-06-02
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
4098dce test(agents): spec-writer 자동리뷰 계약 6종 + 러너 등재 (SW-7)
128db46 feat(rules): 라우팅 2곳에 spec 작성 진입점 추가 (SW-5, SW-6, SW-9)
1e7a81c feat(agents): spec-writer 에이전트 본문 신규 (SW-1~SW-4, SW-8)
28f25f3 docs(spec-writer): brainstorm+spec+plan 설계 단계 + 백로그 정리

## 변경된 파일 (worktree + staged, 미커밋 전체)
trail/dod/.active-dod
trail/dod/.codex-reviewed
trail/dod/.session-has-src-edit
trail/dod/dod-2026-06-01-claude-overlay-cleanup.md
trail/dod/dod-2026-06-01-phase2-parallel-execute-skill.md
trail/dod/dod-2026-06-01-phase3-planwriter-gate-worktree.md
trail/dod/dod-2026-06-01-phase4-regression-suite.md
trail/dod/dod-2026-06-01-v1-4-2-release.md
trail/inbox/2026-06-01-claude-overlay-cleanup.md
trail/inbox/2026-06-01-phase2-parallel-execute-skill.md
trail/inbox/2026-06-01-phase3-planwriter-gate-worktree.md
trail/inbox/2026-06-01-phase4-regression-suite.md
trail/inbox/2026-06-01-session.md
trail/inbox/2026-06-01-v1-4-2-release.md
trail/incidents/.last-aggregate-state.json
trail/incidents/active-dod-cleanup.log
trail/incidents/invalid-active-dod-marker.log
trail/index.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.

---
<!-- source: 2026-06-02-spec-writer-agent.md -->
## spec-writer-agent

### 완료 기록

## 작업

사용자 최우선 지정 항목 SPEC-WRITER-1 을 brainstorm → spec → plan → 구현 풀사이클로 완료. 설계 단계 도구 비대칭(spec 만 전용 도구 부재) 해소.

## 흐름 / 결정

- **stale 정리**: PLN-1/AG-2(병렬 worker)가 v1.4.2 로 이미 릴리스 완료된 것을 tag tree 로 확인, need-to-confirm 에서 제거 + confirmed 이관 (사용자 승인).
- **brainstorm** (Option A): codex-ask high 로 Step 0 전제 검증 — codex 리뷰의 `spec review for design:` 서브플로우 / 편집후 게이트(docs/specs 커버) / pre-edit 하드게이트 / 표식 스크립트가 **이미 spec 을 커버**함을 확인 → 작업 범위가 "plan-writer 복제 + 라우팅 + 테스트" 로 축소. 사용자 결정 2건: 진입점 = 에이전트 단독(writing-specs 스킬 미생성), 사이클 = spec-writer 먼저(plan 경로 일관성 부채는 별도).
- **spec**: codex spec-review 2라운드(R1 NEEDS-FIX → v1 Scope ID 명시 + agents 테스트 CI 확장 명시적 제외 보완 → R2 PASS) + 표식.
- **plan**: plan-writer 에이전트가 작성 + 자동 spec-review(2라운드 보정 후 PASS) + 표식. v2 웨이브 전략(4 edit_only disjoint + 1 의존).
- **구현**: parallel-execute 로 웨이브1 4워커 병렬 + 웨이브2 1워커. 부모가 웨이브 델타(scope 정확 일치) 검증 + 테스트 + 통합 코드리뷰(codex PASS, 차단급 0) + 커밋.

## 산출물 (dev, 미push/미main머지)

- 설계: docs/{brainstorms,specs,plans}/2026-06-02-spec-writer-agent*.md (spec·plan 표식)
- 구현: plugins/rein-core/agents/spec-writer.md (신규), routing-map.md(799B)/routing-procedure.md 'spec 작성' 행, tests/agents/test-spec-writer-auto-review-contract.sh(신규 6 assertion)+run-all.sh 등재
- 커밋: `28f25f3`(설계) `1e7a81c`(agent) `128db46`(routing) `4098dce`(test)
- 검증: 계약 7/7, 전체 agents 러너 ALL PASSED, drift OK, 코드리뷰 PASS

## 다음

- **릴리스 결정 대기** — 버전 bump 등급(신규 user-facing 에이전트 = minor 후보, versioning Rule A) + main 선별 체크아웃 + publish. 사용자 결정 필요.
- 잔여: plan 경로 일관성 부채(별도), agents 테스트 CI 확장(별도), G3-perf-NFR.


---
## 2026-06-04
# Daily Summary: 2026-06-04

---
<!-- source: 2026-06-04-route-bind-1.md -->
## route-bind-1

### 완료 기록

## 한 일

need-to-confirm.md 검토(/codex-ask)에서 시작 → SPEC-WRITER-1/PLAN-PATH-1/G3-perf-NFR 3건이 v1.4.3 에 이미 해결된 것을 검증하고 confirmed.md 이관 → ONBOARD-1 brainstorm 중 사용자가 "규칙이 매 세션 들어오는데 왜 에이전트가 spec-writer 없이 인라인 작성?" 제기 → codex-ask 로 **설계 단계 라우팅 강제력 부재(ROUTE-BIND-1)** 진단 → 별도 항목 등재 + 먼저 설계(사용자 옵션 나).

ROUTE-BIND-1 설계 체인 전부 codex PASS:

- brainstorm: `docs/brainstorms/2026-06-04-design-phase-routing-nudge.md`
- spec: `docs/specs/2026-06-04-design-phase-routing-nudge.md` (spec-writer 경유, codex R2 4건 보완 → R3 PASS). 표식 reviewer=`codex-r3-pass-then-formatonly-restamp` (plan-writer 가 Scope 표 형식 보정해 재표식).
- plan: `docs/plans/2026-06-04-design-phase-routing-nudge-implementation.md` (plan-writer 경유, codex R2 5건 + R2재검토 2건 보완 → R3 PASS). 표식 reviewer=`codex-gpt-5.5-medium-r3`. coverage EXIT=0, schedule EXIT=0.

## 확정 설계 (구현 대기)

spec/plan 파일이 전용 에이전트의 provenance **claim**(작성 *직전* 기록 + 호스트 훅이 매칭 시 **소비**) 없이 써지면 기존 `post-edit-design-plan-coverage-rule.sh` 가 **soft nudge**(stderr, exit 0, 차단 아님) emit. timestamp 비교 없음(presence+consume — 동일초 FN 제거). 경로 = `docs/specs/**`·`docs/plans/**`. operating-sequence 에 설계 체인 1줄. 매 편집 hot-path 부하 0(하드 제약).

## 다음 세션 진입점

ROUTE-BIND-1 **구현** — plan Phase 1~5: ①helper 스크립트(`rein-mark-design-provenance.sh`) ②호스트 훅 nudge inline(함수 정의+호출 같은 커밋) ③에이전트 2개 claim 단계 ④operating-sequence 설계 체인 ⑤성능검증+dogfood+회귀. 웨이브: step1 host-hook(mutating 단독) → step2 helper+operating-sequence → step3 tests → step4 agents. 전부 미커밋·미push.

남은 백로그: SEC-ROUTE-1 / ONBOARD-1 / ROUTE-DOC-1 (모두 결정 대기).

## 구현 완료 (2026-06-04, 같은 날 오후)

병렬 실행(parallel-execute)으로 plan Phase 1~5 완주 → dev 커밋 `d4cfbd8` (미push, main 미반영).

- 웨이브 실측 스케줄: step1 host-hook(mutating 단독) → step2 helper+operating-sequence(병렬) → step3 tests(mutating) → step4 agents. 각 웨이브 후 부모가 델타 ⊆ scope 검증.
- baseline 커밋 `e8b8337`→설계체인/trail 정리 후 클린 트리에서 웨이브 시작. 커밋은 웨이브별이 아니라 통합 리뷰 후 1회(리뷰 게이트가 src 커밋에 표식 요구 → 누적→통합리뷰→커밋 전략).
- codex 코드리뷰: R1 NEEDS-FIX 2건 → 해소 → R2 PASS.
  - R1-1: 호스트 훅 `_hash=$(...)` 무가드 → `set -e` 비정상종료 위험. fix: `|| _hash=""` + 매칭에 `[ -n "$_hash" ]`. sibling `_pd=$(resolve_project_dir)`도 `|| _pd="$PWD"` 가드.
  - R1-2: 케이스(g) `RC==2`만 거부 → `RC==0` 요구로 엄격화 + 케이스(h) 해시명령 실패 fail-soft 회귀 추가.
- security(standard) PASS — path traversal/injection 실측 차단(hex hash collapse), rm 단일마커 scope, claim 위조 영향=advisory 침묵뿐(차단우회 아님), secret 노출 없음.
- 테스트: 회귀 (a)~(h)+(c') 전부 green, helper 단위테스트 green, run-all 전체 PASS. 커버리지/drift EXIT 0.
- NFR 실측(N=100): 비-design hot-path 102ms(nudge 코드 미도달, 추가분 0) / design 경로 +86ms(hash+abspath 1회씩, 신규 hooks.json entry 쉘 spawn 비용보다 작음) / hooks.json diff 0.
- SC-9 dogfood: (auto) helper-level claim/consume 시뮬레이션 = 회귀 case(a)+helper 단위테스트로 증명. (live) 에이전트 호출 plugin-aware 경로 end-to-end(claim 생성→무발화→소비) 세션 실증. **단, spec/plan 자체는 이번 feature 이전(전 세션)에 작성돼 retroactive live 증거는 없음 — 에이전트에 claim 단계가 wired 됐고 다음 실제 설계 사이클에서 발화**.

## 릴리스 완료 — v1.4.4 (2026-06-04)

사용자가 "ROUTE-BIND-1 먼저 릴리스" 선택 → patch bump(advisory·비차단·대상 좁음). dev `f47702a`/main `70fba39`/tag `v1.4.4`. 선별 체크아웃 13파일(plugins/rein-core ROUTE-BIND-1 5개 + tests 3개 + scripts/rein.sh + plugin.json + CHANGELOG + README EN/KO). worktree 격리(dev 트리 dirty=사용자 need-to-confirm + 마커). publish-plugin + mirror(main) + mirror(tag) GH Actions **전부 success**. 릴리스 코드 리뷰 = sonnet 셀프리뷰(버전+docs only, ROUTE-BIND-1 코드는 이미 codex R2+security PASS at d4cfbd8).

- 버전 표면 2곳 parity(rein.sh==plugin.json==1.4.4). README EN/KO parity(최신 1개만, readme-style §3).
- trail 이번 릴리스 미동기화(main 에 이미 존재=bootstrap 충족, ROUTE-DOC-1 trail 혼입 회피, public strip 대상).
- 교훈: ROUTE-DOC-1 spec `.pending`(plan-writer cosmetic 정규화 무효화)이 **전 소스 편집을 차단** → 릴리스 버전 편집 전 spec 재리뷰(R2 PASS)로 선해소 필요했음. 설계 산출물 정규화가 리뷰 표식을 깨면 후속 소스 작업이 막힌다.

## 이전 단계 — 다음 결정 (사용자, 해소됨)

dev 커밋 완료 후 선택지: (1) dev push (2) main 머지+릴리스 (3) 백로그 계속. → 사용자: 백로그 계속(ROUTE-DOC-1) → 그 후 "ROUTE-BIND-1 먼저 릴리스" → v1.4.4 출하 완료.

## 메모

- 산출물(brainstorm/spec/plan) + 표식 + need-to-confirm/confirmed 편집 모두 dev 미커밋 상태로 보존(사용자 결정). → 구현 시작 시 baseline `e8b8337` 로 일괄 커밋함.
- 교훈: 주입된 advisory 규칙은 게이트 없는 단계(설계)에서 안 바인딩됨. 고침은 "조언을 더"가 아니라 **전환점(파일 작성 순간)에서 nudge** — codex 가 첫 세션 프라이머로는 못 고친다고 분명히 함.
- 교훈: codex 가 "작성 직후 표식"의 타이밍 결함(훅이 Write 직후 동기 발화 → 표식보다 먼저)을 잡음 → claim-직전+consume 으로 재설계. 외부 컴포넌트/훅 실행 순서는 설계 시 실측 가정 검증 필요.

---
<!-- source: 2026-06-04-route-doc-1.md -->
## route-doc-1

### 완료 기록

## 한 일

ROUTE-BIND-1 구현 완료 후 백로그 계속(사용자 선택). 3 백로그(SEC-ROUTE-1/ONBOARD-1/ROUTE-DOC-1) 중 ROUTE-DOC-1 선택 → 범위 결정 시 codex 6지적을 직접 실측, **단순 문서 부채가 아니라 plugin-first 전환 후 고아 라우터 동작**임을 발견 → 사용자가 "문서 + 고아 동작까지" 큰 범위 선택 → brownfield 설계 착수.

## 검증된 발견 (실측)

- **두 조합표 불일치**: routing-map(8행/MCP열 없음) vs routing-procedure §5(12행/MCP열). `superpowers:systematic-debugging`·코드리뷰·brainstorming 행 한쪽에만.
- **고아 라우터 동작**(근원): `rein-route-record.py` 의 ID 검증이 `.claude/agents|skills` 스캔하나 **Option C Phase 3 에서 폐기돼 존재 안 함**. agent 는 빈집합 skip 이나 **skill 은 항상 검증 타서 전부 invalid 로 기록되는 잠재 버그**(2026-05-22 feedback-log 에 실발생). `learn` 은 `registry.yaml` 부재로 못 돌고, 어떤 훅/CI 도 호출 안 함(고아). 이름 namespaced(문서) vs unnamespaced(frontmatter) 불일치.
- codex-ask(gpt-5.5/medium, Mode B) 가 질문 전제 4건 중 2건 교정 + 빠진 근본 질문("학습기능이 제품기능인가 감사로그면 충분한가") 지적.

## 확정 설계 (Option A — 사용자 3-결정)

1. 학습 루프 = **감사 로그로 축소** (`cmd_learn`+registry 제거, override/feedback 유지)
2. ID 검증 **제거** (스크립트는 schema/format 만 — SSOT=세션목록인데 스크립트는 못 봄; 검증의 옳은 위치는 LLM 라우팅 시점). stale 테스트 정리.
3. routing-map = routing-procedure §5 의 **압축 projection** 명시
4. namespaced canonical 1줄 + 유형 우선순위·지배적 판정 기준 1줄씩

## 산출물 (dev `08e1313`)

- brainstorm `docs/brainstorms/2026-06-04-routing-def-consistency.md` (codex-ask sanity PASS)
- spec `docs/specs/2026-06-04-routing-def-consistency.md` (RD-1~RD-12, OQ-1~7 해소, sonnet fallback PASS)
- plan `docs/plans/2026-06-04-routing-def-consistency-implementation.md` (coverage/schedule EXIT 0, sonnet fallback PASS)
- codex usage-limit 로 spec/plan 리뷰 둘 다 sonnet fallback(stamp 에 fallback_reason 기재).

## ⚠️ 구현 첫 단계 = spec 재리뷰 (중요)

plan-writer 가 coverage validator 의 Scope ID 추출을 위해 spec 을 정규화(`## 3. Scope Items`→`## Scope Items`, `**RD-N**`→`RD-N`, 내용 불변)했고, 이것이 spec 리뷰 표식을 무효화 → 현재 `trail/dod/.spec-reviews/363e1a36fb99ff62.pending`(.reviewed 없음). **재리뷰+stamp 안 하면 pre-edit-dod-gate 가 구현 편집 차단**. 정규화는 cosmetic 이나 게이트는 모름 → 구현 진입 전 현재 spec 재리뷰(codex 회복 시 정식, 아니면 sonnet fallback) 후 `rein-mark-spec-reviewed.sh` 로 stamp.

## 웨이브 (parallel-execute 대상)

- step1 (mutating 단독): script-edit — `scripts/rein-route-record.py` + `plugins/rein-core/scripts/rein-route-record.py` byte-identical 동시 + sha256/회귀 검증
- step2 (edit_only 병렬): routing-map.md + routing-procedure.md (disjoint)
- step3 (mutating 단독): tests — stale 삭제 + drift-guard 신규 + run-all 등록

## 다음

ROUTE-DOC-1 구현: spec 재리뷰 → DoD(라우팅 추천 승인) → parallel-execute 웨이브 → 통합 코드+보안 리뷰 → 커밋. 그 후 ROUTE-BIND-1+ROUTE-DOC-1 묶어 push/main 릴리스 결정.


---
## 2026-06-05
# Daily Summary: 2026-06-05

---
<!-- source: 2026-06-05-onboard-1.md -->
## onboard-1

### 완료 기록

- 날짜: 2026-06-05
- DoD: dod-2026-06-05-onboard-1-first-session-primer.md
- 설계: brainstorm/spec/plan = `docs/{brainstorms,specs,plans}/2026-06-05-onboarding-first-session-primer.md`
- 커밋: dev (ONBOARD-1, 미push·미main)

## 한 일 (Option A 프라이머 + Option C teach-forward, 핵심 범위)

1. **첫 세션 프라이머 (1회)** — `.rein/.onboarded` 마커 부재로 첫 세션 감지. 두 채널 emit: 사용자 stdout(`session-start-bootstrap.sh`, 읽기만) + 에이전트 additionalContext(`session-start-rules.sh`, 6규칙 앞 prepend). **단일 writer = rules**(마지막 SessionStart 훅) — bootstrap 은 마커 안 씀 → race 없음. 마커 helper `hooks/lib/onboarded-check.sh` 신규(`rein_is_onboarded`/`rein_mark_onboarded`/`rein_primer_body`). `.gitignore` 에 `/.rein/.onboarded` 추가(현행 .gitignore 가 .rein/ 전체 제외 아님). 프라이머 = 1문단 + 3 흐름(작업 기준서→사용자 승인→리뷰 완료) + "막히는 건 정상", 내부용어 비노출. 세 부트스트랩 경로(rc=10 자동 / 수동 / rc=0 기존사용자 backfill) 공통.
2. **핵심 3 게이트 teach-forward** — `pre-edit-dod-gate.sh` 의 DoD 부재 / 라우팅 승인 / 미리뷰 설계 차단 메시지를 "막힌 이유 + 다음 2단계"로 보강. 라우팅 승인에 DoD 승인 줄 형식 힌트(공유 regex 정합) 인라인. **차단 조건·exit 2·bypass 분기 불변**.
3. **회귀 테스트 2 신규** — `test-onboarding-primer.sh`(첫/둘째 세션·두 채널·hooks.json SessionStart bootstrap<rules 순서 단언·backfill·hot-path 0) + `test-teach-forward-gates.sh`(3 게이트 next-step + exit 2 보존 + 형식 힌트 regex 정합). run-all 등록. 기존 bootstrap 테스트 3종 fixture 에 `.onboarded` seed(rc=0 silence 단언이 backfill 분리로 보존되도록 — 회귀 은폐 아님).

## 검증

- 설계 체인 전부 PASS: brainstorm(codex Step 0 통과) → spec(codex R3 PASS, 표 정합 후 sonnet 폴백 R1 PASS+표식) → plan(sonnet 폴백 R2 PASS+표식). codex 는 spec R4/plan/code 단계에서 usage-limit(11:25 리셋) → sonnet 폴백 경로.
- 구현: feature-builder implementer 1회(F-1/F-2 삽입 위치 정확). 코드 리뷰 = code-reviewer-rein(sonnet 폴백, codex_usage_limit) PASS. 보안 = security-reviewer(standard) PASS. `bash tests/hooks/run-all.sh` ALL SUITES PASSED.

## 진행 중 발견 (별건 — ROUTE-DOC-1 회귀)

- `tests/hooks/test-routing-map-emit.sh` 가 routing-map.md **≤800B** 를 강제하는데 ROUTE-DOC-1 이 875B 로 키워 **FAIL**. 이 테스트는 run-all/CI 미등록(orphan)이라 ROUTE-DOC-1 suite 가 못 잡음. ROUTE-DOC-1 의 새 drift-guard(`test-routing-map-projection.sh`)는 ≤900B 라 두 예산 가드 충돌. **후속 = 예산 900B 로 일원화**(ROUTE-DOC-1 의 reviewed 결정) — 별 커밋으로 처리 예정.

## 다음

ROUTE-DOC-1 routing-map 예산 회귀 정리 → 자동모드 종료 + incident 정리 → (사용자 결정 시) push/릴리스.

---
<!-- source: 2026-06-05-plan-writer-git-snapshot.md -->
## plan-writer-git-snapshot

### 완료 기록

Plan complete: docs/plans/2026-06-05-session-state-git-snapshot.md
- Scope IDs covered: [GS-1..GS-8] (8 implemented / 0 deferred)
- Coverage validator: exit 0
- Exec-strategy validator (plan subcommand): exit 0
- Schedule: step1 [gs1-lib gs5-gitignore gs6-rules] / step2 [gs23-session-start gs24-stop] / step3 [gs7-tests]
- .coverage-mismatch marker: 없음

Spec review: NEEDS-FIX (codex gpt-5.5 / high)
Stamp: 생성 안 됨 (.spec-reviews/62cf359a9548e518.pending 유지 — 해당 plan)
  주: .spec-reviews/6d18f84a8bf868c9.reviewed 는 design(spec) doc 의 stamp (별건, codex 이미 PASS)
  코드리뷰 게이트(.codex-reviewed / .review-pending) 건드리지 않음 — spec-review 서브플로우 정상

NEEDS-FIX 사유 (Medium 1건, 나머지 GS-1..GS-8 전부 MATCH):
  Phase 1/2 의 Task 단계가 `tests/hooks/test-git-snapshot.sh` 에 대해 "실패 테스트 먼저 작성/실행"
  을 지시하나, 실행 전략 scope 는 그 테스트 파일을 wave3 의 gs7-tests 에만 배정.
  → 동시 실행 가능한 edit_only 워커(gs1-lib/gs23/gs24)가 scope 밖 테스트 파일을 쓰거나
  필수 단계를 건너뛰어야 하는 모호성. (line 61/85/117/134 vs 217)
  코덱스 권고: 모든 red-test 작성/실행 지시를 gs7-tests 로 일원화하거나, 실행전략 scope 를
  그에 맞게 조정.
  나머지: line anchor 유효(158/251-266/346), wave 위상·disjoint·claim audit 전부 통과.

Next (수동 개입 경로):
  (1) 위 Medium 을 plan 에 반영 (TDD red-test 지시를 gs7-tests 로 일원화 — Phase 1/2 Task 의
      "실패 테스트 먼저" 문구 제거, 구현 완료 후 wave3 에서 테스트 작성·실행)
  (2) coverage + exec-strategy validator 재실행 (exit 0 확인)
  (3) 수동 /codex-review 호출 또는 plan-writer 재실행

---
<!-- source: 2026-06-05-primer-friendlier-copy.md -->
## primer-friendlier-copy

### DoD

- 날짜: 2026-06-05
- 관련: ONBOARD-1 (dev f35d6c9), 사용자 요청(공용 문구 따뜻화 + 3줄)

## 목표

ONBOARD-1 첫 세션 안내문이 다소 사무적이고 길다는 사용자 피드백 → 사람·에이전트 공용 문구(단일 정의)를 **따뜻한 톤 3줄**로 교체. 사람 화면과 에이전트 컨텍스트 양쪽에 동일 반영(단일 정의 유지).

## 완료 기준 (acceptance)

1. `plugins/rein-core/hooks/lib/onboarded-check.sh` 의 `rein_primer_body` heredoc 을 아래 3줄로 교체:
   - 1줄: 환영("처음 오셨네요, 반갑습니다 👋 …첫 세션이라 짧게 안내드릴게요.")
   - 2줄: 핵심 흐름 인라인("코드가 들어오기 전에 "무엇을 할지 정하기 → 당신의 승인 → 리뷰 통과"를 곁에서 도와드려요.")
   - 3줄: 막힘 안심("가다가 막혀도 괜찮아요 — 그때마다 다음에 뭘 하면 되는지 바로 알려드릴게요.")
2. 내부용어 비노출 유지, 단일 정의 유지(양 채널 동일).
3. `tests/hooks/test-onboarding-primer.sh` 의 프라이머 anchor 3종을 새 문구에 맞게 갱신(para/flow/stuck). 양 채널 byte-identical 추출(para→stuck) 동작 보존, 11단계 게이트 지도 미포함 negative anchor 유지.
4. `bash tests/hooks/run-all.sh` 전체 통과(프라이머 회귀 + 인접 무회귀).
5. 코드 리뷰 1회 통과 후 커밋.

## 라우팅 추천

```yaml
agent: rein:feature-builder
skills:
  - rein:codex-review
mcps: []
security_tier: light
rationale: >
  사용자-facing 안내 문구 1개 + 그 회귀 테스트 anchor 갱신만. 로직/제어흐름 변화
  없음, secret/auth/network 표면 없음 → light. 규모 작아 메인 세션 직접 구현 + codex 1회.
approved_by_user: true
```

## 변경 파일

- plugins/rein-core/hooks/lib/onboarded-check.sh
- tests/hooks/test-onboarding-primer.sh

### 완료 기록

- 날짜: 2026-06-05
- DoD: dod-2026-06-05-primer-friendlier-copy.md
- 커밋: dev (미push)

## 배경/한 일

ONBOARD-1 첫 세션 안내문이 사무적이고 길다는 사용자 피드백 → 공용 단일 정의(`rein_primer_body`)를 따뜻한 3줄로 교체:
1. 환영("처음 오셨네요, 반갑습니다 👋 …")
2. 핵심 흐름 인라인("코드가 들어오기 전에 \"무엇을 할지 정하기 → 당신의 승인 → 리뷰 통과\"…")
3. 막힘 안심("가다가 막혀도 괜찮아요 — …다음에 뭘 하면 되는지 바로 알려드릴게요.")

사람 화면·에이전트 컨텍스트 동일(단일 정의 유지). `test-onboarding-primer.sh` 의 anchor 3종을 새 문구로 갱신(byte-identical 추출·negative anchor 보존).

## 검증
- test-onboarding-primer.sh PASS, `bash tests/hooks/run-all.sh` ALL SUITES PASSED.
- codex 코드 리뷰 PASS(real codex), 보안 light(text-only).

## 다음
dev 미push 5건(ROUTE-DOC-1 / ONBOARD-1 / 재리뷰기록 / 예산일원화 / 프라이머 문구). push/릴리스 사용자 결정.

---
<!-- source: 2026-06-05-route-doc-1.md -->
## route-doc-1

### 완료 기록

- 날짜: 2026-06-05
- DoD: dod-2026-06-05-route-doc-1-routing-def-consistency.md
- plan: docs/plans/2026-06-04-routing-def-consistency-implementation.md
- 커밋: dev `825f307` (미push, 미main)

## 한 일

plugin-first 전환 후 고아가 된 라우터 학습/검증 동작 제거 (Option A — 정직한 축소):

1. **rein-route-record.py (+ plugin 미러 byte-identical)** — `learn` 명령·`LEARN_*` 상수 5개·`_registry_path`·`_load_known_ids`/`_read_frontmatter_name`/`_split_valid_invalid`·`_atomic_write`(미사용) 제거. `cmd_feedback` 의 ID 검증·`invalid_ids` schema 제거 → 전달값 직기록. outcome enum + 대상 yaml 존재 검증(exit 2)은 보존. docstring/import 정합(`date` 만 잔존). (RD-1~RD-6)
2. **routing-map.md** — §5 압축 projection 명시 1줄 + 라벨 §5 정렬(`새 기능`→`새 기능 추가`, `문서`→`문서 작성`). 875B (≤900B). (RD-7)
3. **routing-procedure.md** — §5-A 유형 우선순위(spec/plan>review>구현>research>docs) + 지배 판정(acceptance 산출물 기준) + §1 namespaced canonical 1줄. (RD-8/RD-9)
4. **테스트** — stale `test-route-record-validation.sh` 제거(제거된 디스크 스캔/invalid_ids 검증분) + drift-guard `test-routing-map-projection.sh` 신규(map ⊆ §5 라벨 subset + 바이트 상한 + **추출 공집합 false-PASS 가드**) + run-all 등록. (RD-10~RD-12)

## 검증

- codex 코드 리뷰: R1 NEEDS-FIX(Medium 1건 — drift-guard 가 표 구조 변경으로 추출 0건일 때 subset 검사가 공허하게 참 → false PASS) → 추출 성공 가드 추가 후 R2 **PASS**.
- 전체 suite `ALL SUITES PASSED`, 인접 route-record 테스트 무변경 PASS, plugin drift checker OK, 두 사본 sha256 일치(11 helpers).
- drift 3종 모두 FAIL 확인: parse(헤더 rename), subset(map-only 라벨), bytes(행 추가).
- 보안: light tier(디스크 스캔 코드 *제거* = 표면 축소, markdown/bash, secret/auth/network 없음).

## 메모 — 진행 중 발견

- codex 리뷰 wrapper 의 context 조립이 `advisory-latest-mtime` 로 active DoD 를 골라 ROUTE-BIND-1 DoD(SC-1~10)를 주입함 — `.active-dod` 마커(ROUTE-DOC-1 가리킴)와 불일치. codex 가 실제 worktree diff(RD-*)로 정정 리뷰해 결과는 정상이나, stamp 의 `cycle`/`active_dod` 필드가 ROUTE-BIND-1 로 라벨됨. **index/marker 신선도 테마(need-to-confirm 2026.6.4 항목)의 한 인스턴스** — 별도 추적.

## 다음

ONBOARD-1 (신규 설치 첫 세션 게이트 시행착오) 착수 — 사용자 지시(오토모드 자율 진행).

---
<!-- source: 2026-06-05-routing-map-budget-unify.md -->
## routing-map-budget-unify

### DoD

- 날짜: 2026-06-05
- plan ref: (없음 — 소규모 정합성 수정, 단일 파일군 test-only)
- 관련: ROUTE-DOC-1 (dev 825f307), need-to-confirm.md 진입점

## 목표

ROUTE-DOC-1 이 routing-map.md 를 875B 로 키우며 예산 상한을 900B 로 확정했으나, 옛 테스트 `tests/hooks/test-routing-map-emit.sh` 가 여전히 ≤800B 를 강제해 두 예산 가드(800 vs 900)가 충돌한다. 예산을 900B 로 일원화하고, 이 orphan 테스트(어느 run-all/CI 에도 미등록)를 hooks 러너에 등록해 실제로 돌게 한다.

## 완료 기준 (acceptance)

1. `tests/hooks/test-routing-map-emit.sh` 바이트 상한 `800` → `900` (주석 포함), ROUTE-DOC-1 예산 결정 근거 1줄 명시.
2. 동 테스트가 ONBOARD-1 프라이머 간섭 없이 결정적으로 돌도록 `.rein/.onboarded` 마커를 hook 실행 전 seed (프라이머 prepend 회피 — 이 테스트의 관심사 = routing-map emit/순서/바이트). seed 근거 주석 포함.
3. `tests/hooks/run-all.sh` 에 `test-routing-map-emit.sh` 등록.
4. 등록 후 `bash tests/hooks/run-all.sh` 전체 통과 (신규 등록분 + 기존 무회귀).
5. routing-map.md 자체는 변경하지 않음 (875B 유지). projection drift-guard(`tests/scripts/test-routing-map-projection.sh`, ≤900B)와 예산 일치 확인.
6. 코드 리뷰 1회 통과 후 커밋.

## 라우팅 추천

```yaml
agent: rein:feature-builder-fix
skills:
  - rein:codex-review
mcps: []
security_tier: light
rationale: >
  ROUTE-DOC-1 이 남긴 예산 가드 불일치(회귀) 정리 + orphan 테스트 de-orphan.
  test-only 변경(테스트 임계값 + 마커 seed + 러너 등록), 소스 로직/사용자 표면
  변화 없음. secret/auth/network 표면 없어 light. 규모가 작아 메인 세션 직접 구현,
  codex 리뷰 1회.
approved_by_user: true
```

## 변경 파일

- tests/hooks/test-routing-map-emit.sh
- tests/hooks/run-all.sh

### 완료 기록

- 날짜: 2026-06-05
- DoD: dod-2026-06-05-routing-map-budget-unify.md
- 커밋: dev (미push)

## 배경

ROUTE-DOC-1 이 routing-map.md 를 782B→875B 로 키우며 예산 상한을 900B 로 확정했으나, 옛 테스트 `tests/hooks/test-routing-map-emit.sh` 가 ≤800B 를 강제해 두 예산 가드(800 vs 900)가 충돌. 이 테스트는 어느 run-all/CI 에도 미등록(orphan)이라 ROUTE-DOC-1 suite 가 못 잡음.

## 한 일

1. `test-routing-map-emit.sh` 바이트 상한 `800` → `900` (주석·FAIL·PASS 메시지 동기화). `tests/scripts/test-routing-map-projection.sh`(MAX_BYTES=900)와 lockstep. routing-map.md 자체는 875B 무변경.
2. 동 테스트에 `.rein/.onboarded` 마커 seed 추가 — ONBOARD-1 의 session-start-rules.sh 가 첫 세션(마커 부재)에 프라이머를 prepend/마커 write 하므로, 이 테스트(emit 순서/바이트 관심사)를 결정적으로 만들기 위해 프라이머 비활성화. 마커는 gitignored 런타임 데이터, 프라이머 검증은 test-onboarding-primer.sh 담당.
3. `tests/hooks/run-all.sh` 에 등록 — de-orphan. 이제 hooks suite/CI 에서 실제로 돈다.

## 검증

- 표준 실행 PASS(byte<=900, marker present, code-style precedes routing-map), `bash tests/hooks/run-all.sh` ALL SUITES PASSED + emit 테스트 실제 실행 확인.
- codex 코드 리뷰 PASS(real codex, low advisory 1건: seed write 가 `|| true` 라 실패 시 테스트가 직접 fail 안 함 — writable CI 에서 허용 범위로 수용). 보안 light(test-only).

## 결과

routing-map 예산 가드가 900B 로 단일화되고 orphan 테스트가 CI 에 편입됨. ROUTE-DOC-1 잔여 회귀 해소.

## 다음

ROUTE-DOC-1 + ONBOARD-1 + 본 후속 = dev 3건(+trail). main 머지/push/릴리스 여부는 사용자 결정.

---
<!-- source: 2026-06-05-session-state-git-snapshot.md -->
## session-state-git-snapshot

### DoD

- 날짜: 2026-06-05
- plan ref: docs/plans/2026-06-05-session-state-git-snapshot.md
- design ref: docs/specs/2026-06-05-session-state-git-snapshot.md (검토 통과)
- brainstorm ref: docs/brainstorms/2026-06-05-session-state-git-snapshot.md

## 목표

세션 상태 파일 `trail/index.md` 의 git/릴리스 서술이 손글씨라 실제 git 과 어긋나는 신선도 오류 클래스를 제거한다. 훅이 git 객관 사실(branch / HEAD short sha / dirty·clean / ahead·behind / 최신 태그 / UTC 타임스탬프)을 `.rein/state/git-snapshot.md`(gitignored)에 매 세션 자동 생성하고, SessionStart 가 index 블록 직후 주입한다. index 는 서술 전용으로 남기고, 충돌 시 자동 스냅샷(궁극적으로 살아있는 git)이 권위본임을 규칙으로 명문화하며, 손글씨 휘발 git 수치가 잔존하면 Stop 이 비차단 advisory 로 경고한다.

## 완료 기준 (acceptance)

1. 신규 공유 lib `plugins/rein-core/hooks/lib/git-snapshot.sh` — `rein_write_git_snapshot <project_dir>` 가 6필드를 `.rein/state/git-snapshot.md` 에 tmp→mv 원자 재생성. fresh-write-or-clear(실패/non-git 시 stale 파일 rm -f). detached HEAD 라벨 + ahead/behind 생략. 최신 태그 "로컬 최근 생성, 최고 semver 아님" 라벨. 네트워크 명령 0건. 항상 비차단(return 0). (GS-1)
2. SessionStart·Stop 양쪽이 lib source 후 호출. Stop 은 no-source-edit exit(stop-session-gate.sh:158) **이전**에 bootstrap-complete 가드 하 호출(push/릴리스만 한 세션도 갱신). SessionStart 는 emit_file_block "trail/index.md"(line 346) 직후. (GS-2)
3. SessionStart 가 index 블록 직후 git-snapshot 을 별도 블록으로 주입 — **파일 존재 시에만**(fresh-write-or-clear 로 stale 주입 불가). early-exit 경로 미주입. (GS-3)
4. Stop 좁은 비차단 advisory — index 서술의 손글씨 휘발 git 수치(앵커 정규식 목록, bare dirty/clean 금지) 잔존 시 stderr 1줄. 의미 서술 미발화(음성). exit code / block JSON 불변. post-158 경로, 신선도검사 이후. (GS-4)
5. `.gitignore` 에 `/.rein/state/` 추가 + `.claude/rules/branch-strategy.md` 에 `.rein/state/` main 제외 + mirror strip 명시. (GS-5)
6. 권위 규칙 1줄을 `operating-sequence.md` step 11 + `trail/index.md` 헤더 노트 양쪽 배치. (GS-6)
7. 회귀 테스트 신규 `tests/hooks/test-git-snapshot.sh` — assert (a)~(h) + codex Low(index 없고 trail 만 있는 경우) + Stop High(no-source-edit mtime 갱신). `tests/hooks/run-all.sh` 등재. 전체 스위트 green. (GS-7)
8. 버전 등급 단일 권고 = minor 기록(main 머지 직전 메인테이너 확정). (GS-8)
9. 통합 코드 리뷰(codex) + 보안 리뷰 1회 통과 후 커밋.

## 라우팅 추천

```yaml
agent: rein:feature-builder
skills:
  - rein:parallel-execute   # plan 의 v2 실행 전략(웨이브 1: lib+gitignore+rules / 웨이브 2: 두 훅 / 웨이브 3: 테스트) 기반
  - rein:codex-review       # 전체 변경분 통합 리뷰 (완료 전 1회)
mcps: []
security_tier: standard     # 세션 거버넌스 훅(session-start-load-trail, stop-session-gate) 확장 + git 명령 실행 결과를 세션 컨텍스트에 주입하는 신규 lib. secret/auth/network 표면 없음(로컬 git 읽기 전용)이나 gate-touching + 새 데이터 흐름이라 보수적으로 standard tier 보안 리뷰 1회.
rationale: >
  git 객관 사실 자동 스냅샷(신규 기능)이므로 feature-builder. plan 이 파일소유권 기준
  6 worker scope(웨이브 1 disjoint 3개 / 웨이브 2 두 훅 / 웨이브 3 테스트)를 산출 —
  parallel-execute 로 실행 전략을 태우고 부모(메인 세션)가 웨이브 단위로 bash -n/정적
  grep 스모크 검증·커밋, 회귀 스위트는 웨이브 3 일괄. codex-review 로 전체 통합 리뷰 1회.
approved_by_user: true
```

## 변경 파일

- plugins/rein-core/hooks/lib/git-snapshot.sh
- plugins/rein-core/hooks/session-start-load-trail.sh
- plugins/rein-core/hooks/stop-session-gate.sh
- .gitignore
- .claude/rules/branch-strategy.md
- plugins/rein-core/rules/operating-sequence.md
- trail/index.md
- tests/hooks/test-git-snapshot.sh
- tests/hooks/run-all.sh

### 완료 기록

- 날짜: 2026-06-05
- 유형: feature (brownfield, 훅 2 확장 + 신규 lib + 규칙/문서)
- DoD: trail/dod/dod-2026-06-05-session-state-git-snapshot.md
- 설계 체인: brainstorm → spec(codex PASS) → plan(codex PASS) 전부 표식 생성

## 무엇을 / 왜

세션 종료 시 index.md 의 git/릴리스 서술이 손글씨라 실제 git 과 어긋나는 신선도 오류(실측: "미push 5건" vs 실제 ahead 6)를 클래스 단위로 제거. git 객관 사실을 훅이 직접 도출해 자동 기록하고, 손으로 쓰는 건 의미 서술만 남김.

## 구현 (GS-1~GS-8)

- **GS-1** 신규 lib `plugins/rein-core/hooks/lib/git-snapshot.sh` — `rein_write_git_snapshot` 가 6필드(branch[detached 라벨]/HEAD short/dirty·clean+수/ahead·behind[로컬 추적, 네트워크 없음]/최신 태그[creatordate, describe 아님]/UTC)를 `.rein/state/git-snapshot.md` 에 tmp→mv 원자 기록. fresh-write-or-clear(non-git/unborn HEAD/실패 시 stale rm -f). BC-INFO1 env 위생(`_rein_git_clean`). 항상 return 0.
- **GS-2/3** SessionStart(`session-start-load-trail.sh`): lib source(fail-open) + index 블록 직후 재생성·주입(파일 존재 시만, `~~~` 펜스).
- **GS-2/4** Stop(`stop-session-gate.sh`): no-source-edit exit(line 158) **이전** bootstrap-complete 가드 하 재생성(push/릴리스만 한 세션도 갱신) + 신선도검사 이후 좁은 비차단 advisory(앵커 정규식, bare dirty/clean 미매칭).
- **GS-5** `.gitignore` `/.rein/state/` + `branch-strategy.md` 제외 명시.
- **GS-6** 권위 규칙 1줄: `operating-sequence.md` step 11 + `index.md` 헤더.
- **GS-7** 회귀 `tests/hooks/test-git-snapshot.sh` (a~i + g2 + index-less + no-src-edit) 17 assert + run-all 등재.
- **GS-8** 버전 등급 권고 = minor (main 머지 직전 확정).

## 리뷰 (전 라운드 PASS)

- 설계: spec codex R2 PASS, plan codex R2 PASS (각 author 보완 후 재리뷰).
- 코드 codex R1 NEEDS-FIX(3건: GIT_* env 위생 High / 빈-repo Medium / 테스트 mtime Low) → 보완 → R2 PASS.
- 보안(standard) NEEDS-FIX(프롬프트 주입: ref 이름 백틱이 펜스 파손) → 보완(백틱 무력화 3필드 + `~~~` 펜스) → 재검토 PASS.
- 보안 fix 로 코드 변경 → codex R3 PASS.

## 진행 중 발견 (별건)

- **테스트 하네스 거짓-통과 버그**: 서브셸 안 `fail()` 카운터 미전파로 실패해도 ALL PASSED 되던 결함을 발견·수정(파일 기반 FAILFILE 카운터). 강제-실패 sanity 로 rc=1 확인. 이 과정에서 보안 fix 의 ab_line 백틱 누수도 잡음.
- **codex wrapper active-DoD 오라벨**: `.codex-reviewed` 표식의 cycle/active_dod 가 route-bind-1 로 잘못 찍힘(`.active-dod` 는 올바름). verdict PASS·diff_base 는 정상. index/marker 신선도 테마의 별건 wrapper 버그 — 본 작업 범위 밖, 추적 유지.

## 상태

dev 커밋(미push·미main). push/릴리스는 사용자 결정 대기.


