# 2026-08-27 — 최행배 페르소나 추가 + v2 청산 후속 게이트 잔재 정리

## 완료 (커밋 2건, dev, 미푸시)

### 1. 최행배 페르소나 배포본 기본 프리셋 추가 — `7d7e181`

부산 사투리 승부사 "최행배"(행님 호칭)를 세 번째 내장 페르소나로 추가.

- **핵심 발견**: "프롬프트 텍스트만"이 아니었다. 내장 프리셋 이름이 **8곳에 복제** — 로더 등록 집합(plugin+root 2사본 `KNOWN_PERSONA_PRESETS`), lint 집합(`BUILTIN_PRESETS`, 로더와 동기화 계약), 스킬 안내문 메뉴/열거 4곳, 부트스트랩 persona.yaml 템플릿 주석, 테스트 6곳. 하나라도 빠지면 프리셋이 조용히 기본값으로 되돌려지거나 테스트가 깨진다. 전수 grep 으로 복제 지점 확정 후 전부 동기화.
- **바이트 예산**: 원본(1701B)이 프리셋 크기 상한(1536B)을 초과 → jennie 수준으로 압축(1531B). 사용자 승인 A안(상한 완화 대신 파일 압축).
- byte-budget 테스트 (b2) 축이 새 최대 프리셋을 검사하도록 확장.
- 리뷰: codex PASS(1회차) + 보안 PASS + 증거 발급 + 커밋 완주.

### 2. v2 청산 후속 게이트 잔재 정리 — `eaf14c6`

게이트 대장 "비차단 백로그" 5건 중 3건 정리, 2건 취소/이월.

- **1번(문구 2곳)**: 삭제된 옛 훅 이름이 남은 stderr 안내문(`spec-review-gate.sh`) + 우회 로그 라벨(`incident-review-gate.sh`) 교정. 역사 서술 주석은 무변경.
- **2-b(죽은 가드)**: `REIN_GATE_SOURCE_ONLY` 조기종료 가드 — 실행 소비자 0건 전수 확인 후 제거(사용자 승인). 보안 리뷰: 잠재 fail-open 우회 스위치 소멸로 오히려 표면 축소.
- **2-c(빈 경로 메시지)**: `pre-bash-commit-discipline-gate.sh` 오류메시지의 미정의 `$DOD_DIR` → 정의 추가 + **T19 회귀 테스트**(rename 실패 결정론적 유도 + 경로 특정 검증, 양방향 실증: 정상 통과 / 정의 무력화 시 실패).
- **2-a 취소 (non-issue)**: 백로그는 "코드리뷰 위임의 REIN_POLICY_DIR 상속 미차단"이라 했으나, 조사 결과 다른 축이 인라인 env prefix 로만 주입(export 안 함)해 상속 경로가 없고, **기존 테스트가 이미 "code-review는 그 변수 미참조"를 계약으로 가드**하고 있었다. env -u 추가가 그 계약을 깨서 되돌림. 백로그 진단 과대.
- **3번 이월**: 래퍼 발급 실패 종료값 정직화는 wrapper 종료코드 계약 확장(신 코드+문서+사본+테스트)이라 잔재정리 스코프 초과 → 별도 "래퍼 계약" 사이클.
- 리뷰: codex 2회차 PASS(1회차 Medium=회귀 테스트 누락 → T19 로 해소) + 보안 PASS.

## 교훈

- **백로그를 액면대로 착수하지 말 것** — 2-a 는 기존 테스트가 이미 올바른 계약을 가드하고 있었다. 신설 전 기존 계약/테스트 확인 필수.
- **차단 로그 자기증식 고리**(백로그 등재): coverage 오탐 incident 를 종료했더니 그 차단 자체가 다시 "incident review pending"으로 재집계 → 새 incident. 이번에 재집계 소스가 99건 쌓여 있었다.

## 백로그 (별도 사이클 — 게이트 대장 후속 항목 절 참조)

1. codex 가 발견한 `/codex-review` 2분 하니스 타임아웃 버그 (`docs/reports/2026-08-27-codex-review-harness-timeout.md`).
2. 차단 로그 → incident 자기증식 고리.
3. 래퍼 발급 실패 종료값 (3번 이월).
4. 계획 없는 직접 DoD 에서 coverage 검사가 혼란스럽게 막는 점 (안내문이 plan 부재를 구분 못 함).
