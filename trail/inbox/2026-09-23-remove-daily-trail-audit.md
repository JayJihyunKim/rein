# 2026-09-23 — 죽은 일일 trail 점검 워크플로 제거 (완료)

- DoD: `trail/dod/dod-2026-09-23-remove-daily-trail-audit.md` (승인 2026-09-23 "지우자")
- 오케스트레이션: 단독 처리 — 파일 3개(삭제 1·줄 제거 1·표 한 칸), 워커 불필요
- 커밋: dev (본 기록의 trail 커밋 직전 커밋 `chore(ci): 죽은 일일 trail 점검 워크플로 제거 …`)

## 배경

사용자 질문 "daily trail audit 이 왜 돌아가는가" → 2026-04-16 SOT→trail 리네임 때 들어온 메인테이너용 스케줄(매일 18:00 UTC, 기본 브랜치 main 에서 실행, 공개 미러에서는 strip). 읽기 전용 경고 3종(index 20줄 초과 / inbox 3일 초과 / 24시간 내 사고 기록)만 출력. **작동 불능 근거**: `find -mtime` 검사는 CI 체크아웃에서 모든 파일의 수정 시각이 체크아웃 시각이라 "3일 초과" 는 절대, "24시간 내" 는 전부 잡힘; index 줄 수는 훅(세션 종료 게이트 5~25줄)이 대체; 기준(20줄)도 낡음; 출력 소비자 없음. 최근 실행 실패는 스크립트가 아니라 **GitHub Actions 과금("최근 결제 실패 또는 지출 한도")** — 모든 워크플로에 걸리는 별개 문제(사용자 계정 설정).

## 한 것

- `.github/workflows/daily-trail-audit.yml` 삭제(42줄; permissions·secrets·외부 호출 없던 파일 — 공격면 소폭 감소).
- `.github/workflows/mirror-to-public.yml`: 그 파일을 strip 하던 `if [ -f … ]` 블록 7줄 제거(strip 블록 8→7). 다른 블록·순서·조건 무변경.
- `.claude/rules/branch-strategy.md`: main 포함 표 행에서 파일명 제거 + 제거 사유 주석(파일명 없이).

## 검증

- 기준서 검증 기준 5항목 통과(추적 항목 0, 미러·규칙 문서 참조 0, YAML 파싱, strip 블록 8→7).
- 미러 strip 단계를 스테이징 트리 복사본 임시 저장소에서 실제 실행(원격 접속 직전까지): 정리 커밋 9개, 남는 민감 경로는 공개용 `issue-triage.yml` 뿐, 플러그인 파일 324개 보존. 이 하니스는 릴리스의 로컬 미러 절차로 재사용.

## 리뷰

- 코드 리뷰 1회차(산출 medium, 463초): High 1 — 규칙 문서 주석에 삭제 파일명을 다시 적어 기준서 검증 기준("그 문서에 파일명 없음")에 위배 → 파일명 없는 문구로 수정. 2회차(후속 양식, 직전 High 라 규칙대로 high 마커, 568초): 통과·기록 발급.
- 보안 리뷰(standard): 통과·기록 발급. 참고 1 — main 선별 반영 때 미러 변경과 삭제를 같은 커밋에 넣을 것(main 에 파일이 남으면 한 번의 미러에서 공개로 흘러감). 이번 릴리스는 미러를 로컬에서 수행하므로 그 절차에서 함께 확인.

## 후속 후보

- `repo-audit.yml`·`weekly-agent-evolution.yml` 도 같은 기준(소비자·작동 여부)으로 점검.
