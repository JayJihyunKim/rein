# 2026-09-21 — 메인 세션 지휘 기본화 구현 2/4: 워커 병렬 1단계

## 한 것

계획 `docs/plans/2026-09-21-orchestrator-first-default.md` 의 Phase 1(Task 1.1~1.4)을 편집 전용 워커 4개로 같은 작업 트리에서 동시에 실행했고, 메인 세션이 끝에서 한 번에 점검·테스트·리뷰·커밋했다.

- `flag-loader`: `plugins/rein-core/scripts/rein-policy-loader.py`(+ 루트 미러)에 `get_rule_enabled()` 와 CLI 모드 `--rule-enabled <rule-name>`. 모든 오류 경로가 비활성으로 귀결(fail-closed — 기존 override 읽기의 fail-open 과 의도적으로 반대). 신규 `tests/hooks/test-policy-rules-enabled-flag.sh`, 실패하는 테스트 먼저.
- `rule-pair`: `plugins/rein-core/rules/orchestrator-first.md`(9,337B) + `rules/short/orchestrator-first-summary.md`(648B, 이스케이프 후 참고 길이 1,127자) 신설. 실행 규율·워커 선택 네 가지는 `agents/orchestrator.md` 의 anchor 를 가리키기만 하고 본문을 옮겨 적지 않았다.
- `cross-refs`: `routing-map.md` 각주(770B→939B) + 상한 1100, `operating-sequence.md` Step 4 셀, `routing-procedure.md` §6 의 선택 필드 두 개와 세 필드 경계표.
- `asset-redefine`: `agents/orchestrator.md` 의 description 과 depth-rule 서문 두 지점만, 강등 테스트는 docstring 만.

워커 모델: 규칙 본문을 쓰는 `rule-pair` 는 세션 모델 그대로, 나머지 셋은 가벼운 모델 — 지휘자 판단.

## 점검 (부모)

- 시작 시점 기준선 대비 변경 경로 12개 = 선언 쓰기 범위 12개. 범위 밖 0, 변경 없는 태스크 0, 워커의 커밋·스테이징 없음.
- 계획 Task 1.1~1.4 확인 명령 전부 기대값. 새 테스트는 계획 전문과 diff 로 동일, 로더 미러 cmp 동일, 지휘자 계약 테스트 30 + 강등 테스트 35 그대로, 무변경 계약 경로 변경 0줄.

## 계획 대비 이탈 (부모가 점검 뒤 직접 편집)

1. **세 번째 상한 위치**: `tests/agents/test-plan-path-consistency.sh` 에도 라우팅 표 크기 상한(800B)이 걸려 있었다 — 설계 §3.4 와 계획은 두 곳만 알고 있었다. 각주 추가로 이 테스트가 떨어져, 설계가 이미 정한 1100 을 같은 값으로 맞추고 "세 곳 lockstep" 주석을 달았다. 새 임계값 결정 아님. 계획 문서는 고치지 않았다(검토 통과 표식 유지).
2. 워커가 설계에서 옮겨 적은 문구 중 배포되지 않는 설계 문서의 결정 번호를 가리키던 곳(`routing-procedure.md` 경계표)을 기존 정보성 필드와의 비교로 교체, 강등 테스트 docstring 의 설계 절 참조에 문서 경로 명시.

## 리뷰

- 코드 리뷰: 1회 통과(893초, 높은 강도). 높은·중간 등급 0건, 범위 항목 11개 전부 설계와 일치. 비차단 참고 — 래퍼가 검토자에게 넘기는 변경 파일 목록에 신규 미추적 규칙 파일 두 개가 빠져 있었으나 검토자가 실제 변경분에서 직접 읽었다(래퍼 쪽 후속 후보).
- 10분 실행 한도를 넘겨 리뷰가 백그라운드로 넘어갔지만 통과 기록은 정상 기록됐다.
- 보안 리뷰: 통과, 차단급 없음(표준 강도). 검토자가 저장소 밖 샌드박스에서 비정상 입력 26개 경우를 직접 돌려 활성으로 새는 경로가 없음을 확인. 낮은 등급 2건 + 정보 1건을 반영:
  - 플래그 읽기 함수의 존재 확인을 try 안으로 — 읽을 수 없는 정책 디렉토리에서 트레이스백·종료 코드 1 이 나오던 경로가 "false" + 종료 코드 0 으로.
  - 새 테스트에 활성으로 새면 안 되는 값 모양 6개(문자열 "true"/"false", 숫자 1, 빈 매핑, 스칼라 항목, 최상위 리스트)와 인자 누락 경우 추가.
  - 규칙 본문의 "워커 dispatch 자체 불가" 예외에 "직접 수행하더라도 검증 → 테스트 → 리뷰 → 커밋 절차와 커밋 시점의 리뷰 요구는 그대로다" 한 문장.
  - 반영 뒤 코드 리뷰 통과 기록은 낮은 등급 수정 규칙대로 자체 검토 후 새 지문으로 재발급, 보안 검토자는 안 바뀐 9개 파일을 해시 대조하고 바뀐 4개만 재검토한 뒤 통과 기록 발급.
- **미결(사용자 결정, 별도 작업)**: 보안 검토자가 중간 등급으로 짚은 문서 모순 — 워커 금지목록은 "워커는 보안 통과 기록을 발급하지 않는다", 보안 리뷰어 정의는 "검토자가 직접 발급". 설계 §8 항목 1 로 이미 분리돼 있던 항목이며, 새 규칙 본문이 그 금지목록을 기본 경로의 기준으로 가리키게 됐다. 이번에는 지금까지의 절차(검토자가 검토 시작 시점 지문에 결속해 직접 발급)대로 했다. 다음 단위에서 기능을 켜기 전에 사용자에게 보고함.

## 전체 회귀

이 변경으로 새로 깨진 테스트 0개. 떨어지는 9개는 전부 기존 항목:
- 사고 기록 집계 계열 7개(`tests/hooks/test-incidents-automation.sh`·`test-stop-incident-gate.sh`·`test-incidents-semi-automation-full.sh`·`test-incident-advisory-check.sh`, `tests/scripts/test-advisory-summary.sh`·`test-aggregate-combined-cli.sh`·`test-aggregate-masking-ssot.sh`) — 직전 커밋의 별도 체크아웃에서도 같은 목록이 떨어짐. 집계 스크립트의 문자 인코딩 오류(surrogate)로 보임. CI 에서도 떨어지는지는 미확인 — **새 후속 후보**.
- 스킬 묶음 2개(`test-review-evidence-manifest.sh`, `test-persona-skill.sh`) — 이전부터 기록된 항목.

## 관찰

- 통과 기록의 검토 대상 지문은 코드·테스트 파일과 `.rein/state.lock`(빈 런타임 잠금 파일, 무시 규칙에 없음)만 덮고 `*.md`·`trail/**` 는 제외한다. 그래서 리뷰 뒤에 완료 기록을 써도 지문이 바뀌지 않는다. 잠금 파일이 지문에 들어가는 것은 의도로 보이지 않는다 — 무시 규칙 후속 후보.
- 전체 회귀 묶음은 이 기기에서 약 17분(훅 5.5분 + 스크립트 1분 + 스킬 10분). 스킬 묶음에 리뷰 래퍼 자체 테스트가 있어 실제 리뷰와 겹치지 않게 순서를 잡았다.

- 보안 검토 통과 기록의 지문은 **스테이징된 변경만** 대상으로 한다(코드 리뷰 지문은 작업 트리 기준). 스테이징 없이 보안 검토를 맡기면 검토는 되지만 기록이 "대상 없음"으로 건너뛰어진다 — 다음 단위부터는 스테이징을 먼저 하고 보안 검토를 부른다.
- 완료 기록(`trail/inbox/<같은 slug>.md`)을 미리 써 두면 편집 게이트가 그 작업을 끝난 것으로 보고 소스 편집을 막는다(정당한 차단). 리뷰 의견 반영이 남아 있을 때는 완료 기록을 쓰지 말 것 — 이번에는 초안을 임시 폴더로 옮겨 두었다가 끝난 뒤 되돌렸다.

- 오케스트레이션: 4개 하위 작업(병렬 Y) — 강등 없음

## 다음

3/4 워커 2단계(`inject-wiring`): 세션 시작 훅의 조건부 슬롯 + 주입량 테스트 + CI 목록 등록 + 이 저장소에서 기능 켜기. 단독 워커.

DoD: `trail/dod/dod-2026-09-21-orchestrator-first-wave1.md`
