# v1.3.8 release prep — CHANGELOG 통합 entry + main 머지 + tag

- 날짜: 2026-05-27
- 유형: release prep
- 변경 파일:
  - **dev step**: `CHANGELOG.md` (v1.3.8 entry 확장 — hotfix + G3 + TONE-1 + 자동모드 통합 6 bullet) + `trail/dod/dod-2026-05-27-v1-3-8-changelog-release-prep.md` (DoD) + `.codex-reviewed` + `.active-dod` + 본 inbox + `trail/index.md`
  - **main step**: branch-strategy ✅ 포함 목록의 변경 파일 선별 체크아웃 — plugins/rein-core/** + scripts/rein.sh + tests/** + trail/** + CHANGELOG.md. `.claude/CLAUDE.md` @import 라인 제거 (메인테이너 전용 — main 직접 편집 예외)
  - **tag**: `v1.3.8` annotated
- 요약:
  - dev 누적 (origin/dev force-with-lease push 후 동기): plugin install hotfix `cb4d997` + G3 ship `021bbf9` + TONE-1 `413169d` + 자동모드 `080455e` + backlog cleanup `2b4ece9` + 본 release prep
  - CHANGELOG v1.3.8 entry 가 hotfix-only 였으나 오늘 user-facing 신규 기능 5건 추가됐으니 통합 entry 로 확장
  - 사용자 명시 v1.3.8 (patch bump). versioning Rule A advisory 권고는 minor (v1.4.0) 였으나 사용자 결정 우선 — advisory hard gate 아님. 5 신규 기능 모두 advisory/optional/opt-in (block 안 함, 기존 동작 무변화 default) 라 patch 도 합리적
  - plugin.json + scripts/rein.sh VERSION 둘 다 `1.3.8` (이미 cb4d997 hotfix 로 적용됨, parity OK)
  - origin/dev 의 Hermes Phase 1 4 commits (defer 결정, `hermes-experimental-2026-05-26` branch 보존) 는 force-with-lease push 로 정리됨
- 사용자 결정: 본 turn = "메인 머지부터 진행. 버전은 1.3.8". 분기 정리 = force-with-lease.
- 다음 작업 후보:
  - main push 직후 `mirror-to-public.yml` 자동 실행 — strip 검증 (trail/, .rein/, .claude/, AGENTS.md 부재) 사후 확인
  - `publish-plugin.yml` 자동 실행 — plugin tarball 갱신 (Anthropic + self-marketplace)
  - 남은 백로그 3건 모두 보류·low (PLN-1+AG-2 / SR-1.b / G3 perf NFR follow-up) — 별 cycle
