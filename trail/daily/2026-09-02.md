# Daily Summary: 2026-09-02

---
<!-- source: 2026-09-02-issue-triage-replan.md -->
## issue-triage-replan

### 완료 기록

## 판정 (dev 트리 코드 대조)

| 리포트 | 상태 | 근거 |
|---|---|---|
| `docs/reports/[issues]_2026-08-28.md` 편집 전면 차단 (task-axis) | **해결** — v2.0.1 (`dc1b421`) | 번들 `policies/task-axis/` + 프로젝트→번들 2단 해소 + 차단 문구 "재설치" 안내. 잔여 = doctor 축별 진단(09-02 제안 4 와 동일 항목으로 흡수) |
| `docs/reports/[issues]_2026-09-01.md` non-git 온보딩 실패·오진 | **미해결** (A/B/C 전부) | `session-start-bootstrap.sh:128` non-git 거부 유지, `stop-session-gate.sh:55` trap 이 degraded/미완료 검사(184·193) 앞, `rein-aggregate-incidents.py:436,666` 무조건 mkdir, `.onboarded` 마커 그대로 |
| `docs/reports/[issues]_2026-09-02.md` 보안 검토 축 무음 소멸 | **미해결** (제안 1~4 전부) | 번들 `policies/` 에 security-axis 부재. `pre-bash-commit-review-gate.sh:305` 폴더 없음→exit 0, `security-review-gate.sh:266`·`rein-mark-security-reviewed.sh:235` 프로젝트 경로 고정, `facts.py:256` OSError 를 git 조회 오류로 흡수. dogfood 는 git 추적 override(`.rein/policy/security-axis/`)가 있어 못 느낌 — **v2.0.1 과 동형 클래스**(배포 기본 전환 축의 정책이 override 로만 존재) |

## 재편 순서 (사용자 결정 2026-09-02)

1. **보안 검토 축 복구** — 번들 동봉 + 3곳 2단 해소 + facts.py 부재/조회실패 구분 + **배포 기본 전환 3축 전부에 대한 번들 해소 계약 테스트**(08-28·09-02 재발 클래스 구조 봉합) + `rein doctor` 축별 진단(사용자 "이번 사이클 포함" 선택). DoD: `dod-2026-09-02-security-axis-bundled-policy.md`. 릴리스 등급은 배포 DoD 에서 결정(권고 patch — 문서화된 동작 복원).
2. **온보딩 오진 수리** — B(집계 스크립트 미초기화 가드 + trap 등록 순서) + A(**사용자 결정: non-git 이면 git 필요성을 안내하고 승인 시 준비(git init/설치)해 온보딩 계속, 거부 시 온보딩 중단**. 훅 안내는 어시스턴트가 이 대화를 진행하도록 지시하는 프라이머 형태, UserPromptSubmit 안내와 동일 지시, 초기화 스크립트의 non-git 기본 허용은 플래그 뒤로 이동해 "거부" 일관성 확보, bash 게이트 allow-list 에 `git init` 필요 여부 확인, 세션 중 bootstrap 후 degraded 마커 해제 경로 확인) + C(프라이머 끝 초기화 상태 1줄, 마커 개명은 보류). 설계 요소가 있어 brainstorm 경량 통과 후 DoD.
3. **리뷰 자기증폭 봉합** — 08-27 spec 재작성(High 2 + Medium 4)→재리뷰→plan→축1 3건 구현. 사이클 1·2 리뷰 회차 실측이 근거로 누적.

## 결정 근거

사이클 1·2 는 이미 배포된 사용자 결함(2.0.x 전 사용자 보안 검토 기록 0건 + 신규 사용자 진입 모순). 자기증폭은 메인테이너 내부 효율이라 며칠 밀려도 사용자 피해 없음.

## 백로그 (사용자 지시 2026-09-02) — 페르소나 프리셋 전환 체감 지연

실측: 로더(`scripts/rein-policy-loader.py`) 호출은 약 50ms 로 원인 아님. 원인은 (1) 스킬 절차가 순차 도구 왕복 8~12회를 강제(본문 로드→활성 확인→머리말 읽기 N회→질문→설정 쓰기→경로 얻기→본문·불변층 읽기→인사말 얻기→출력) — 왕복마다 모델 생성 시간이 얹힘, (2) `skills/persona/SKILL.md` 약 18KB/122줄, (3) 편집 1회당 훅 pre 4 + post 11 = 격리 합산 약 1.5초. 방향: 로더에 목록 일괄 출력 옵션(표시 이름·요약·활성 표식 한 번에) + 선택 후 "설정 쓰기·본문·인사말"을 한 명령으로 묶어 왕복 3~4회 제거 + 스킬 본문 압축(규칙 불변). 사이클 1~3 뒤 별도.

## 사이클 1 진행 중 발생한 결정·백로그 (2026-09-02)

- **doctor 축별 진단 제외 재결정**: codex 코드 리뷰 6회 중 5회가 doctor 의 계약 정밀도로 소모(핵심 수리는 1회차부터 통과). 사용자 결정으로 doctor 구현·테스트를 되돌리고 별도 사이클(spec 선행)로 이관. 그 spec 의 입력으로 남기는 리뷰 지적: (1) `enforced` 의미 — 훅 토글(`.rein/policy/hooks.yaml`)을 반영하지 않으면 "정책 준비됨" 으로 이름을 좁혀야 함, (2) authority override 손상 시 기본값 대체 금지·tri-state(알 수 없음)로 보고, (3) 정책 내용 검증 — require·trigger·when 이 실제 이벤트 fact 계약과 일치해야 함(커밋 이벤트 fact = command.type·task.exists·tool("Bash")), (4) `_version.yaml` 필수(런타임이 부재 시 차단), (5) code_review 는 `REIN_POLICY_DIR` 환경변수 우선(env tier), (6) **active_task 는 도구별 파일 단위로 프로젝트→번들 해소**(부분 오버라이드 = 혼합 상태가 정상) — 디렉토리 단위 모델은 오진, (7) 경로 구성요소가 존재하면 검색 가능한 디렉토리여야 함(매달린 링크·일반 파일·진입 불가 = 손상, 번들 폴백 금지). 08-28 리포트 제안 2·4(스캐폴딩/doctor) 잔여도 이 사이클로.
- **흔들리는 백그라운드 작업 테스트 2건**: `tests/scripts/test-job-completion-wrapper.sh`, `tests/scripts/test-job-gc.sh` — 자기 검증은 OK 를 찍은 뒤 정리 단계의 `rm -rf` 가 아직 쓰는 중인 백그라운드 작업의 임시 폴더에서 "Directory not empty" 로 실패해 비0 종료(전체 배터리 8회 중 3회 재현, 개별 실행도 간헐). 관련 파일은 이번 변경과 무관. 수리 방향: 정리 전에 작업 종료를 기다리거나 정리 실패를 테스트 실패로 세지 않기. 또한 `tests/scripts/run-all.sh` 가 실패 스위트 이름을 출력하지 않아 진단이 어려움 — 스위트별 종료값 출력 추가 권장.
- **주석 자기증폭 실증**: 워커가 남긴 날짜·리포트·회차 서술을 세 차례 걷어냈고 codex 도 Low 로 두 번 지적 — 사이클 3(자기증폭 봉합) 축2 규칙(주석은 지속 계약만) 의 실측 근거.
- **코드 리뷰 종결 기록**: codex 7회차(코드 결함 0) 후 사용자 승인 종결 → 독립 대체 리뷰어(opus)가 인증 경로 18건 전량 리뷰, High/Medium 0, 통과 증거 기록(fallback_reason: user_approved_termination_after_round_7). **대체 리뷰어의 선택 개선(Low) 5건 — 후속**: (1) `facts.py` 부재 오류 문구 꼬리("no project override … via REIN_POLICY_DIR")가 번들 경로 부재 시 자기모순, (2) `rein-mark-security-reviewed.sh` 의 해소 lib 소싱이 존재 검사만 하고 소싱 실패는 unbound-variable 로 죽음(방향은 fail-closed, 문구 불친절), (3) `test-code-review-authority-switch.sh`·`test-security-review-authority-switch.sh` 의 번들 헬퍼가 아직 strict 픽스처를 복사(결과 무영향), (4) 커밋 게이트 스위트에 번들(sensitive) 경로용 문서-only 핀 없음(strict 핀이 함의), (5) resolver 단위 스위트 케이스 번호 순서 표기.
- **사이클 1 코드 커밋 완료**: dev `54b1377` (19파일, +1574/−132). 코드 리뷰 증거(독립 대체 리뷰어, 인증 경로 18건)와 보안 리뷰 증거(standard, 같은 지문 — 보안 축은 strict 프로필이라 스테이징 후 재캡처해 기록) 모두 커밋 게이트 통과. 수정 전/후 실측: 같은 보안 민감 파일 변경에 대해 수정 전엔 코드 리뷰 증거만으로 커밋 게이트가 조용히 통과·기록 스크립트는 git 을 지목하며 실패, 수정 후엔 증거 없으면 차단·기록 성공·증거 후 통과. 다음: 배포 DoD(등급 사용자 결정, 권고 patch; CHANGELOG 에 "보안 표면을 건드린 커밋은 보안 검토 기록이 필요" 안내 한 줄 필수, 오버라이드 없는 배포 시뮬레이션 재실행) → 사이클 2.

## 사이클 2 진행 (2026-09-02 저녁~)

- **A · 세션 종료 훅 잔여물 오진 — 코드 커밋 완료 dev `4430959`** (7파일): 마킹 함수가 EXIT 시점에 초기화 여부를 직접 확인(초기화된 프로젝트면 어느 경로든 마킹, 미초기화면 아무것도 안 만듦), 집계 스크립트 두 함수 no-op 가드(루트 폴백 사본 동기화), 우회 탈출구 감사 로그 조건부, advisory 호출 `--project-dir`. codex 4회차 통과(1회차 High = "비활성이면 마킹 안 함" 이라는 기준서 문구가 세션 시작의 비정상 종료 판정과 충돌 → 계약 정정, 2·3회차는 문서 정합·테스트 강화(역변이 확인)), 보안 리뷰 통과. 교훈: 기준서에 구현 방식(trap 위치)을 적지 말고 계약(무엇을 마킹하나)만 적을 것 — 구현 문구가 리뷰 회차를 만들었다.
- **B · git 필수 안내형 온보딩 — 구현 반입, codex 1회차 NEEDS-FIX**: High 3(안내 명령의 경로 인용이 셸 삽입 가능 → `%q`; 두 훅의 프로젝트 경로 출처 불일치($PWD vs 이벤트 cwd) → bootstrap_check 확정값 공유; 거부 후에도 매 프롬프트 승인 질문 반복 → 세션당 1회 전체 안내 + 이후 짧은 알림), Medium 3(bash 게이트 허용목록에 `--allow-non-git`; README 두 벌의 non-git 지원 서술 정정; 프라이머 상태 줄 테스트) 워커 F 수정 중.
- **세션 종료(2026-09-03 새벽) 시점 B 상태**: 본 트리에 B 2회차 상태 미커밋(코드 리뷰 2회차 NEEDS-FIX: High 2 = 단일 확정 경로 미완 / 복구 명령이 bash 게이트 정규식과 불일치, Low = 테스트 주석 이력 서술). 워커 F(누적 5시간+ 실행)가 3차 수정 중 정지 — 격리 트리 `wt-onboarding` 에 5파일 부분 작업(구문 OK, 미검증). 재개 절차는 index 참조. 수정 방향은 DoD 5-f(세션 시작 훅의 모든 판정·행동을 `bootstrap_check` 확정 경로 하나로)·5-g(인용 헬퍼 단일화 작은따옴표 우선 + 게이트에 기대 명령 정확 일치 경로, 정규식 불변, 작은따옴표 포함 경로는 차단 허용). 교훈: 한 워커를 여러 회차 동안 이어 쓰면(누적 5시간) 정지 위험 — 회차마다 새 워커에 지적 목록을 넘기는 편이 안전.
- **B 1·2회차 확정 사항(재개 시 유지)**: 안내문 단일 lib + `%q`→(다음 회차) 인용 헬퍼, 세션당 1회 전체 안내 + 이후 짧은 알림(`.claude/cache/.rein-git-guidance-shown.session-<key>`), 초기화 스크립트 non-git 기본 거부 종료 코드 3 + `--allow-non-git` + 성공 시 degraded 마커 제거, bash 게이트 허용목록 `--allow-non-git` 토큰, README 두 벌 정정, 프라이머 상태 줄 테스트. 수정 전/후 실측 로그는 스크래치(`e2e-onboarding-before.log`, `e2e-onboarding-afterB.log`).


---
<!-- source: 2026-09-02-session.md -->
## session

### 완료 기록

- 날짜: 2026-09-02
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
(오늘 커밋 없음)

## 변경된 파일 (worktree + staged, 미커밋 전체)
trail/daily/2026-08-21.md
trail/daily/2026-08-23.md
trail/daily/2026-08-24.md
trail/daily/2026-08-25.md
trail/dod/dod-2026-08-28-task-axis-bundled-policy.md
trail/inbox/2026-08-28-persona-display-name-ui.md
trail/inbox/2026-08-28-release-v2-0-0.md
trail/inbox/2026-08-28-task-axis-bundled-policy.md
trail/inbox/2026-09-01-session.md
trail/index.md
trail/weekly/2026-W34.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.

