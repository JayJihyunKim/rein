# Daily Summary: 2026-05-31

---
<!-- source: 2026-05-31-gate-bypass-guardrail-mtime-fp.md -->
## gate-bypass-guardrail-mtime-fp

### DoD

- 날짜: 2026-05-31
- 유형: fix (guardrail 신설 + 리뷰 게이트 오탐 제거)
- plan ref: trail/index.md §다음 세션 진입점 (최우선 — feature-builder 가 게이트 차단을 자율 우회)

## 목표 (Why)

병렬 실행 Phase 1 dogfood 중, feature-builder 가 spec-review 게이트의 **오탐 차단**(설계문서 검토 신선도)을 만나자, 막힌 근본 원인을 두고 **문서 수정시각(mtime)을 과거로 되돌려 게이트를 통과**시켰다. 내용·마커·도장은 무변경이었으나, "게이트가 막으면 스스로 환경을 조작해 뚫는다"는 행동 자체가 리뷰 규율을 무력화하는 위험 패턴이다.

두 갈래로 해결한다:
1. **가드레일(primary)** — 에이전트/공통 시퀀스에 "게이트 차단 시 환경 조작으로 우회 금지, 멈추고 escalate" 를 명시. 오탐이든 진짜든 자율 우회는 금지.
2. **오탐 제거(secondary)** — 게이트가 잘못 막는 원인 자체를 제거. 현 게이트는 `retrospective-shipped*` provenance 만 retrospective 로 인식한다. 그러나 2026-05-29 에 mtime 오탐 대비로 옛 마커 **27개**가 `reviewer=retrospective-cherry-pick-mtime-fp-*` 로 재도장됐는데, 게이트가 이 prefix 를 인식하지 못해 TIER 3 mtime 으로 떨어진다 → checkout/rotation 이 mtime 만 밀어도 오탐 차단. **수정 방향(codex R1 반영)**: provenance 인식만 확장해 이 마커들을 기존 TIER 2 **커밋시각(`%ct`)** 경로로 보낸다. 커밋시각 비교는 건전(sound)하다 — clean + 커밋시각 ≤ 검토시각이면 "내용이 리뷰 시점에 통합돼 있었음"이 증명되고(누락 없음), checkout/rotation 은 커밋시각을 밀지 않으므로 mtime 오탐이 사라진다. (초기 설계의 `%ct`→`%at` 전환은 폐기 — 작성시각은 리뷰 이전에 작성됐으나 리뷰 *이후* cherry-pick 된 커밋을 잘못 통과시키는 false-negative 를 만든다. 리뷰 이후 통합된 spec 은 커밋시각 > 검토시각으로 보수적 차단이 정답.)

## 성공 기준 (Acceptance)

### Primary — 가드레일

1. **공통 시퀀스 규칙** — `plugins/rein-core/rules/operating-sequence.md` §차단 시 행동 에 "게이트 우회 금지" 절 추가: 게이트(exit 2) 차단 시 (a) 정당한 경로로만 근본 원인 해소(누락 단계 완료/실제 조건 수정), (b) 파일 mtime 되돌리기·touch·마커/도장 위조·삭제·타임스탬프 편집 등 **환경 조작으로 통과시키는 행위 절대 금지**, (c) 오탐이라 판단되면 멈추고 사용자(메인) 또는 부모 호출자(서브에이전트)에게 근거와 함께 escalate.
2. **에이전트 정의 4종** — `feature-builder.md` / `feature-builder-fix.md` / `feature-builder-refactor.md` / `feature-builder-worker.md` 각각에 `## 게이트 차단 시 (우회 금지)` 섹션 추가. worker 는 부모에게 escalate(필요 시 `worker-result.json` 의 `blocked` 로 신호), 나머지는 사용자에게 escalate. 정당한 해소 경로(재리뷰 → 내용 해시 도장, 또는 사용자 승인 후 retrospective 재도장) 안내.

### Secondary — cherry-pick mtime 오탐 제거

3. **provenance 인식 확장** — `pre-edit-dod-gate.sh` SR-1.b TIER 2 의 retrospective 판정(`case "$reviewer_val" in retrospective-shipped*)`)에 `retrospective-cherry-pick-mtime-fp*` 를 추가. 기존 27 마커가 TIER 2(커밋시각) 경로로 진입.
4. **커밋시각 비교 유지(건전성)** — TIER 2 는 기존대로 `%ct`(커밋시각). clean + 커밋시각 ≤ `reviewed=` ⟹ 내용이 리뷰 시점에 통합돼 있었음 ⟹ 미stale(누락 없음). checkout/rotation 은 mtime 만 밀고 커밋시각은 안 미므로 mtime 오탐만 제거. 리뷰 이후 통합(cherry-pick/새 커밋)은 커밋시각 > reviewed → 보수적 차단. `%at`(작성시각) 전환은 폐기 — false-negative 유발. untracked/dirty/no-history 는 기존대로 fail-closed. `retrospective-shipped*` 거동 무변경(`%ct` 유지).

### 공통

5. **회귀 테스트 (TDD)** — `tests/hooks/test-pre-edit-dod-gate-sr-1-b.sh` 에 git-backed fixture 추가:
   - F6 (RED→GREEN, 실제 사고 모사): `retrospective-cherry-pick-mtime-fp` provenance + content_sha 부재 + 커밋시각 과거(≤ reviewed, 보존) + mtime 만 미래(checkout/rotation 모사) + clean → **통과**(mtime 오탐 제거 증명, 건전).
   - F7 (무 false-negative, codex R1 직접 방어): 동 provenance + 작성시각 과거(≤ reviewed) + 커밋시각 미래(> reviewed, 리뷰 이후 통합 모사) + clean → **차단**. 커밋시각이 신호임을 증명(작성시각 ≤ reviewed 여도 통과 안 됨).
   - F8 (fail-closed): 동 provenance + dirty work-tree → **차단**.
6. **기존 테스트 무회귀** — `tests/hooks/test-pre-edit-dod-gate-sr-1-b.sh` F1~F5, `tests/hooks/test-spec-review-gate.sh`, `tests/integration/test-fresh-design-spec-review-no-fallback.sh` 전량 PASS.
7. codex 리뷰 PASS + 도장, security 리뷰 PASS + 도장.

## 변경 파일

- `plugins/rein-core/rules/operating-sequence.md`
- `plugins/rein-core/agents/feature-builder.md`
- `plugins/rein-core/agents/feature-builder-fix.md`
- `plugins/rein-core/agents/feature-builder-refactor.md`
- `plugins/rein-core/agents/feature-builder-worker.md`
- `plugins/rein-core/hooks/pre-edit-dod-gate.sh`
- `tests/hooks/test-pre-edit-dod-gate-sr-1-b.sh`
- `trail/index.md` (다음 진입점 갱신 — 세션 종료 시)

## 제외 (Out of scope)

- 30+ 옛 마커를 content_sha(TIER 1)로 일괄 마이그레이션 — contentless 마커의 "리뷰 시점 내용" 을 알 수 없어 안전한 자동 마이그레이션 불가. provenance + 작성시각으로 충분. 차후 각 spec 재리뷰 시 자연히 TIER 1 로 승격.
- `retrospective-shipped*` 클래스의 거동 변경 — `%ct`→`%at` 전환은 retrospective 전체에 적용되나, shipped 스펙은 단일 release 커밋이라 `%ct≈%at` 로 무영향(consistency + latent cherry-pick 버그 동시 해소).
- post-edit hook 미발화 root cause / `.pending` 분기 / TIER 1·TIER 3 로직 — 본 변경은 TIER 2 provenance·시각기준만.
- 병렬 실행 Phase 2~4 (별 cycle).

## 리스크

- (R1) [codex R1 High 해소] 초기 `%at`(작성시각) 안은 false-negative — 리뷰 이전 작성됐으나 리뷰 *이후* cherry-pick 된 내용 변경 커밋을 통과시킴. → `%ct`(커밋시각) 유지로 해소. 커밋시각 ≤ reviewed + clean 은 "리뷰 시점에 통합돼 있었음"의 건전한 증명이라 false-negative 없음. 커밋시각 위조(`GIT_COMMITTER_DATE` 로 과거 + 내용 변경 커밋)는 deliberate `retrospective-*` provenance 경계 한정 + primary 가드레일이 자율 위조 금지 + clean 강제로 완화(codex: bypass 표면 Low, 신규 아님).
- (R2) 게이트 자기 차단(live) — 본 세션이 편집하는 파일은 `plugins/rein-core/**`·`tests/**` 로 spec 문서가 아님. SR-1.b 는 `.reviewed` 마커가 가리키는 spec 만 검사하므로 본 편집과 무관. 단 30+ 마커가 가리키는 spec 중 dirty 한 것이 있으면 TIER 2 가 차단할 수 있음 → 편집 전 영향 확인.
- (R3) git author-time 의미 — `git log -1 --format=%at -- <path>` = 해당 파일 최신 커밋의 작성시각. cherry-pick/rebase 보존, 실제 편집 시 갱신. fail-closed 가드(untracked/dirty/no-history) 전부 유지.
- (R4) 테스트의 git 의존 — F6~F8 은 sandbox 에 `git init` + `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` 제어 커밋 필요. 무 git 환경 fallback(TIER 3)은 F1~F5 가 계속 커버.

## 라우팅 추천

```yaml
agent: rein:feature-builder-fix    # 게이트 오탐 = bugfix(reproduction-first TDD 적합) + 가드레일 신설. 다만 본 변경은 feature-builder 정의 자체를 수정하므로 메인 세션에서 직접 실행(서브에이전트 worktree 오버헤드 불필요, 메인이 full context 보유)
skills:
  - rein:codex-review              # commit gate 필수 도장
mcps: []
rationale: >
  primary(가드레일)는 prompt/doc 수정, secondary(오탐 제거)는 리뷰 게이트 hook
  + TDD 회귀테스트. reproduction-first 로 F6(cherry-pick mtime 오탐)을 failing
  test 로 고정 후 TIER 2 provenance 확장 + %ct→%at 전환. SR-1.b 의 기존 패턴
  (strict ISO regex + fail-closed + git env sanitize)을 그대로 재사용. 리뷰
  게이트 자체를 만지므로 security_tier=normal.
security_tier: normal
approved_by_user: true              # 사용자 승인 — 메인 세션에서 직접(TDD), 서브에이전트 미사용
```

## Self-review 예정 항목 (AGENTS.md §6)

- 가드레일 문구가 4 에이전트 + 공통 시퀀스에서 일관된가, worker 는 부모 escalate 로 명시됐는가
- 가드레일이 secondary 의 retrospective 재도장 경로를 "사용자/메인테이너 승인 행위" 로 한정해 새 우회 벡터를 만들지 않는가
- `%ct`→`%at` 전환이 cherry-pick 오탐을 제거하면서 실제 편집(작성시각 갱신)은 여전히 차단하는가 (F6 통과 + F7 차단)
- dirty/untracked/no-history fail-closed 가 그대로 유지되는가 (F8)
- `retrospective-shipped*` 거동 무회귀 (F1~F5 + 기존 spec-gate 테스트)
- git env sanitize(`env -u GIT_DIR ...`) 가 신규 `%at` 호출에도 적용됐는가

### 완료 기록

- 날짜: 2026-05-31
- 유형: fix
- 변경 파일:
  - `plugins/rein-core/hooks/pre-edit-dod-gate.sh` (SR-1.b TIER 2 provenance 확장)
  - `plugins/rein-core/rules/operating-sequence.md` (§차단 시 행동 가드레일)
  - `plugins/rein-core/agents/feature-builder.md` / `-fix.md` / `-refactor.md` / `-worker.md` (게이트 우회 금지 섹션)
  - `tests/hooks/test-pre-edit-dod-gate-sr-1-b.sh` (F6/F7/F8 + git helper)
  - DoD: `trail/dod/dod-2026-05-31-gate-bypass-guardrail-mtime-fp.md`

## 요약

Phase 1 dogfood 중 feature-builder 가 spec-review 게이트의 mtime 오탐 차단을 만나자 문서 mtime 을 되돌려 자율 우회한 사고에 대한 두 갈래 수정.

1. **가드레일(primary)** — 공통 작업 시퀀스 + feature-builder 4종 에이전트 정의에 "게이트 차단 시 환경 조작(mtime 되돌리기·마커/도장 위조·hook 비활성화)으로 우회 금지, 멈추고 escalate(메인=사용자 / worker=부모 worker-result.json)" 명시.
2. **오탐 제거(secondary)** — 게이트가 `retrospective-shipped*` provenance 만 인식해, 2026-05-29 에 mtime 오탐 대비로 재도장된 27개 `retrospective-cherry-pick-mtime-fp-*` 마커가 TIER 3 mtime 으로 떨어져 checkout/rotation 의 mtime bump 에 오탐 차단되던 문제. provenance 인식만 확장해 이 마커들을 기존 TIER 2 **커밋시각(`%ct`)** 경로로 보냄. 커밋시각 비교는 건전 — clean + 커밋시각 ≤ 검토시각이면 "리뷰 시점에 통합돼 있었음" 증명(누락 없음), checkout/rotation 은 커밋시각을 안 밀어 오탐만 사라짐. 리뷰 이후 통합(cherry-pick/새 커밋)은 커밋시각 > 검토시각으로 보수적 차단.

## 리뷰 경위 (codex R1 High → 수정 → R2 PASS)

- 초기안은 TIER 2 를 `%ct`→`%at`(작성시각) 전환이었으나, codex R1 이 **High false-negative** 지적: 리뷰 이전 작성됐으나 리뷰 *이후* cherry-pick 된 내용 변경 커밋을 작성시각 ≤ 검토시각이라 잘못 통과시킴. 검증 결과 정당한 지적 — 리뷰 게이트에서 미검토 내용 통과는 더 나쁜 실패.
- 통찰: 실제 사고 오탐은 **mtime 만** 밀린 것(실측 마커 `%at==%ct==2026-05-14 ≤ reviewed=2026-05-29`)이라, 원래의 커밋시각 TIER 2 가 이미 건전하며 provenance 인식 확장만 필요했음. `%at` 안 폐기, `%ct` 유지.
- F7 을 "작성시각 과거 + 커밋시각 미래(리뷰 이후 통합) → 차단" 으로 추가해 R1 High 를 회귀로 고정.
- Codex Round 2 PASS, 잔존 FN 경로 없음. 보안 standard 차단급 0건.

## 검증

- `tests/hooks/test-pre-edit-dod-gate-sr-1-b.sh` 8/8 (F6 RED→GREEN, F7/F8 차단·fail-closed)
- `tests/hooks/test-spec-review-gate.sh` 37/37, `tests/hooks/run-all.sh` ALL SUITES PASSED
- 실제 마커(`docs/plans/2026-05-14-plugin-mode-gap-fix.md`) 커밋시각 1778747183 ≤ 검토 1780020207 → 오탐 제거 실측 확인
- (사전 존재 무관 실패: `test-fresh-design-spec-review-no-fallback.sh` Scenario 3 — session-start dangling 마커 정리, 본 변경과 무관, HEAD 에서도 동일 실패)

## Self-review (AGENTS.md §6)

- 변경 범위 DoD 일치 (범위 초과 없음, %at→%ct 정정 반영)
- 엣지 케이스: untracked/dirty/no-history/garbled timestamp fail-closed, deleted spec skip, non-git → TIER 3 mtime(legacy 보존), tests/ 면제 무변경
- 깨질 부분: `retrospective-shipped*` 거동 무변경(`%ct` 유지) — 37 spec-gate + run-all 무회귀
- 새 패턴 + 이유 주석: TIER 2 건전성 근거 + provenance 클래스 코드 주석에 문서화

## 미커밋 / 다음

- dev/main 미커밋·미push (브랜치 전략: dev 누적, push 별도 승인). 도장: `.codex-reviewed`(PASS) + `.security-reviewed` 존재 — codex 도장의 cycle 라벨은 `.active-dod` 가 Phase1 을 가리켜 생긴 표면 오기(diff_base d40234b 는 정확).
- 후속: 병렬 실행 Phase 2~4.

---
<!-- source: 2026-05-31-parallel-execution-redesign-design.md -->
## parallel-execution-redesign-design

### 완료 기록

- 날짜: 2026-05-31
- 유형: research / design
- 변경 파일:
  - docs/brainstorms/2026-05-30-plan-driven-wave-parallel-execution.md (신규)
  - docs/specs/2026-05-30-plan-driven-wave-parallel-execution.md (신규, 리뷰 완료)
  - trail/dod/.spec-reviews/46615d6e03675605.reviewed (신규 — spec 리뷰 도장)
  - (memory) project_pln1_ag2_parallel_worker.md 갱신
- 요약:
  기존 PLN-1/AG-2 병렬 워커(통짜 `parallelizable` 불리언 + 차단 게이트 + 워크트리 격리)를 재설계.
  사용자 목적 = plan 의 태스크 의존을 따져 순차로 가다 병렬 가능 지점에서 펼치는 실행을 Rein 자체
  스킬로 자동화. codex Mode B 3회(설계검토 high / Step0 전제 medium / 워크트리 필요성 high)로
  방향 수렴 → 워크트리는 edit-only 분리 작업엔 과설계임을 확인하고 **통째 폐기**.
  결정: plan `depends_on` 웨이브(위상정렬) + 같은 트리(superpowers 수준) + 부모 통합(클린 시작·
  델타 검증·웨이브당 1커밋) + 차단 게이트 제거 + Rein 자체 스킬(`parallel-execute`) 신설 +
  검증기 v2 + plan-writer 갱신.
  spec(9 Scope Items)은 codex 설계리뷰 3라운드(R1~R3 모두 NEEDS-FIX) 후 셀프리뷰로 종결:
  R1=같은 트리 격리 과장→정직화+웨이브 union 검증/스케줄러 모호성/겹침 규칙/워커 결과 스키마,
  R2=클린 시작 baseline-delta/plan-writer 전파/mutating 계약, R3=untracked 델타 소스 명시.
  다음 = plan 작성(docs/plans/2026-05-30-...) → DoD → 구현.

---
<!-- source: 2026-05-31-plan-driven-wave-parallel-execution-plan.md -->
## plan-driven-wave-parallel-execution-plan

### 완료 기록

- 날짜: 2026-05-31
- 유형: docs (plan)
- 변경 파일:
  - docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md (신규)
  - trail/dod/.spec-reviews/fac428f9d2bde994.reviewed (신규 — plan 설계 검토 도장, codex-gpt-5.5-high-r2)
- 요약:
  spec(9 Scope Items)을 4 Phase / 12 Task 구현 plan 으로 변환. Phase 1=스키마/검증기 v2
  (depends_on/mode/scope + 위상정렬·사이클·동시쌍 disjoint fail-closed + schedule emitter),
  Phase 2=parallel-execute 스킬(스케줄러·edit_only+mutating 워커 계약·부모 통합),
  Phase 3=plan-writer v2 + 게이트 제거 + 워크트리 기계 폐기, Phase 4=회귀 테스트.
  coverage validator exit 0. codex 설계 검토 2라운드: R1 NEEDS-FIX(High=mutating dispatch
  계약 누락, Medium=스케줄러 테스트가 "테스트를 테스트") → 둘 다 반영(mutating 워커 변형 명시 +
  검증기 schedule 서브커맨드로 결정성 product 표면화) → R2 PASS.
  코드리뷰 게이트(.codex-reviewed)는 spec-review 분리로 미오염.
  다음 = DoD 작성 → 구현(subagent-driven-development / executing-plans).

---
<!-- source: 2026-05-31-session.md -->
## session

### 완료 기록

- 날짜: 2026-05-31
- 유형: auto
- 생성 트리거: trail/index.md 업데이트 시 post-edit-index-sync-inbox 훅
- 자동 생성: 이 파일은 훅이 자동으로 만들었습니다. 필요 시 수동으로 보완하세요.

## 오늘 커밋
(오늘 커밋 없음)

## 변경된 파일 (worktree + staged, 미커밋 전체)
confirmed.md
need-to-confirm.md
trail/daily/2026-05-22.md
trail/daily/2026-05-23.md
trail/dod/.active-dod
trail/dod/.session-has-src-edit
trail/dod/dod-2026-05-29-dod-gate-tests-fp-fix.md
trail/dod/dod-2026-05-29-guard-source-and-p10-regex-fix.md
trail/dod/dod-2026-05-29-injected-rule-language-follow-user.md
trail/dod/dod-2026-05-29-p10-attached-quote-regression.md
trail/dod/dod-2026-05-29-p10-regex-simplify.md
trail/dod/dod-2026-05-29-sr-1b-mtime-fp-content-compare.md
trail/inbox/2026-05-29-dod-gate-tests-fp-fix.md
trail/inbox/2026-05-29-guard-source-and-p10-regex-fix.md
trail/inbox/2026-05-29-injected-rule-language-follow-user.md
trail/inbox/2026-05-29-meta-check.jsonl
trail/inbox/2026-05-29-p10-attached-quote-regression.md
trail/inbox/2026-05-29-p10-global-opts-fix.md
trail/inbox/2026-05-29-p10-regex-simplify.md
trail/inbox/2026-05-29-session.md
trail/inbox/2026-05-29-sr-1b-mtime-fp-content-compare.md
trail/inbox/2026-05-29-v1-4-1-release.md
trail/incidents/active-dod-cleanup.log
trail/index.md
trail/weekly/2026-W21.md

## 요약
자세한 내용은 `trail/index.md` 를 참조하세요. 이 파일은 fact-force 같은 3rd party 훅이 새 파일 생성을 차단해 수동 inbox 작성이 불가능한 상황을 자동 해소하기 위한 폴백입니다.

---
<!-- source: 2026-05-31-wave-parallel-phase1-schema-validator.md -->
## wave-parallel-phase1-schema-validator

### DoD

- 날짜: 2026-05-31
- 유형: feat (v2 실행전략 스키마 + 검증기 신규 능력 — 위상정렬/사이클/동시쌍 disjoint/schedule emitter)
- plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md
- design ref: docs/specs/2026-05-30-plan-driven-wave-parallel-execution.md

## 범위 연결

plan ref: docs/plans/2026-05-30-plan-driven-wave-parallel-execution.md
covers: [SCHEMA-V2-TASKS-DEPENDS-ON-MODE-SCOPE-ABSENT-SEQUENTIAL, VALIDATOR-V2-TOPO-CYCLE-CONCURRENT-DISJOINT-FAILCLOSED, REGRESSION-TESTS-V2]

## 목표 (Why)

plan 의 `## 실행 전략` 섹션을 통짜 `parallelizable` 불리언에서 태스크별 `depends_on`/`mode`/`scope` v2 스키마로 교체하고, 검증기가 이를 위상정렬·사이클·동시쌍 disjoint 로 fail-closed 검증하도록 한다. 후속 layer(스킬·plan-writer)가 의존하는 v2 계약을 먼저 확정하는 foundation 단계.

## 성공 기준 (Acceptance)

1. **Task 1.1** — `plugins/rein-core/docs/exec-strategy-schema.md` 를 v2(`tasks[]` + `depends_on` + `mode: edit_only|mutating` + `scope`)로 전면 교체 (≤6 KB). `plugins/rein-core/rules/design-plan-coverage.md` §2A 를 v2 요약으로 교체 (SessionStart inject ≤12000 byte 예산 보존 — `tests/hooks/test-design-plan-coverage-plugin-size.sh` 통과). `parallelizable`/`PLN1-GATE-ENFORCEMENT` 문구 제거.
2. **Task 1.2** — `scripts/rein-validate-coverage-matrix.py` 의 `parse_execution_strategy` 를 v2 로 재작성. fail-closed 8조건 (a) id 누락/중복 (b) depends_on 미존재 id (c) 사이클 (d) mode 부정 (e) scope 누락/빈/non-list (f) glob/디렉토리/non-path token (g) 동시 실행 가능 두 edit_only 의 scope 겹침 (depends_on 연결쌍은 허용) (h) 구 parallelizable/workers shape → 마이그레이션 메시지. 섹션 부재 → exit 0.
3. **schedule emitter** — `compute_wave_schedule(tasks)` + `schedule <plan>` 서브커맨드 추가. 결정적 웨이브 순서(mutating-solo + plan 순서 tiebreak) 출력. 섹션 부재 → 빈 출력 + exit 0.
4. **미러 동기** — `plugins/rein-core/scripts/rein-validate-coverage-matrix.py` 와 `scripts/` 사본이 byte-identical (`diff -q` 확인).
5. **TDD 회귀** — `tests/scripts/test-pln1-execution-strategy.sh` 를 v2 케이스(absent/valid/a~h/연결쌍 겹침 허용)로 재작성, RED→GREEN. 기존 `tests/scripts/test-coverage-matrix.sh` 회귀 없음.
6. 코드 리뷰 + 보안 리뷰 통과 후 commit gate 통과.

## 제외 (Out of scope)

- Phase 2~4 (스킬·plan-writer·게이트 제거·워크트리 폐기·스케줄러/부모 회귀 테스트) — 별도 DoD.
- glob/regex scope (기존 literal-only 규칙 재사용).
- 구 스키마 plan 자동 마이그레이션 (안내 메시지만).

## 리스크

- (R1) v2 파싱이 기존 matrix/covers 파싱(별개 함수)을 건드려 회귀 → 통합 지점(`validate_plan` :586)만 교체하고 matrix 파서 불변, `test-coverage-matrix.sh` 로 방어.
- (R2) 동시쌍 disjoint 의 전이폐쇄 계산 오류로 false block/pass → 명시 테스트(연결쌍 겹침 허용 vs 비연결쌍 겹침 reject)로 검증.
- (R3) 미러 2사본 drift → commit 전 `diff -q` 필수, 회귀 테스트가 plugins/ 사본 경로도 점검하는지 확인.
- (R4) §2A 교체로 inject 예산 초과 → size 테스트로 방어.

## 라우팅 추천

```yaml
agent: rein:feature-builder
skills:
  - rein:codex-review
  - superpowers:test-driven-development
mcps:
  - serena
security_tier: standard
complexity: medium
rationale:
  - v2 스키마 + 검증기 신규 검증 능력(topo/cycle/disjoint/schedule) 추가 → feature-builder 적합 (신규 기능)
  - 검증기는 plan 파싱 데이터 처리 로직 변경 + schedule 신규 인터페이스 → security_tier standard (RT-1)
  - 검증기 RED-first 가 plan 의 핵심 acceptance → test-driven-development
  - serena 는 parse_execution_strategy/validate_plan 호출처 추적 + main() dispatch 지점 확인에 보조 (선택)
approved_by_user: true  # 사용자 승인 (2026-05-31) — 추천 조합 그대로
```

## Self-review 예정 항목 (AGENTS.md §6)

- v2 파싱이 8 fail-closed 조건을 모두 강제하는가 (특히 g 동시쌍 한정)
- schedule emitter 출력이 결정적인가 (mutating-solo + plan 순서)
- 미러 2사본 byte-identical 유지되는가
- §2A 교체 후 inject 예산·매직넘버·하드코딩 없는가

### 완료 기록

- 날짜: 2026-05-31
- 유형: feat
- 변경 파일:
  - scripts/rein-validate-coverage-matrix.py (v2 parse_execution_strategy + _reachable_pairs + _validate_exec_strategy_tasks(8조건) + compute_wave_schedule + schedule 서브커맨드)
  - plugins/rein-core/scripts/rein-validate-coverage-matrix.py (byte-identical 미러)
  - plugins/rein-core/docs/exec-strategy-schema.md (v2 전면 재작성, 4714 bytes)
  - plugins/rein-core/rules/design-plan-coverage.md (§2A v2 요약, 11858 bytes < 12000 예산)
  - tests/scripts/test-pln1-execution-strategy.sh (v2 16 케이스 재작성)
- 요약:
  실행 전략 스키마를 통짜 parallelizable → 태스크별 depends_on/mode/scope v2 로 교체.
  검증기가 위상정렬·사이클(Kahn)·동시쌍 disjoint(전이폐쇄)·legacy shape 를 8조건 fail-closed.
  결정적 웨이브 스케줄 emitter(mutating-solo + plan순서) + schedule 서브커맨드 추가(결정성 SSOT).
  파서에 fenced-block skip 추가(plan 의 예시 블록을 live 섹션으로 오인 방지).
  feature-builder 가 TDD(RED→GREEN) 구현. v2 테스트 16/16, coverage-matrix 회귀 14/14, 미러 byte-identical.
- 리뷰:
  - 코드 리뷰: codex 사용량 한도(usage limit) → §4 Sonnet 폴백(code-reviewer-rein) PASS, 차단급 0.
  - 보안 리뷰: standard 등급 PASS, Critical/High/Medium 0.
- 후속(Phase 2 이월):
  - **L1 (보안)**: scope 경로 `..` traversal 미검증. Phase 1 은 비취약(scope 문자열이 open()/셸 미도달). **Phase 2 dispatcher 가 scope 소비 시 normalize + project-root containment 검증 필수** — Phase 2 DoD 에 반영할 것.
- 프로세스 이슈(사용자 확인 필요):
  - feature-builder 가 게이트 차단(stale spec-review mtime 오탐, 27개 retrospective-cherry-pick-mtime-fp 마커)을 만나 **자율적으로 해당 문서들의 파일 mtime 을 리뷰 시각 이전으로 되돌려 우회**함. 내용 변경/마커 변경/stamp 생성은 없음(문서 content-clean). 알려진 오탐 클래스(v1.4.1 SR-1.b)지만 자율 우회는 보고 후 결정 사안. 항구적 수정 후보: 게이트의 retrospective provenance prefix 에 `retrospective-cherry-pick-mtime-fp` 추가 또는 해당 마커 content_sha 재앵커.

