reviewer: security-reviewer
timestamp: 2026-05-29T17:42:00
cycle: 2026-05-29-injected-rule-language-follow-user (v1.4.1 bundle release)
scope: integrated pre-public-release security pass over full v1.4.1 main bundle (git diff a474ea4, ~22 files). (1) injected rule text KR->EN + Output Language policy (rules markdown + tests); (2) SR-1.b spec-gate orphan backstop mtime->content_sha + content_sha writers (rein-mark-spec-reviewed.sh, rein-heal-legacy-pending.py); (3) P10 commit-am guard regex rewrite + rein-codex-review.sh wrapper fail-closed; (4) tests/ exemption in spec gate; release surface (plugin.json/rein.sh VERSION 1.4.1, CHANGELOG, README x2).
review_depth: standard
resolution: passed
blocking_findings: 0
checks_evaluated: standard 9-item set (secrets, SQLi, XSS, weak-hash, env-sensitive, deserialization, path-traversal, sensitive-logging, TLS)
findings: |
  - pre-edit-dod-gate.sh content_sha + git fallback: content_sha read from rein-owned trail markers is only string-COMPARED, never eval'd/interpolated -> no command injection. spec_path/rel_spec passed as separate argv with `--` separator on all git calls (ls-files/diff/log) -> no git-option injection. GIT_DIR/GIT_WORK_TREE/GIT_COMMON_DIR/GIT_INDEX_FILE sanitized via `env -u` on every git invocation (BC-INFO1 class, mirrors project-dir.sh); GIT_CEILING_DIRECTORIES intentionally preserved (can only narrow). git fallback gated to retrospective/healer markers only.
  - tests/ exemption: realpath normalizes `..` BEFORE commonpath component-wise containment -> tests/../src/evil.py, tests/../../etc/passwd, /etc/passwd, testsX/, src/tests-helper.ts all correctly NOT exempted. ValueError -> inside=False (fail-closed). Scoped relaxation (test files cannot bypass production-code review) -> not a defect.
  - P10 regex (pre-bash-safety-guard.sh): mistake-prevention guard, NOT adversarial. New regex is STRONGER than retired `git commit.*-[a-z]*a[a-z]*m` — now catches `-a -m`, `--all --message`, global-opt-prefixed (`git -c x commit -am`, `git --no-pager commit -am`), attached-quote (`-am"msg"`). Documented false-negative (adversarial quoted global-opt value with embedded spaces) is an explicit non-goal for a mistake guard; documented false-positive (over-block on quoted message containing ` -a `) errs toward blocking. No bypass introduced for the protected scenario (committing .env via auto-add).
  - rein-codex-review.sh / rein-mark-spec-reviewed.sh git calls: env-sanitized; wrapper now fail-closed ([ ! -r ] precheck + subshell function-existence verify under set -e). top-level scripts/ copies byte-identical to plugin source (no drift). version parity plugin.json==rein.sh==1.4.1.
  - content_sha hashing: sha256 used for content-integrity/change-detection (NOT password hashing) -> weak-hash check N/A. Untrusted-input handling safe: hashing operates on local spec bytes; failure paths fail-closed (mark-spec-reviewed refuses content-less stamp; gate treats unreadable as stale; healer degrades to git fallback).
  - no secrets/credentials, no sensitive logging, no TLS bypass, no insecure deserialization in added lines (scanned).
verification: |
  - bash -n + py_compile on all 6 changed scripts: pass
  - tests: pre-bash-safety-guard 48/48, spec-review-gate 37/37, tests-exempt 5/5, codex-review-wrapper 33/33, user-prompt-submit-rules + bootstrap-advisory: pass
  - P10 regex matrix (11 block / 5 pass scenarios): correct
  - tests/ containment traversal matrix (3 exempt / 9 blocked): correct
  - content_sha writer->reader contract: unchanged=match, mtime-bump=match (FP fixed), content-change=mismatch (stale): correct
remaining_issues: none
