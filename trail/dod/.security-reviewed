reviewer=security-reviewer
reviewed=2026-06-17T02:30:00Z
security_level=standard
user_level=auto
cycle=2026-06-16-commit-gate-security-surface-exempt
verdict=PASS
mechanism=llm-security-review
scope=v1.5.6 release re-confirmation — commit-gate security-surface exemption (pre-bash-test-commit-gate.sh check_review_stamp + _sx_* helpers). Hook code byte-for-byte unchanged since prior PASS (2026-06-17T01:19:12Z); fresh stamp required because version-bump code review (2026-06-17T02:04:09Z) became newer than the prior security stamp, tripping commit-gate M2 (security >= code freshness).
files_reviewed=plugins/rein-core/hooks/pre-bash-test-commit-gate.sh
threat_model=honest-agent-discipline (adversarial 우회 하드닝은 위협모델 밖, 단 fail-closed 필수 + 파서가 입력을 실행하지 않을 것)
checks=hardcoded-secret,injection,path-traversal,sensitive-info-logging,tls-https,deserialization,parser-purity,fail-closed,toctou,m2-hole-reopen
findings=0 critical, 0 high, 0 medium, 0 informational
code_unchanged=git diff db288f3(codex diff_base)..worktree -- pre-bash-test-commit-gate.sh EMPTY; last hook commit d0b9a3e; commits after d0b9a3e are trail/release-chore only (fac2000 version bump + CHANGELOG/README, audit-log trail records) — no hook logic change
notes=보안 게이트 자체의 면제 로직 재확인 (코드 무변경). 면제는 (a) eval/subshell/process-subst 부재 + (b) plain 단일-clause git commit(allowlist 옵션만) + (c) staged diff 전부 허용목록(문서/trail/버전문자열-only)일 때만. 그 외 전부 fail-closed.
evidence=(1) 파서 순수성 실측 — 7종 eval/subshell/process-subst/backtick/${} 공격 입력에 카나리 파일 미생성 → _sx_command_has_eval_or_subshell entry-guard(line 755-757)가 토큰화 전에 전부 REJECTED, $COMMAND 절대 미실행(순수 문자열 스캔); (2) form 거부 실측 — git add&&commit / cd&&commit / env GIT_INDEX_FILE prefix / 백그라운드 & / -C repo-redirect / -am / trailing pathspec / true;commit → 전부 _SX_FORM_OK=false(단일-clause 종착 + 옵션 allowlist); (3) M2-hole 차단 실측(temp repo) — src.py / 임의 .sh·.yaml·비버전 .json / rein.sh 비버전라인 변경 / plugin.json 비버전키 변경 → 전부 _SX_ALLOW=false; 버전문자열-only rein.sh + 버전키-only plugin.json + docs/trail-only 만 ALLOW; (4) audit 안전 — _sx_audit_exempt 는 고정 경로(PROJECT_DIR/trail/incidents/security-surface-exempt.log) append, format 은 timestamp+리터럴 라벨+정수 카운트 4개(staged_files/docs/trail/version)만 — 파일명·메시지·$COMMAND 미기록 → 로그 인젝션·path traversal 없음, { } 2>/dev/null||true 로 비차단(return 0 고정); (5) fail-closed — 토큰화 실패·미닫힌 따옴표·staged diff 비0/빈출력·예상외 status·JSON 파싱 실패·미상 명령형태 전부 false; (6) plugin.json 검사 임베디드 python 은 json.loads(try/except)만, eval/exec/pickle 없음, staged/head 를 argv 로 전달; (7) bash -n OK; 테스트 104/104 GREEN
freshness=이 stamp reviewed(2026-06-17T02:30:00Z)는 코드 stamp .codex-reviewed reviewed_at(2026-06-17T02:04:09Z)보다 신선, cycle 정확히 일치 (M2 충족)
