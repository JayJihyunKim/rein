# DoD — v2.0.3 배포 (보안 검토 축 복구 + git 필수 온보딩 안내 통일 + 런타임 상태 파일 무시 규칙)

- date: 2026-09-04 착수
- approved_by_user: true (2026-09-04 "오늘 들어온 이슈까지 끼워서 v2.0.3 배포하자")
- plan ref: 없음 (배포 사이클 — 대상 코드는 각 DoD 에서 리뷰·커밋 완료)


## 대상 변경 (dev 커밋)

- `54b1377` 보안 검토 축 무음 소멸 수리 (`dod-2026-09-02-security-axis-bundled-policy.md`, doctor 축별 진단은 제외)
- `4430959` 미초기화 프로젝트 세션 종료 훅 잔여물 (`dod-2026-09-02-stop-hook-residue.md`)
- `aafbc9c` git 필수 안내형 온보딩 (`dod-2026-09-02-git-required-onboarding.md`)
- `ab55438` `.rein/state.json` 무시 규칙·기존 프로젝트 보정·스테이징 삭제 지문 (`dod-2026-09-04-rein-state-gitignore-digest.md`)
- (이 세션, main 통합 리뷰 High + dev CI 실패 수리) 세션 시작 훅: 리눅스 POSIX 모드 특수 내장명령 리다이렉트 셸 종료 + 초기화된 프로젝트 표식 삭제 실패 무음 (`dod-2026-09-04-session-start-stuck-marker-rc0.md`)

## user-facing 영향 (versioning.md Rule A 판정)

- 사용자 대면 버그 수정 4건 묶음(보안 축·세션 종료 훅 잔여물·git 필수 온보딩·런타임 상태 파일) → **patch (2.0.2 → 2.0.3)**. git 필수 정책은 안내형(승인 후 진행)이고 `--allow-non-git` 로 이전 동작 유지 가능 → breaking 아님. Rule B: 오늘 main 머지 없음.

## 범위

1. 버전 표면 2곳 동기화: `plugins/rein-core/.claude-plugin/plugin.json` `version` + `scripts/rein.sh` `VERSION=` → 2.0.3 (publish VER-1 게이트).
2. `CHANGELOG.md` v2.0.3 항목 — user-facing 만(Rule C), 기존 추적 저장소의 `git rm --cached .rein/state.json` 안내 한 줄 포함.
3. README 두 벌 릴리즈 히스토리: 최신 1개 = v2.0.3, 이전 = v2.0.2 (1:1 parity).
4. dev 커밋(리뷰·보안 기록 후) → push dev.
5. main: 격리 작업트리에서 branch-strategy 포함 목록만 선별 체크아웃(`plugins/rein-core/**`, CHANGELOG, README 2벌, `scripts/rein*`, `tests/**`, `trail/**`, `.claude-plugin/`, `.github/workflows/`, `.gitignore`, `AGENTS.md`, `.rein/project.json`, `docs/{maintainer,troubleshooting,changelog-archive}`, `docs/agents-md-examples.md`) + dev 에서 삭제된 포함 경로 `git rm`. 제외: `.claude/**`, `docs/{specs,plans,brainstorms,reports}`, `.rein/{policy,cache,state}`, 루트 임시 노트.
6. main 트리 preflight = tests.yml 전량(hooks/scripts/skills/rules/agents/CLI/integration/unittest) + drift check + `claude plugin validate` GREEN.
7. main 커밋(리뷰 기록 후) → tag `v2.0.3` → push main + tag → mirror-to-public·publish-plugin 워크플로 success → `scripts/rein-verify-release.sh 2.0.3` 전량 PASS + 공개 트리 strip(trail/.rein/AGENTS.md/docs-specs 미노출)·plugin.json 2.0.3 도달 확인.

## 변경 파일

- `plugins/rein-core/.claude-plugin/plugin.json`, `scripts/rein.sh` (버전)
- `CHANGELOG.md`, `README.md`, `README.ko.md`
- (main 전용) 선별 체크아웃 결과

## 검증 기준

- [x] 버전 두 곳 2.0.3 일치, `claude plugin validate` 통과.
- [x] CHANGELOG 항목이 user-facing 만 담고 README 두 벌 릴리즈 줄이 1:1.
- [x] main 트리 preflight 전량 GREEN (dev-green ≠ main-green 함정 확인).
- [x] tag/push 후 mirror·publish 워크플로 success, 검증기 PASS, 공개 트리 strip 확인.
- [x] trail index 직전 릴리스 갱신 + 완료 기록.

## 라우팅 추천

agent: rein:docs-writer
skills:
  - rein:codex-review
mcps: []
security_tier: base
complexity: low
model_hint: sonnet
effort_hint: low
rationale:
  - 코드 변경은 버전 문자열 2곳뿐, 나머지는 문서(CHANGELOG/README) + 배포 절차 → docs-writer
  - 배포는 메인 세션이 직접 수행(선별 체크아웃·preflight·tag·push 는 판단·검증 작업)
approved_by_user: true

## 완료

- 2026-09-04 main `e5049c4` / tag `v2.0.3` / public `5e7033a`. 미러 success, 발행 success(재실행 1회 — 흔들리는 `test-job-transport.sh`), 검증기 6/6. 상세 `trail/inbox/2026-09-04-release-v2-0-3.md`.
