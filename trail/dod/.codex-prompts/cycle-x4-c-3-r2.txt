[NON_INTERACTIVE] [EFFORT:high]

Round 2 — applied Round 1 NEEDS-FIX fixes for cycle X4.C.3.

## Round 1 fix summary

**HIGH (Round 1)** — state.json 부재 / malformed / lock-fail 시 read_effective_mode 의 fallback "answer" 가 envelope skip 으로 잘못 사용되어 greenfield 환경의 legacy fallback contract 위반.

**Fix applied** — 3 hook (post-edit-design-plan-coverage-rule.sh, post-edit-routing-procedure-rule.sh, pre-edit-dod-gate.sh) 의 fast-path 블록에 `if [ -f "$STATE_FILE" ]` 게이트 추가. state-machine.sh source 후 `_state_machine_paths` 호출 → STATE_FILE 경로 resolve → 파일 존재 시만 read_effective_mode 결과를 fast-path 신호로 사용. 파일 부재 → legacy path 진입.

**MEDIUM (Round 1)** — T5 claim "4 patched hooks fail-soft" 인데 실제로 3 hook 만 검증.

**Fix applied**:
- T5 에 pre-edit-dod-gate fail-soft 케이스 (5.4) 추가 — state-machine.sh 부재 + DoD scaffold + dirty_files 매칭 의도 → fast-path NOTICE 미출력 검증. .pending marker 가 5.3 에서 잔존하므로 `.skip-spec-gate` touch 로 spec gate 우회.
- T5 의 legacy envelope contract 도 강화 — post-edit-design-plan-coverage / routing 의 stdout 에 `hookSpecificOutput` 존재 검증 추가.
- **신규 T6 추가** — `state.json 부재 + lib 존재` 케이스 (Round 1 의 가장 중요한 V5 미커버 케이스). 4 hook 모두 legacy 산출 정상 (envelope emit, marker 생성, fast-path NOTICE 미출력) 검증.

## 변경 파일 (변경 list 유지, 4개 hook + lib + 2 test)

- `plugins/rein-core/hooks/pre-edit-dod-gate.sh` — fast-path 블록에 STATE_FILE 존재 gate 추가 + 들여쓰기 정리
- `plugins/rein-core/hooks/post-edit-design-plan-coverage-rule.sh` — 동일 gate 추가
- `plugins/rein-core/hooks/post-edit-routing-procedure-rule.sh` — 동일 gate 추가
- `plugins/rein-core/hooks/post-edit-spec-review-gate.sh` — marker dedup (Round 1 동일, MATCH 판정)
- `plugins/rein-core/hooks/lib/state-machine.sh` — pipe+heredoc fix (Round 1 advisory PASS)
- `tests/hooks/test-state-fast-path-skip.sh` — T5 강화 + T6 신규 (총 6 test, 5/5 → 6/6)
- `tests/hooks/run-all.sh` — 신규 test 등록
- `tests/hooks/test-post-edit-design-plan-coverage-rule.sh` — Round 2.5 회귀 fix (아래 §Round 2.5)
- `scripts/rein-check-plugin-drift.py` — Round 2.5 회귀 fix (아래 §Round 2.5)

## Round 2.5 — test isolation 회귀 fix (이번 세션 systematic-debugging 발견)

**증상**: Round 2 resume 직전 `bash tests/hooks/run-all.sh` 가 2 SUITE FAILED.
- `test-post-edit-design-plan-coverage-rule.sh`: "docs/specs relative match — envelope assertion: empty stdout"
- `test-rein-validate-plugin-rules.sh`: "validator unexpectedly failed (rc=1): conditional hook empty envelope on matching path: post-edit-design-plan-coverage-rule.sh"

**근본 원인 (단일)**: X4.C.3 fast-path 가 `$CLAUDE_PROJECT_DIR/.rein/state.json` 을 읽는데, 두 caller 모두 project dir 을 격리하지 않음 → hook subprocess 가 `git rev-parse --show-toplevel` 로 메인테이너 dev repo 를 잡아 **live state.json (mode=answer)** 을 상속 → fast-path 가 envelope 을 skip → empty stdout. CI fresh checkout 엔 state.json 이 없어 green, live session 에선 red (non-deterministic). R-7 production 위험과는 별개의 test/checker hygiene 결함.

**Fix**:
- `test-post-edit-design-plan-coverage-rule.sh`: setup 에서 `SANDBOX=$(mktemp -d)` + `export REIN_PROJECT_DIR_OVERRIDE="$SANDBOX"` + trap cleanup → 모든 hook 호출이 state-absent legacy path 를 결정론적으로 검증.
- `scripts/rein-check-plugin-drift.py` `check_conditional_event_hook`: 본문을 `with tempfile.TemporaryDirectory()` 로 감싸 `env["CLAUDE_PROJECT_DIR"]` 를 격리 dir 로 pin (inject hook 체크는 미변경 — targeted). `import tempfile` 추가.

**검증**: 두 caller + `run-all.sh` ALL SUITES PASSED, `rein-check-plugin-drift.py` rc=0, temp dir leak 0, `rein-test.sh` 15/15.

## 검증 결과 (Round 2)

- `bash tests/hooks/test-state-fast-path-skip.sh` 6/6 PASS
- `bash tests/hooks/run-all.sh` ALL SUITES PASSED (62 suites)
- `bash tests/rein-test.sh` 15/15 PASS (CLI 표면 회귀 0)

## Verify focus (Round 2)

1. **State gate 강화 — false-negative 검증** — 3 hook 의 fast-path 가 STATE_FILE 존재 gate 통과 후에만 read_effective_mode 결과를 신뢰. malformed JSON 케이스도 cover 되었나 (read_state default fallback + stderr NOTICE) — 기술적으로 state.json 이 파일로 존재하지만 schema_version != 1 인 경우는 여전히 read_state 가 default state (mode=answer) 반환 → fast-path 진입. 본 cycle scope 에서 이게 acceptable 한지, SPIKE (X4.C.4) 에서 추가 검증 권고.

2. **T5/T6 coverage** — T5 는 lib 부재, T6 는 state.json 부재 (lib 존재). 둘 다 4 hook (pre-edit-dod-gate / post-edit-design-plan-coverage-rule / post-edit-routing-procedure-rule / post-edit-spec-review-gate) 검증. design memo §3.4 의 "legacy fallback contract" 와 일치.

3. **R-7 보수적 skip 잔존 위험** — pre-edit-dod-gate 의 fast-path 는 STATE_FILE + mode=source_edit + file in dirty_files 모두 충족 시만 validator subprocess skip. plan 자체가 같은 burst 에서 변경됐다면 stale validator 결과를 reuse 하는 false positive 가능. 본 cycle 의 mitigation 한계 (advisory) + SPIKE 측정 (X4.C.4) 으로 confirm.

4. **Round 2.5 isolation fix 가 R-7 을 가리지 않는가** — 두 caller 의 project-dir 격리는 default(state-absent) envelope contract 를 결정론적으로 검증할 뿐, fast-path 의 실제 mode=answer skip 동작은 `test-state-fast-path-skip.sh` (T2/T3) + R-7 SPIKE 가 계속 cover. isolation 이 stale-state false-positive 를 production 에서 숨기지 않음을 확인.

## 검토 항목

- (a) Code Defects — Round 1 issue 해소 여부 + 새 회귀 없는지
- (b) Design Alignment — STATE_FILE gate 가 design memo §3.4 "legacy fallback contract" + §6.2 보수적 skip 와 일치
- (c) Test Alignment — T5/T6 가 V5 contract 를 적절히 검증
- (d) Claim Audit — Round 1 의 "4 hooks" claim 이 6 test 로 확장됨
- **FINAL_VERDICT: <PASS|NEEDS-FIX|REJECT>**
