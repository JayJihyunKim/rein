# DoD: v2.2.0 릴리스 — 리뷰 게이트 차단 기준·재리뷰 강도 계약 + 보안 통과 기록 발급 주체 이관 (CI 없이 로컬 검증·배포)

- 작성일: 2026-09-23
- plan ref: 없음 — 릴리스 커밋(버전 표기·안내문)과 배포 절차. 설계·계획 없이 이 기준서와 `versioning.md`·`branch-strategy.md` 절차로 닫는다.
- 사용자 지시: "릴리즈는 하는데 ci나 workflow는 돌지마 지금 원격 레포에서 크레딧 결제 문제로 안돌아. 최대한 로컬에서 검증한다음 배포만 해라" (2026-09-23)
- 등급 근거(Rule A): 두 사이클 모두 사용자 프로젝트의 skill/agent/rule 동작이 바뀌는 새 동작(`/codex-review` 차단 기준·조건부 재리뷰 강도·이벤트 로그 필드, 지휘 경로의 부모 발급 절차) → user-facing 신규 기능 → **minor, v2.2.0**. breaking 아님(CLI·exit code·hook 차단 범위 불변). Rule B: 09-23 첫 main 머지. Rule C: 죽은 워크플로 제거·`.gitignore`·테스트·메인테이너 규칙은 CHANGELOG 제외.

## 범위

IN
1. 버전 표기: `scripts/rein.sh` `VERSION="2.1.3"` → `2.2.0`, `plugins/rein-core/.claude-plugin/plugin.json` `"version"` → `2.2.0`.
2. `CHANGELOG.md` 에 v2.2.0 절(사용자 대면 항목만 — 초안 `scratchpad/release-2.2.0-drafts.md`).
3. `README.md`·`README.ko.md` 릴리스 히스토리 절: 최신 = v2.2.0, 이전 = v2.1.3 (두 언어 병렬).
4. 배포 절차(코드 변경 없음): 로컬 preflight(tests.yml 의 우분투 잡 8종 + BusyBox awk 대역으로 훅·스크립트·스킬 스위트 재실행 + drift) → main 선별 체크아웃(격리 worktree, branch-strategy 포함 목록; 삭제된 워크플로는 `git rm` 동반) → main 트리 preflight → 태그 `v2.2.0` → main·태그 push(Actions 는 과금 문제로 미실행 — 실패/미시작 무시) → **로컬 미러**(strip 단계를 임시 클론에서 실행 → public main force-push → 태그 전파 → 사후 검증) → **로컬 발행 자체 검사**(`scripts/rein-publish.sh` 셀프 호스트 모드로 tarball 빌드·매니페스트 갱신이 되는지 임시 출력으로 확인; Anthropic 채널은 CI 와 같이 건너뜀) → GitHub Release(공개 저장소, CHANGELOG 사용자 항목).

OUT
- 코드·규칙·스킬 본문 변경 — 이번 릴리스에 들어가는 변경은 이미 dev 에 커밋·리뷰됨(`7c1cd1e`, `292cd16` 병합, `ce73bda`).
- GitHub Actions 과금 복구 — 사용자 계정.
- 원격 CI 실행 — 사용자 지시로 금지.

## 계획 대비 이탈

- 릴리스 커밋 코드 리뷰 3회차가 정본 `plugins/rein-core/skills/codex-review/SKILL.md` §2 79행·§6.3 359행의 "직전 보고에 High 가 잔존" 이 "아직 안 고친 High" 로도 읽혀 "고쳤지만 직전 보고에 High 가 있었던 회차" 의 강도가 정본에 정의되지 않았다고 지적(High). 의도는 처음부터 "고쳤더라도 그 수정은 아직 독립 검토 전이므로 high"(같은 행의 "High 를 고쳤다 는 (a) 충족이 아니다" 와 한 뜻) — 정본 두 줄을 "High 가 있었거나(그 High 를 고쳤더라도 — 수정은 아직 독립 검토 전이다)" 로 명확히 하고 안내문은 그대로 둔다. 변경 파일에 스킬 추가(계약 문구 명확화 — 동작 의도 불변).
- 로컬 preflight 를 v2.1.3 트리(`3db148f`)와 비교해 **회귀 1건 발견**: `feature/security` 병합(웨이브 2, `2a8895a`)이 `plugins/rein-core/skills/parallel-execute/SKILL.md` 5번 항목에 참조 괄호를 넣어 파일이 6122→6260 바이트가 되어 `tests/skills/test-parallel-execute-skill.sh` 의 크기 예산(6144)을 초과 — dev CI 가 과금 문제로 돌지 않아 병합 시 잡히지 않았다. 수정: 괄호 문구 압축(anchor 참조 유지) + frontmatter 설명과 중복되던 "목적" 문단·스키마 안내 한 줄 축약(테스트가 요구하는 키워드 전부 유지). 변경 파일에 추가.

## Definition of Done

- [ ] 버전 표기 2곳 일치(2.2.0), CHANGELOG·README 두 언어 갱신, 릴리스 커밋 코드 리뷰 + 보안 리뷰(`scripts/rein-*.sh` floor 경로) 통과
- [ ] 로컬 preflight: 기준선(dev `f2ad6fd` 복제본) 대비 새 실패 0 — 이 기기 선재 실패는 기준선과 같은 집합
- [ ] main: 선별 체크아웃 결과 제외 경로 잔존 0, 비-trail 파일 dev 와 바이트 동일, 태그 `v2.2.0` = main tip
- [ ] public: main = strip 된 커밋, 태그 v2.2.0 = 같은 커밋, 메인테이너 전용 경로 0건, plugin.json·rein.sh 2.2.0
- [ ] GitHub Release v2.2.0 생성(Latest), 완료 기록·index·메모리 갱신, dev push

## 검증 기준

- `grep -c '2.2.0' scripts/rein.sh plugins/rein-core/.claude-plugin/plugin.json` → 각 1 이상; `bash tests/rein-test.sh` 통과(버전 정합 포함)
- `bash <scratch>/local-preflight.sh <clone> release` 의 스위트별 FAIL 수가 `baseline` 과 동일
- `git ls-tree main -- <제외 경로들>` 빈 출력; `git diff --stat dev main -- . ':!trail' ':!.claude' ':!docs/specs' ':!docs/plans' ':!docs/brainstorms' ':!docs/reports' ':!docs/maintainer'` 빈 출력
- `git ls-remote --tags git@github.com:JayJihyunKim/rein.git v2.2.0` = strip 커밋; public 트리에 `trail/`·`.rein/`·`AGENTS.md`·메인테이너 워크플로 없음
- `gh release list --repo JayJihyunKim/rein --limit 3` 에 v2.2.0

## 라우팅 추천

agent: rein:docs-writer
orchestration: standalone
worker_strategy: 없음 — 안내문·버전 표기 5개 파일 + 배포 절차는 메인 세션 단독(절차의 각 단계가 앞 단계 결과에 의존)
skills:
  - rein:codex-review
mcps: []
security_tier: standard      # scripts/rein.sh(floor 경로) 한 줄 + 공개 저장소 force-push·태그 — 배포 경계
complexity: medium
model_hint: sonnet
effort_hint: medium
rationale:
  - 코드 변경 없는 안내문·버전 표기 → docs-writer 분류. 릴리스 커밋은 관례대로 codex 리뷰(안내문의 사실·영향 범위 주장 감사) + 보안 리뷰(floor 경로·배포 경계)
  - 배포는 CI 대체 — 미러·발행 워크플로의 동작을 로컬에서 같은 스크립트로 재현하고 사후 검증을 코드로 확인
approved_by_user: true  # 사용자 지시 2026-09-23 "릴리즈는 하는데 … 로컬에서 검증한다음 배포만 해라"

## 변경 파일

- scripts/rein.sh
- plugins/rein-core/.claude-plugin/plugin.json
- CHANGELOG.md
- README.md
- README.ko.md
- plugins/rein-core/skills/codex-review/SKILL.md
- plugins/rein-core/skills/parallel-execute/SKILL.md
- trail/inbox/2026-09-23-v2-2-0-release.md
- trail/index.md
