# DoD: v1.6.6 릴리스 — 안전 소릴리스 배포 (로그 봉합 + 자동 CI + preflight)

- 날짜: 2026-08-07
- slug: release-v166
- 유형: 릴리스 실행 (버전 승격 + main 선별 체크아웃 + 태그 + push + publish 검증)
- 입력: `trail/dod/dod-2026-08-07-v1-safety-prerelease.md` 사이클 완료분 (dev `3e1213a`+`3ab5ca2`, 리뷰 3R+보안 PASS)
- 사용자 결정 (2026-08-07): 릴리스와 v2 설계 정식화 병렬 진행. 등급은 patch (v1.6.6) — 사용자 노출 변경은 hook 로그 동작(버그/보안 수정 계열).

## 범위

1. 버전 승격 (patch): `scripts/rein.sh` VERSION 1.6.5→1.6.6 + `plugins/rein-core/.claude-plugin/plugin.json` version 동기 (VER-1 두 표면 규칙).
2. `CHANGELOG.md` — user-facing 항목만: 차단 로그 민감정보 봉합 (사용자 저장소 hook 동작 변경). CI/preflight 는 메인테이너 인프라라 제외 (Rule C).
3. `README.md`/`README.ko.md` — 버전 히스토리 최신 1건 엔트리 (1~2줄, parity 유지).
4. main 선별 체크아웃 — **격리 worktree 에서** (dev tree 는 병렬 스펙 작업으로 dirty 가능): branch-strategy 포함 목록의 변경 파일만. 태그 `v1.6.6`, push origin dev/main/tag.
5. publish 검증 — **preflight 첫 실전 구동**: preflight tests(macOS job 실행 여부 육안 확인 — 직전 사이클 이월 검증 항목)+drift 통과 후 publish 실행 확인, mirror strip 확인, public tag clean 확인.

6. **hotfix (릴리스 중 편입, 2026-08-07)**: 첫 Linux CI(dev push + preflight)가 `tests/hooks/test-state-fast-path-skip.sh` T9 로 잡아낸 `plugins/rein-core/hooks/lib/state-machine.sh` `acquire_state_lock()` flock 경로 fail-open 수리 — open/flock 실패 미검사 return 0 + timeout 계약 미구현 (macOS 는 flock 부재로 mkdir 경로만 검증돼 은닉). exec 실패·flock 실패 전파 + `-w` timeout 적용. 태그 v1.6.6 은 수리 포함 커밋으로 재지정 (미배포 상태에서의 이동 — publish 는 preflight 차단으로 미실행이었음).

### 범위 외

- 코드 변경 (직전 사이클에서 완료 — 본 DoD 는 배포 절차만).
- v2 설계 정식화 (병렬 트랙 — 별도 산출물).

## 변경 파일

| 파일 | 변경 |
|---|---|
| `scripts/rein.sh` | VERSION 1.6.6 |
| `plugins/rein-core/.claude-plugin/plugin.json` | version 1.6.6 |
| `CHANGELOG.md` | v1.6.6 user-facing 엔트리 |
| `README.md` / `README.ko.md` | 버전 히스토리 엔트리 (parity) |

## 검증 기준

- [ ] 두 버전 표면 값 일치 (1.6.6).
- [ ] CHANGELOG 는 user-facing 만 (내부 CI 항목 제외).
- [ ] README KR/EN 엔트리 동기.
- [ ] 릴리스 델타 리뷰 통과 (버전·문서 변경분 — codex 한도 중 대체 경로, 사유 기재).
- [ ] main 커밋이 포함 목록만 담음 (`git status --short` 예상 외 파일 0).
- [ ] tag push 후 preflight 가 실행되고 **통과 후에만** publish 실행 (Actions 로그 확인).
- [ ] preflight 에서 macOS job 실행 확인 (이월 검증 항목).
- [ ] mirror: public repo 에 내부 기록(trail/ 등) strip 확인, public tag 가 clean commit 을 가리킴.
- [ ] Rule B: 오늘 main 머지 첫 회.

## 라우팅 추천

- agent: 없음 — 메인 세션 직접 (릴리스 절차는 git 상태 조작이라 위임 부적합)
- skills: `rein:codex-review` (델타, 대체 경로), 보안은 버전·문서-only 커밋 자동 면제 대상
- mcps: 없음
- rationale: v1.6.5 릴리스와 동일 패턴. 병렬 스펙 트랙과 파일 disjoint — main 작업만 worktree 격리로 충돌 차단

approved_by_user: true
