# DoD — v2.1.1 patch 릴리스 (서브에이전트 stash 차단 + 가드 절 시작 확대 + 초기화 스크립트 수리)

- date: 2026-09-08 착수
- plan ref: 없음 (배포 사이클 — 구현은 `dod-2026-09-08-post-v210-low-followups.md` 로 종결됨)
- approved_by_user: true (2026-09-08 "CI 확인하고 patch로 릴리스 준비해")
- 선행: dev `f74eca2`(코드) + `6c49f31`(마감) push 완료, dev CI success(6c49f31). 코드 커밋은 codex 4회차 뒤 사용자 승인 종결 + 부모 자체 검토 기록, 보안 검토 PASS(대상 없음).

## 배경

v2.1.0 이후 잔여 Low 후속 일괄 수리가 사용자 대면 변화를 만들었다 — Bash 안전 가드의 서브에이전트 `git stash` 차단[P12](서브에이전트 안에서만 발동, 메인 세션 무영향), 공유 절 시작 분류기의 POSIX 확대(제어문·백틱·중괄호 안 호출을 P10/P11/P12 가 잡음; 예약어 뒤 언급은 보수 방향 차단), 초기화 스크립트의 비 UTF-8 `.gitignore` 보존·`project.json` 디렉터리 거부. (`scripts/rein.sh` 의 `: >` 제거는 `set -e` 아래라 사용자 동작 변화가 없는 위생 수정 — CHANGELOG 제외, codex 1회차 High 정정.) 등급 **patch** — 사용자 결정(2026-09-08 "patch로"). 근거: 차단 범위 확대는 문자 그대로 breaking 후보이나 versioning Rule A 의 사용자 승인 예외 세 조건을 만족(차단 채널 JSON deny 그대로·안내문이 직접 편집/blocked 보고 경로 제시·비용 비소모)하고, 내용이 결함 수정(워커 stash 재발·게이트 우회 형태·초기화 traceback·리눅스 셸 종료) 묶음이라 patch 로 둔다. Rule B: 오늘 main 머지 없음(직전 09-07 v2.1.0).

## 범위

1. 버전 표면 2곳 동기화: `scripts/rein.sh` `VERSION="2.1.1"`, `plugins/rein-core/.claude-plugin/plugin.json` `"version": "2.1.1"`.
2. `CHANGELOG.md` v2.1.1 항목 — user-facing 만(Rule C): 서브에이전트 stash 차단(발동 조건·예외·안내), 가드 절 시작 확대(잡히는 형태·적용 규칙 네 가지·보수 방향 오차단 안내), 초기화 스크립트 수리 2건. 등급 메모는 넣지 않는다(이 DoD 에 기록). 테스트·래퍼 변수 정합·스킬 문구는 제외.
3. `README.md` / `README.ko.md` 최신 릴리스 줄 갱신(1:1 parity, 상세는 CHANGELOG 링크).
4. 릴리스 커밋(dev) → codex 리뷰 + 보안 리뷰 → dev push → CI 초록 확인.
5. main 선별 체크아웃(branch-strategy 포함 목록: `plugins/rein-core/**`, `scripts/rein-*.sh`, `scripts/rein.sh`, `tests/**`, `CHANGELOG.md`, `README*.md`, `trail/**`; 제외: `docs/specs|plans|brainstorms|reports|maintainer`, `.claude/**`, `.rein/policy/**`, 루트 메모) → main 격리 worktree 에서 preflight 전량 배터리(`.claude/.gitignore` `*` 선생성, `env -u CLAUDE_PLUGIN_ROOT`) → 통합 리뷰 1회 → main 커밋 → 태그 `v2.1.1` → push(main+tag) → mirror/publish 워크플로 success 확인 → 공개 저장소 `gh release create v2.1.1 --repo JayJihyunKim/rein --verify-tag --latest` → 검증기.
6. 포함하지 않음: 정규식 분류기의 파서급 한계(별도 설계 후보), 코드 변경.

## 변경 파일

- scripts/rein.sh
- plugins/rein-core/.claude-plugin/plugin.json
- CHANGELOG.md
- README.md
- README.ko.md

## 검증 기준

- [ ] 버전 두 곳 `2.1.1` 일치, `claude plugin validate plugins/rein-core` 통과, drift 검사 OK.
- [ ] CHANGELOG 에 user-facing 항목만(등급 메모 없음), README 두 벌 parity — 영문 릴리스 줄에 한글 없음.
- [ ] 릴리스 커밋 codex 리뷰 PASS + 보안 리뷰 → dev push → dev CI success.
- [ ] main 선별 체크아웃 후 제외 경로 0, main 격리 트리 preflight(skills/hooks/scripts run-all + python 스위트) 전부 0, 통합 리뷰 PASS.
- [ ] 태그 push 후 mirror-to-public·publish-plugin 워크플로 success, public 트리 strip 검증(trail·.rein·AGENTS.md·docs 내부 폴더·.claude 0건), plugin.json 2.1.1.
- [ ] `gh release list --repo JayJihyunKim/rein` 에 v2.1.1 Latest.
- [ ] index.md "직전 릴리스" 갱신 + 완료 기록.

## 라우팅 추천

agent: rein:docs-writer
skills:
  - rein:codex-review
mcps: []
security_tier: standard
complexity: medium
model_hint: sonnet
effort_hint: medium
rationale:
  - 버전 문자열 2곳 + 변경 안내 문서 3개, 코드 동작 변경 없음; 절차 위험은 main 선별 반영·preflight 에 집중
approved_by_user: true
